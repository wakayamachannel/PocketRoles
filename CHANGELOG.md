# Changelog — PocketRoles

Among Us **2026.8.18**（Steam / Windows）向けのホスト専用役職 MOD。日付は日本時間。詳しい説明は `README.md`（日本語）/ `README.zh-CN.md` / `README.en.md`。

## v0.5.5 — 2026-09-24

- **必要なアップデート・版ごとに書ける判定値・ゆるめる変更は全員に**（作者の決定 9/22「やろう」: 「版ごとに書ける仕組み」「アップデート必須」「ゆるめる変更は全員が受け取る」、質問「バニラを遊びたくなったらどうするの？」）:
  - **必要な版**: 署名付きの定義ファイルの新しい節 `[update]` に `minmod = 0.5.6` と書くと、それより古い PocketRoles は部屋を作れなくなります（「作成」ボタン・切断後の自動の作り直し・`/move` などの作り直しを断る。いまの部屋と試合はそのまま。人の部屋に入る・設定・フリープレイはそのまま。`/mod off` や登録オフでも同じ）。タイトル画面の上に 3 行のお知らせ（1 行目は赤い字）と、少ししてからくわしいお知らせ（3 言語。理由・アップデートのしかた（どの版のランチャーにもある「更新を確認」）と、どれも「Steam から起動するふつうの Among Us は、いつもどおり遊べます」を書き、Steam のフォルダーそのものに入れている時は、ふつうに遊ぶには `winhttp.dll` を外すと書き分けます）。部屋の中で必要になった時は、ホストの画面にだけお知らせとチャット（フリープレイでは出しません）。左上の表示に `(update required)`。`/ac rules` の見出しのすぐ下に「必要な版: v0.5.6 以上」の 1 行。`RemoteRules` に関係なく、最後に署名を確かめた定義ファイル（オフラインならキャッシュ）の値を使い、止まっている間はオンラインの画面を開いた時か部屋を断った時に 5 分に 1 回まで取り直します（作者が下げたらその場で戻る）。メジャー版が 2 つ以上先か 999 より大きい数の必要な版は書きまちがいとみなして使いません。条件の付いた行（`minmod = 0.5.7 @game>=2026.9.1`）も書けます。0.5.6-beta のようなテスト版は 0.5.6 より前として数えます。結果は `BepInEx\PocketRoles\update-required.txt` にも書きます（ランチャー用）。テスト用の設定 `[Diagnostics] SimulateMinMod`（この PC だけ、その必要な版があるように動く。本当の値は下げられない）。
  - **ランチャー**: 「起動」を押した時に必要な版より古いと「アップデートが必要です」の画面で「アップデート」「ふつうの Among Us (Steam)」「やめる」だけ（「このまま遊ぶ」はなし）。「アップデート」は GitHub の最新版を入れてそのまま起動（API の回数制限の時はリリースのページから探す。最新のリリースが必要な版より古い時はそう出して起動だけ（作者が直すまで部屋は作れない。Steam のふつうの Among Us はそのまま、とも出す）。必要な版より古いものは入れず、古い版に戻すこともしない）。状態の欄に「必要な版」と、赤い字のお知らせ（Steam のふつうの Among Us はそのまま）。友達モードに **「ふつうの Among Us (Steam)」ボタン**を追加。必要な版はトレイアプリと MOD が書いたファイルの、新しい定義ファイルの方から読みます。開発モードはログだけ。
  - **トレイアプリ**: スキャンに「PocketRoles の版」の行（14 行に。足りない時は黄色の行と直し方、通知。起動は止めない）。ランチャー用に `%LOCALAPPDATA%\PocketRoles\Aegis\update-floor.txt` を書きます（新しい接続はなし）。
  - **版ごとの行**: `[rules]` の行の終わりに `@mod<=0.5.5` や `@mod>=0.5.6, game>=2026.9.1` を付けると、その版だけに効きます。上から読んで最後に当てはまる行が使われます。この版で判断できない条件（知らない言葉・書きまちがい・起動直後でまだ分からないゲームの版）の行は、ゆるめる時だけ使うので、厳しくなることはありません（ゲームの版が分かったら読み直します）。ルールの強さは今までどおり組み込みより厳しくできません。
  - **ゆるめる変更は全員に**: `[AntiCheat] RemoteRules = false` でも、署名を確かめた定義ファイルから、退出させないようにゆるめる変更（知らせるだけ・オフ）と必要な版を受け取ります（数値と NG ワードは組み込み、共有 BAN は使わない（今までどおり）。くり返しで退出への変更はまだ退出させるので受け取りません）。`/ac rules reload` もオフのまま使えます。設定の説明・README・`/ac rules` の表示を合わせました。
  - **署名の道具**（`tools\sign-definitions.ps1`）: 条件と `[update]` は、どの MOD でも同じに読める形だけ署名（mod / game、数字 3 つの版）。`[rules]` 200 行・`[update]` 50 行まで。minmod はリポジトリの版より新しくできず、上げる時は GitHub の最新リリース以下かを確かめます（つながらない時は署名しない。上げない時はネットを使わないので、BAN 管理のオフラインの署名はそのまま）。下げる・消す時は画面で確認。比べる元はいつもリポジトリの `aegis\definitions.txt`（署名の合うコミット済みの最新）。署名の前に「v0.5.6 より前の PocketRoles は部屋を作れなくなります」と版ごとの値を表示。
  - `build-release.ps1`: `PocketRolesPlugin.Version`・csproj の `<Version>`・DLL の版が違うとリリースしません（MOD とランチャーが同じ版で比べるため）。共通のテスト `tests\aegis-scope-vectors.txt`（MOD・トレイ・署名の道具・ランチャーで同じ結果）。
- **BAN 管理を見やすい色に・返事の案を AI で**（要望 9/22「BAN管理もなんか水色でみずらくない？返事は管理でできるの？そうならそこにもai搭載して欲しい」）
  - **見やすい色**: 水色（#26C6DA）を本文・見出し・枠・ボタン・チップのすべてに使っていたのをやめ、ブランドの紺（#1E2257）の下地、少し温かい白（#F3EFE6）の文字、読める灰色の補足（#C3BFD2）にしました。**金（#F7C548）はダイアログの主なボタンだけ、青緑（#2EC4B6）は選んだものだけ**。見直しで、キーボードの位置を金の枠から**温かい白の枠**に（金のボタンの上で 1.00:1、青緑の上で 1.35:1 と見えなかったため。ぬったボタンでは枠との間に紺の線をはさみ、枠と紺 15.98・紺と金 11.38・紺と青緑 8.46）、注意の色を金と分けた**橙（#FFA657。下地の上で 9.47）**に、検索で当たった行の字は金のまま。入力欄の枠 #6A70A8 → #7479B3（Panel2 の上で 3.16 → 3.61、下地の上で 4.48）。明るさの比は、補足の暗い灰色が 3.99〜4.28 → 5.25 以上、補足 7.93 → 10.22、本文 15.98、区切り線 1.47（飾りだけに）。見出しは「▍」の短い線＋本文の色、返事の例の BAN の番号は枠の中（ⓘ / ⚠）、状態のしるしは色だけでなく **✓（よい）・✕（だめ）・⚠（注意）・△（状況証拠）**と言葉で（ログの確認・署名・ホストの数・方針タブもそろえた）、選んだタブ・種類は青緑でぬりつぶし。プルダウン（青い選択・明るい灰色の矢印）、入力欄と複数行の欄（灰色・白の枠、Windows の灰色の空欄のヒント）、**期間を選ぶ画面の丸と日数の欄**（Windows の白い丸・青い点・灰色の上下ボタン）を暗い画面に合わせて描き直し（DarkCombo・FieldSkin・DarkRadio・DarkCheck）。8.5pt の小さい字は 9pt 以上に（ロゴの BAN CONSOLE を除く）。盾のアイコンの色はそのまま（assets\Aegis.ico・トレイアプリと同じ）。レイアウトは変えず、余白を 8px 刻みに（ダイアログの端 24px・ボタン 36px、プルダウンの下も入力欄と同じ 14px、件名の欄の上の余分な 4px をなくす）。候補の詳しい欄の下のボタンの左右は 10px のまま（1340px で 1 行に収まる）。**返事の例は、ノート PC の低い画面（826px・704px）でも下のボタンが隠れないように**、注意書きとボタンを下に固定し、上の欄はスクロール、本文は窓の高さに合わせて伸び（最低 200px）、窓の高さを変えられるように。開いた時の位置は宛名の欄（言語のプルダウンではなく）。
  - **AI で返事の案を作る**: 「返事の例」に「AI で返事の案を作る」「元の文に戻す」「送る内容」「AI の設定」。作者の PC で**作者として解除した時だけ**（Windows Hello か署名の鍵。4 けたのコードでは作者の料金を使えない）、押した時だけ、返事の種類・言語・BAN の番号・ルールの種類・日付・返事の例の文と、**結果の段落（元の文のまま写す段落）**を Claude API（`POST /v1/messages`、既定 `claude-sonnet-5`、effort low、max_tokens 8000、温度なし。Claude Opus 5 / Fable 5.1 ではサーバー側の予備のモデル）に送ります。「情報が足りない」は番号・日付・ルールも送りません。宛名と担当者の名前は「NAME_1」「STAFF_1」、**共有 BAN の行の番号（ハッシュの頭 8 けた）は「BANID_1」**にして送り、戻った案にこの PC で入れ直します。PUID・フレンドコード・ハッシュ・証拠ファイル・ルール名は送りません。返ってきた案は手元で確かめてから本文へ: **結果の段落がそのままであること・結果と反対の言葉がないこと**（受け入れないのに「解除しました」、受け入れるのに「解除できません」など）、元の文にない AEG 番号・8 けた以上の番号・日付（**月の名前・日が先・年のない書き方も**）・メールアドレス・URL（**`discord.gg/…` のような短いもの・@ の名前も**）・フレンドコードがないこと、**元の文の番号と日付が抜けていないこと**、問い合わせ先のメールアドレス、「情報が足りない」の番号・日付、宛名・名前の置き場所、担当者の名前、書式の記号、長さ。使った案は「AI の案です。送る前に確かめてください」と「元の文に戻す」、**AI が書き直した行に色**。種類・言語・宛名・担当者を変えると元の文に戻り、作っている途中の案はやめて「作っていた案をやめて元の文にしました」と出します。**「相手の文を貼る（任意）」は、プライバシーポリシーの D-04 が決まるまで出しません**（「AI の設定」でオン。オンの時の注意書きはポリシー 3.5 の 5 のとおり（メールと Discord のチケットの文は D-04 まで貼らない）、フレンドコードは名前ごと・メールアドレス・URL・@ の名前・長い番号を伏せ、宛名・記録の名前・担当者の名前を記号に（ほかの人の名前などは伏せられないことも書く）、**初めて送る前に送る文をそのまま見せて「送る／送らない」**）。「送る内容」は、今送られる文をそのまま見せます。**管理人の PC には AI の部分を出しません**（前は押せないボタンと「キーがありません」が出ていた）。
  - **AI の設定**（作者の PC で、作者として解除した時だけ）: API キーは伏せ字で入れ、Windows の DPAPI（この Windows ユーザーだけ）で状態フォルダーの `ai-key.dat` に保存。表示もログへの記録もしません（エラーの文にキーが入っていても伏せます）。保存は新しいキーを先に置き換えてから古いものを上書きして消し、**「キーを消す」は保存の途中で残った控え（.tmp・.old）も消して、消せなかった時はそう出します**。「確かめる」（`GET /v1/models/{model}`、無料）・モデル・待つ時間（15〜180 秒、既定 60）・「相手の文を貼る」の欄を出すか（既定はオフ）・今月の回数と金額の目安（settings.txt には数字だけ）。送るもの・送らないもの・送った後を 3 つに分けて 9.5pt で。**Anthropic での扱い（学習・残る期間）は画面では言い切らず**、「Anthropic の API の約束とプライバシーポリシーのとおり」とだけ書きます（約束は変わることがあり、画面の文はスクリプトに入るため）。「やめる」を押した時は、キーを読んでいる間でも送りません。ban-console.log には案を頼んだ時の解除のしかた（作者・管理人と方法）・種類・相手の文の有無だけを残します（文は残しません）。サポート窓口のサーバー（スタッフのログイン）用の口（`IReplyDrafter`・`DeskDrafter`）だけ用意。
  - -SelfTest 414 項目（palette 4・ai 73・clip 5 を追加。AI は 127.0.0.1 の模擬サーバーと偽のキーだけで、送る項目・ヘッダー・エラーの対応・時間切れ・つながらない・やめる（送る前にやめた時も）・キーの暗号化とログに出ないこと・控えの消去・古い案を捨てること・元の文に戻すこと・今月の集計・相手の文は既定で出ない・相手の文の名前とリンクの伏せ方・共有 BAN の行の番号・結果の段落・反対の言葉・日付の書き方・抜けた番号と日付・作者として解除した時だけ・管理人の PC では出ない・実際に押した時の流れ（作る・やめる・途中で種類を変える・相手の文を聞く・途中で閉じる）・低い画面（826px・704px）で下のボタンが見えること、中文と English の画面の切れ）。README 3 言語。
- **記録の自動削除と、作者がまとめて消す「消去の一覧」**（要望 9/22「この仕組みどうにかできない？せっかくダウンロードした遊ぶだけなのにこれじゃあ利点が台無しだ。こっちで一括管理してあげたい。余計な負担はかけたくない」）: 記録を消してほしい参加者は作者に 1 回連絡するだけで、ほかのホストの作業はありません。
  - **30 日で自動で消す**（設定なし・ホストの作業なし）: 名前・履歴・部屋コード・送った通報の控えは、いらなくなってから 30 日で消えます。証拠の記録は、作られてから 90 日で消えます（オーナーの決定 9/23。BAN されていない人も 90 日。前は 30 日）。今も効いている BAN（その PC の BAN。共有 BAN の一覧に載っている人は、その PC に BAN の記録がある時）は、効き続けるための記録と、その記録が指している証拠を残します（証拠は BAN が終わってから 30 日まで。作られてから 90 日より早くは消しません。無期限なら残り続けます。名前と、30 日より古い入室の記録は、最後に見かけてから 30 日で消します）。共有 BAN の一覧に載っている人でも、その PC の BAN の記録が指していない証拠（関係のない退出など）はふつうどおり 90 日で消えます。日付のまったくない記録は、見つけた日から数えます。`aegis-bans.json` が壊れて読めない時は、最後に書き換えてから 30 日はそのまま（その中の証拠の番号は残し、ほかの証拠はふつうどおり消します）、そのあと控え（`.broken-<日時>`。名前の日時から 30 日で消えます）に移して、使っている BAN で作り直します。違反の回数は、最後の BAN が終わってから 1 年だけ数えます（段階の 30 日 → 180 日 → 無期限も、1 年たつと最初から。`/aegis unban` の返事も「1 年残る」に）。その 1 年はハッシュ・回数・日付だけを残し（フレンドコードのハッシュは PUID のハッシュに置き換え）、そのあと消します。ホストが書く `Banlist.txt`・`Admin.txt`・`Moderator.txt`・`VIP.txt` は変えません（`Banlist.txt` に行がある人は、行がある間は最小限を残します）。動くのは MOD の起動時と、起動中は 1 日 1 回（試合中・開始のカウントダウン中はしません）。証拠のフォルダは裏のスレッドで調べます。`aegis-bans.json` の `"housekeeping"` に最後に消した件数を残します（BAN 管理が「アプリの外の変化」と見分けられるように）。
  - **ランチャーとトレイアプリも 30 日**: ランチャーは開いた時に、30 日より古い過去のログ・`LogOutput.log`・デスクトップの報告 zip を消します。7 日より前のログは月ごとの zip をやめて**日ごとの zip**（`logs-<yyyyMMdd>.zip`。zip ごと消せるように）に。報告 zip に入れる証拠の記録は直近 90 日（200 件・5 MB までのまま。それより前の記録は、1 人ずつ「ひとり分の証拠」で。9/23 に証拠の記録を 90 日残すことにしたので、いちど 30 日にしたのを 90 日にもどしました）、ログの中の「記録を消すためのコード」は伏せます。ランチャーを使わずにゲームを起動した時も、MOD が起動時に 30 日より古いログを消します。トレイアプリは起動時に `events.log` の 30 日より古い行を消します。
  - **消去の一覧** `[erase]`: 署名付きの定義ファイル（共有 BAN と同じファイル・同じ署名）に、消してほしい人の「記録を消すためのコード」と依頼の日を載せると、全部のホストの PC が次に定義ファイルを受け取った時に、その日（+2 日）までのその人の記録を消します。`[AntiCheat]` の設定に関係なく効きます（`RemoteRules` がオフでも定義ファイルを受け取って署名を確かめ、この一覧だけを使います）。残すのは、今も効いている BAN が効き続けるための最小限とその証拠（異議申し立てのため）、違反の回数の最小限、30 日より新しい証拠の、名前・部屋コード・ログ・検知の文を除いた残り（ハッシュや検知の数値など。30 日まで）。ログは 30 日で消えるのを待ちます。起動中も 1 日 1 回、定義ファイルを受け取り直します（新しい判定値は次の起動か `/ac rules reload` から使い、試合の途中で変わることはありません）。1 行は `<コード> <yyyy-mm-dd>`、または `@yyyy-mm-dd` の行の下にコードだけ（コードは `/cmd id` の表示どおりの空白入りでも、`-` 区切りでも、区切りなしでもよい。日付は 3 日先まで。1000 件まで。多い時は新しい方）。
  - **`/cmd id`**（`/id`・`/myid` でも）: 記録を消すためのコードを返します。PUID から専用の計算で作る英字 16 文字（最後の 1 文字で、1 文字の打ち間違いのほとんど（16 回に 15 回）を見つけます。チャットで伏せ字にされそうな文字の並びは避けます）で、共有 BAN の一覧のハッシュやフレンドコードからは作れません。コードはパスワードではありません（見た人はだれでも代わりに削除を頼めます）。登録ありの部屋では本人にだけ、登録オフの部屋では「名前: コード」で全員に（部屋全体で 15 秒に 1 回、待ちは 4 件まで）。1 人 1 分に 1 回まで、コマンドをオフにした部屋でも使えます。コードは BAN の記録・証拠の記録・送った通報の控えにも入れます（MOD のログには書きません）。`/cmd h` にも 1 行。
- **証拠の記録は 90 日・ひとり分の証拠の zip**（オーナーの決定 9/23「A」（証拠の記録を残す期間）と、前に OK の出た「解除のことはいいよ」）
  - **証拠の記録は 90 日**: Aegis の証拠の記録（`BepInEx\PocketRoles\evidence\AEG-*.json`）は、BAN された人もされていない人も、作られてから 90 日残します（`AegisPrivacyCore.EvidenceKeepDays`。前は 30 日。異議申し立てがおそく届いても確かめられるように）。今も効いている BAN の証拠は、今までどおり BAN が終わってから 30 日まで（作られてから 90 日より早くは消しません）。証拠のほかは 30 日のまま: 名前・履歴・過去のログ・報告 zip・送った通報の控え・壊れた BAN のファイルの控え・異議申し立てのあとの 30 日・消去の一覧の 2 日のゆとり。**消去の一覧は今までどおりすぐ効きます**（30 日より古い記録はすぐ消し、新しい記録は名前・部屋コード・ログ・検知の文をすぐ消して、残りを 30 日で消す。消去で空にした記録は、一覧の行がなくなっても 30 日で消します）。
  - **ログは 30 日のまま。記録を裏づける 2 行だけ残す**: BAN 管理は取り込んだ記録を同じ zip のログと照らします（「ログと合う」）。ログは 30 日で消えるので、31〜90 日の記録が照らせなくならないように、ランチャー（と、ランチャーを使わずに起動した時の MOD）は、30 日たったログを消す直前に、まだ残る証拠の記録を裏づける 2 行（その記録の検知の行と、MOD がその記録を書いた行）だけを、その記録の隣の `evidence\AEG-XXXXX.log` に残します。ログのほかの行（ほかの人のチャットや名前）は残しません（ログを丸ごと 90 日残す案や、その日のログを記録があるあいだ残す案より、残るものがずっと少ないため）。この 2 行は記録と一緒に消え、消去の一覧ではすぐ消えます。報告 zip には、その記録と一緒に入ります（ログと同じように伏せます）。BAN 管理はこれをふつうのログと同じように照らすので、変えたのは文だけです（`AegisPrivacyCore.EvidenceBacking`・`AegisEvidence.BackingJob`・ランチャーの `Save-EvidenceBackingFrom`。3 つとも同じ決まり）。
  - **ひとり分の証拠の zip**（ランチャーのボタン「ひとり分の証拠」。ヘッドレスは `-Action ExportOne -Who <コード>`）: 異議申し立てを確かめるために、1 人のプレイヤーの証拠の記録だけを入れた `PocketRoles-evidence-<日時>.zip` をデスクトップに作ります。その人の「記録を消すためのコード」（`/cmd id` の英字 16 文字）・フレンドコード（名前#1234）・証拠 ID（AEG-XXXXX）のどれかを入れます。入るのは、この PC に残っているその人の記録（今も効いている BAN の分は 90 日より古くても）と、それぞれを裏づけるログの行（残しておいた 2 行か、今のログ・過去のログから探した行）、`export.txt`、`system.txt`・`launcher-state.json`（BAN 管理が同じホストの zip だと分かるように）だけです。**ほかのプレイヤーの記録とログは入りません**（ただし、その人の記録の検知の文に、同じ試合のほかの人の名前 — キルされた人・通報された人・投票された人など — や、BAN したモデレーターの名前が入っていることはあります）。消去を頼まれた記録（すでに空にした記録と、定義ファイルの `[erase]` に載っている記録）と、90 日をすぎてまだ消えていないだけの記録は入れません（今も効いている BAN の記録は、90 日より古くても入ります）。伏せ方は報告 zip と同じです。人を見分けるのは PUID のハッシュと「記録を消すためのコード」だけで、フレンドコードもそのハッシュも入れません（フレンドコードのハッシュは全部試せば戻せるので、入れても守れないため。作者は本人の `/cmd id` のコードで、申し立てとこの記録を結び付けます。コードはパスワードではないので、送ってきた人が本人だという証明にはなりません）。zip の `export.txt` と画面には「作者にだけ送ってください。ほかの人に渡したり、公開したりしないでください（遊び方のルール 第7条）。中にはその人のコードと、入室制限の一覧で使う PUID のハッシュが入っています」と出します。作った件数と証拠 ID は `launcher.log` に 1 行残します（入れたコードは残しません）。名前は `PocketRoles-evidence-<日時（秒まで）>.zip` で、同じ秒に 2 つ作っても前の zip を上書きしません。作者のメール（`+host`）に送るボタンがあり、30 日たつと自動で消えます。ボタンが 1 つ増えたので、ボタンの欄は行の数に合わせて高くなります。
  - **文**: ランチャー（報告 zip の説明・新しいボタンと窓、3 言語）、BAN 管理の方針（報告 zip の証拠は直近 90 日。それより前でも、まだ効いている BAN はそのホストの「ひとり分の証拠」で）と「解除を提案」の案内（その人の分だけの zip を送る）、「ログと合わない」の説明（30 日より前の記録は裏づけの 2 行が入る）、管理人キットの手順 4（3 言語）、README 3 言語（Aegis と個人情報・入室制限された人へ・ランチャーのボタン・ログの保存・第 28 章の「ひとり分の証拠の zip」）。署名済みの `aegis/definitions.txt` の `[erase]` の説明（「60 日以上」「30-day deletion」）は、署名を壊さないように今は変えていません。次に署名し直す時に必ず直るよう、`tools/sign-definitions.ps1` が「60 日」のままのファイルへの署名を止めます（直す文も出します）。
  - **テスト**: 新しい頭だけのテスト（90 日の境目 89 / 90 / 91 日、今も効いている BAN の残り方、消去はすぐ、ログは 30 日のまま、裏づけの行の選び方）64 件、前からの頭だけのテスト 114 件と 159 件、ランチャーの新しい `-Action SelfTest` 126 件（画面なし。一時フォルダだけ。本物の Steam のフォルダも見に行きません）、BAN 管理の `-SelfTest` 356 項目（裏づけのファイルを取り込んで「ログと合う」になること、書いた行がなければならないこと、ログの大きい報告 zip でも「ログと合う」になること）。
  - **9/23 の見直しで直したこと**: 「ほかのプレイヤーのことは入りません」という文は正しくなかったので、ランチャー・`export.txt`・README の 3 言語とも直しました（検知の文には、キルされた人・通報された人・投票された人や、BAN したモデレーターの名前が入ります）。ひとり分の証拠の zip は、消去を頼まれた記録（すでに空にした記録と、定義ファイルの `[erase]` に載っている記録）と、90 日をすぎてまだ消えていないだけの記録を入れません（今も効いている BAN の記録は入ります）。zip の名前は秒まで書き、前の zip を上書きしません。まだ残っているログから探した裏づけの行には、「ログが消えたので残した 2 行」ではなく「このために今のログから写した行」と書きます。消去の依頼で記録を消した・空にした時は、裏づけの 2 行のファイルもその場で必ず消します（フォルダを全部は読めなかった時も。前は、読めないファイルが 1 つあると残っていました）。BAN 管理は、報告 zip の中の裏づけのファイルの行を、ログの行数の上限とは別に数えます（ログが大きいと、取り込んだ記録の検知の行が捨てられて「ログと合わない」になっていました）。BAN 管理の方針の「本人確認」と「コードがなくても」も、ひとり分の証拠に合わせて直しました（3 言語）。取り込んだ zip が「ひとり分の証拠」の時は、`source.txt` に `kind=one-player` と残します。ランチャーの自己テストは、本物の Steam のフォルダを見に行きません。日本語 README の送り先のメールと `evidence\AEG-*.log` の書きまちがいも直しました。
  - **Starpocket Client と合わせる時の注意**: Client はゲームの起動前に 30 日より古いログを消します。そのままでは裏づけの 2 行が残らず、Client を使うホストでは 31〜90 日の記録が「ログと合わない」になります。Client 側に `Save-EvidenceBackingFrom` と同じ処理（共通の `backing-fixture.txt` で確かめる）をログの削除の前に入れ、デスクトップの `PocketRoles-evidence-*.zip` も 30 日で消すようにしてから、この変更と合わせます。
- **入室制限中の人は本人に知らせて 30 秒後に退出**（要望 9/22「30がいいと思うけどねそれか1分とか」。満員の部屋では待つ間も枠がふさがるので 30 秒）: 登録ありの部屋では本人にだけ「[Aegis] この部屋には入れません（入室制限中）。もうすぐ部屋から出されます。まちがいだと思ったら「PocketRoles」で検索して「入室制限された人へ」を見てください」（チャットを閉じていても通知に出るよう、本人の言語を最後に）、登録オフの部屋では名前を出さずに全員へ「[Aegis] 入室制限中の人はこの部屋に入れません（もうすぐ出されます）。まちがいだと思ったら「PocketRoles」で検索して「入室制限された人へ」を見てください」（10 秒に 1 回まで）。退出させたあと、ほかの人に「[Aegis] 入室制限中の人を退出させました」。待つ間に Aegis の検知・NG ワード 1 回・連投（短い時間にたくさん。コマンドも数えます）か 4 行目の発言（コマンドは数えません）があった時と、開始のカウントダウン・試合の開始の時はすぐに退出させ、試合には入れません（まだいれば開始を 1 秒ずつ 5 回まで待ってから取り消し、その人がいなくなるまで自動開始しません。退出させて本体の最低人数を下回った時も取り消し）。自動開始の人数に数えません。自分で抜けたら何もしません。待つ間に BAN を解除したり VIP にしたりしたら退出させません。最初に入った時から「また来たらすぐにその部屋への BAN 付きで退出」の対象です。キックで出ない時は 5 秒後にその部屋への BAN 付きでもう一度。
- **Aegis の判定値を GitHub から更新**（要望 9/21「アンチチートは常にアップデートしていきたい 抜け穴をやられないために」。9/21 の話し合いで、DLL を分けるより数値を定義ファイルに入れる方を選択）: ゲーム内 Aegis が判定に使う数値とルールの強さを、トレイアプリと同じ定義ファイル `aegis/definitions.txt` の `[rules]` から取るようにしました。誤検知や抜け穴が見つかったら GitHub のファイルを直すだけで、MOD の更新なしに全部のホストへ届きます（新しい種類の検知は今まで通り MOD の更新が必要です）。
  - 変えられる数値: チャット連投（3 秒に 5 通）、ロビーの色の高速変更（3 秒に 20 回）、スピードハック（2.5 倍）と速い移動（1.8 倍）、速さを測る秒数、遠いベントの距離、2 回で退出の数え方（回数・10 秒あける・5 分以内）、早すぎるキル、遠すぎるキル、一瞬のタスク完了、会議・追放画面・イントロの直後に会議外チャットを数えない秒数（8 秒）、言い当て通知の人数。かっこ内は組み込みの値です。数値は抜け穴をふさぐため厳しくする方向にも変えられますが、どれも項目ごとに決めた範囲の中だけです。
  - ルールの強さは**ゆるくする方向にしか変えられません**（オフ ＜ 知らせるだけ ＜ くり返しで退出 ＜ 1 回で退出）。ファイルが間違っていても乗っ取られても、なりすましのありうる検知（名前・色の変更、速い移動、遠いベント、遠いキルなど）が退出に変わることはありません。「オフ」にしたルールはログに 1 行残すだけです（通知・記録・退出なし。トレイの通知も出ません）。
  - ゲームの起動時に裏で GitHub から取り（起動は待たせません）、使ったファイルは `BepInEx\PocketRoles\aegis-rules-cache.txt` に残して次の起動ではまずそれを使います。取れない・壊れている時は今の値のまま続けます。
  - `/ac rules`（`/aegis rules` でも。ホストのみ）: 今の判定値（組み込み / キャッシュ / GitHub の版と取得時刻）、組み込みと違う値、ルールの強さの変更を表示。`/ac rules reload` で今すぐ取得し、結果をホストの画面に出します。
  - 設定 `[AntiCheat] RemoteRules`（既定オン。`/opt anticheat.remoterules on|off`、設定タブの「Aegisの判定値をGitHubから更新」）: オフなら組み込みの値だけを使います（取得もキャッシュもしません）。切り替えはすぐ効きます。
  - 定義ファイルは version=3 に。`[rules]` には全項目の組み込みの値と範囲を説明として書いただけで、今はどの値も変えていません（判定は v0.5.4 と同じです）。
- **NG ワード（暴言・不適切な発言の対策）**（要望 9/21「言動の対策もある？」「つける あなたが登録してくれる？ NGは繰り返したら自動退出でできない？」「3回でばんで良くない？仏の顔も三度までだし」）: ほかの人のチャットを NG ワードの一覧と照らします。ロビーでも試合中でも、登録オン・オフどちらの部屋でも、生きている人も死んでいる人も対象です。1 回目と 2 回目は全員のチャットに注意（1 回目「[Aegis] ○○ さん、暴言・不適切な発言はやめてください。あと 2 回で退出になります。」、2 回目「…もう一度で退出になります。」。NG ワード自体は繰り返しません）、ホストの画面には当たった言葉と回数。**3 回目で自動退出**（この部屋へのバン付き）、全員に「[Aegis] ○○ は暴言・不適切な発言を繰り返したので退出になりました。」。ホスト・VIP・モデレーター・アドミンは対象外で、クイックチャットは数えません（コマンドの行は、ホストの画面では消えてもほかの人には見えるので数えます）。部屋にいる人の名前の中に NG ワードがあっても、その人の名前を書いただけでは数えません。
  - 数え方: 同じ部屋の中で人ごと（フレンドコードなど）。1 発言は 1 回まで、前の回から 5 秒以内の続けての発言は同じ 1 回（ログには残ります）。別の部屋に移ると 0 から（「もう一度遊ぶ」では続きます）。
  - 試合中に死んでいる人への注意は、全員に出すと生きている人に誰が死んでいるか分かってしまうので、登録オンの部屋ではその人にだけ送ります。登録オフの部屋では、ホストが生きている間は死んでいる人に、ホストが死んでいる間は生きている人に注意が届かないので、その間は出しません（回数は数えます）。**退出させるのは本人に見える注意を出した後だけ**です（NgKickAt = 1 の時を除く）。死んでいる人についてのホストへの知らせは、ホストが生きている間はホストの死亡後か試合後まで待ち、退出の全員へのお知らせは試合後のロビーで出します。
  - 表記ゆれを吸収します（全角・半角、カタカナ・ひらがな、大文字・小文字、見えない文字）。日本語や英語の暴言は、間に記号や空白をはさんでも当たります。区切りの後や 1 語としてだけ当たる日本語の言葉も、1 文字ずつ間をあけた書き方で当たります（説明用の作り話の言葉「ほげら」で書くと、「ほ げら」「ほ.げら」は当たり、「よほ げらよう」のように前後の言葉につながる書き方は当たりません。英語の言葉は今まで通りで、ふつうの英文の中にたまたま同じ並びがあっても当たりません）。
  - **ローマ字も規制**（要望 9/22「ローマ字も規制してよ」）: チャットをローマ字として読み直し、日本語の NG ワードと許可の一覧をその読みにも使います。大文字でも、1 文字ずつ間をあけても、ほかのローマ字の言葉と続けて書いても当たります（ヘボン式・訓令式・IME の書き方: shi/si、chi/ti、tsu/tu、fu/hu、ji/zi、sha/sya、nn、kk などの小さい「っ」、語の後の「-」「~」は「ー」）。ローマ字として最後まで読めない英単語（shiny、sky、the など）はそのままで、許可の一覧も読みに効くので、許可された言い方やのばす書き方は当たりません。ローマ字の単語の間の空白は区切りとして数えますが、間をあけた書き方の判定には使わないので、日本語の文をローマ字で書いたものやピンインの「wo jue de bu hao」には当たりません（メッセージの先頭や記号の後から始まる時だけ当たります）。英語の NG ワードは英語のまま照らします。英語にも同じつづりの単語があるもの（ローマ字として読むと日本語の NG ワードになるもの）は、9/22 の見直しで、and・the・on・it・so・to などの英語の語といっしょに書いた文の中では読まないようにしました（「rise and shine」「shine on」「the stars shine」「let it shine」のような英語の言い回し）。その単語ひとつだけ、色や部屋にいる人の名前といっしょの時は今までどおり読みます。1 行の判定は 2 マイクロ秒ほど（部屋にいる人の名前も使う時は 12 マイクロ秒ほど）で、ふつうの行ではメモリ確保はありません（NFKC の変換が要る行を除く。部屋にいる人の名前は、区切りがないだけで当たらなかった言葉がある行と、当たった後にだけ集めます）。
  - 9/22 の追加（同じ要望）: 色の名前＋暴言を全色（18 色。かな・漢字・カタカナ語。かなと一部の漢字は区切りの後だけにして、ふつうの言い回しには当たらないようにしています）、**部屋にいる人の名前＋暴言**（作り話の言葉で書くと「もりほげら」は「もり」さんが部屋にいる時に当たります。名前がメッセージの先頭か区切りの後に書いてある時だけで、「うまいほげら」は「まい」さんがいても当たりません。ローマ字の名前とかなの名前は同じとみなします）、当て字 1 つ、中国語のピンインの罵倒の略語 5 つ、韓国語のローマ字 3 つ。名前の後に続くふつうの言い方 4 つは許可に追加。ふつうの言葉に紛れやすいもの（食べ物・作業・地名の名前や、同じ文字の並びを含むふつうの言い回し）は「区切りの後だけ」「1 語として」の書き方と許可の一覧で守ります。「殺す」「ゴミ」「ばか」「あほ」「きもい」「うざい」「ざこ」などゲームの会話や冗談で使う言葉は入れていません（`/ng add` で足せます）。
  - 9/22 の見直し（同じ要望。指摘 28 件）: ローマ字の読みの誤検知を直しました。「〜できなかった」「〜と言われた」のように NG ワードと同じ音を含むふつうの報告の言い方（ローマ字も）、スペイン語の「kiero ser impostor」（quiero）とタガログ語の「nood tayo sine」（映画）・英語の「sine wave」（ほかに読めない語がある文の中の kiero・sine は読みません）、中国語のピンインの文（「zhen de bu zhi dao」「ying gai jiu shi ta」など）、日本語のローマ字の文がとなりの語とつながって別の語に読めてしまうもの（「ichiman koete」など）は当たりません（ローマ字の単語をまたぐ当たりは、語の始まりから語の終わりまでの時だけ数えます。間をあけて書いたものは今まで通り当たります）。かな・韓国語・中国語でふつうに使う言い方も許可に追加。部屋にいる人の名前を区切りとみなすのは、名前の後に敬称や地名が続く時には使わず、名前の後に続けて書くと暴言と同じ形になるふつうの言い方も許可に追加（その名前の人がいても当たりません）。ローマ字の名前の人がいても、ほかの人がかなやカタカナで書いた暴言は数えます（数えないのは、その名前をローマ字のまま書いた時だけ）。ホストが英数字で足した許可はローマ字の読みにも効きます（空白をはさむ許可は読みには効きません）。避け方への追加: 語の後の「w」（笑い）、b・p の前を m で書いたローマ字、日本語キーボードの「〜」（「ー」と同じ）。NG に、韓国語の罵倒 8 つ（子音だけの略字、英字キーボードのまま打った形、ローマ字を含む）、中国語の罵倒 4 つ、中国の人をばかにする言い方 1 つ、英語の罵倒の略し方 3 つ、当て字 1 つを追加。見分けられないので受け入れたもの: スペイン語の「te kiero」「no kiero」、メッセージの先頭で間をあけたピンインの 2 語、インドネシア語の「banyak debu」、タガログ語の「sine tayo」、名前＋同じ音になるふつうの言い方。
  - 一覧は MOD に組み込み（v0.5.5 からは**ハッシュだけ**。下の「NG ワードの一覧をハッシュだけに」を見てください）。Aegis の定義ファイル `aegis/definitions.txt` に `[ngwords]` `[ngallow]` があればそちらに置き換わるので、GitHub を直せば言葉の追加も削除も MOD の更新なしで全部のホストに届きます（`[rules]` と同じ取得・キャッシュ。定義ファイルにも組み込みと同じ一覧が、同じようにハッシュだけで入っています）。ホストが自分で足す言葉は `BepInEx\PocketRoles\NgWords.txt`（`/ng add <言葉>`、`/ng del <言葉>`、`!言葉` で許可。ファイルを直接書き換えても 10 秒以内に読み直します）。
  - 実際の部屋のチャットで見直して言葉を追加（要望 9/21「きょうとか過去のログにもある言葉も禁止にして」）: 色＋暴言の言い方 2 つ、差別的な言い回し 2 つ、罵倒の略語 1 つ、性的な言葉のローマ字の略語 2 つ（1 語として書いた時だけ）。ふつうの言い方（「〜笑った」「〜笑顔」など）は許可の一覧に追加。組み込みの一覧と `aegis/definitions.txt` に同じ中身を入れました（9/22 の追加と見直しの後で NG 151 語・許可 99）。実際のチャット 519 行で当たるのは暴言の 8 行だけで誤検知は 0（ログに出てくる 24 人の名前が部屋にいるとしても同じ）、MOD 自身の文（3 言語）にも当たらないことを確かめています。9/22 には、ローマ字・間をあけた書き方・色・ピンインで当たるべき 184 行、部屋にいる人の名前の例 50 行と、会議・タスク・色の話など当たってはいけないふつうのチャット（日本語・ローマ字・英語・中国語・韓国語）635 行も試して、すべて期待どおりでした。見直しの後は、指摘の例を中心に当たってはいけない 136 行と当たるべき 109 行、ローマ字の単語 305 個の 2 語の組み合わせ 9 万通り、英単語 261 個の組み合わせ 6.8 万通り（0）、ランダムな 30 万行（例外 0）も試しています。
  - 一覧の誤りへの安全装置: 2 分以内に 3 人以上が当たったら、その部屋では NG ワードでの自動退出を止めて注意だけにし、ホストに知らせます。
  - 部屋バン付きで退出させた時は Aegis のトレイアプリにも「NG ワードの繰り返し」と通知が出ます（スキャン画面の検知ルールは 25 件に）。`/ac` の記録にも載ります。
  - `/ng`（ホストとアドミン。登録オフの部屋ではホストだけ）: 状態（オン・オフ、何回目で退出、一覧の数、この部屋で当たった人）、`/ng add|del <言葉>`、`/ng list`（自分の言葉）、`/ng test <文>`（当たるかどうかだけ試す。記録しません）、`/ng on|off`（ホストだけ）。`/h host` にも 1 行。
  - `/ng list all [ページ]`（要望 9/21「どうやってその禁止一覧みる？」。`all` の代わりに `全部` `ぜんぶ` `すべて` `全て` でも可）: 使っている一覧の全部を、出どころ（MOD の組み込み / GitHub の定義ファイル v◯ / 前に GitHub から取って保存した定義ファイル v◯）→ NG → 自分の NG（`NgWords.txt`。空なら「なし」）→ 許可 → 自分の許可（`!` 付き。ある時だけ）の順に出します。**ホスト専用で、ホストの画面にだけ出します**（ほかの人には送らず、全員に見える発言もしません。暴言の一覧を人に見せないため）。リモートのアドミンが使うと「全部の一覧はホストの画面にだけ出せます」と返すだけです。1 通 100 文字以内に詰め、1 通に入りきらない分は次の通の「NG（続き）:」などに続けます（言葉の途中では切りません。1 通より長い言葉だけは「…」で切ります）。1 ページは最大 6 通＋ページの行（「1/2 ページ。続きは /ng list all 2」、最後は「2/2 ページ（ここまで）」。範囲外のページ番号は最初か最後のページ）。9/23 に一覧をハッシュだけにしたので、組み込みや定義ファイルの言葉は**件数だけ**（「この一覧のうち ◯ 件は読めない形（ハッシュ）で保存されています」）になり、自分の言葉がなければ 1 ページに収まります。ホストが自分で足した言葉は、これまでどおりそのまま出ます。
  - 英語の注意文を 1 通に収まるよう短くしました（10 文字の名前でも、登録オフの部屋の 1 通の上限 86 文字に収まります。これまでは 2 通に分かれていました）: 「[Aegis] NAME, please stop the offensive language. 2 more and you're removed.」／「… Next time you're removed.」。
  - 設定 `[Chat] NgFilter`（既定オン、`/opt ng`）、`NgKickAt`（何回目で退出。既定 3 = 2 回注意して 3 回目で退出、1 = 注意なしですぐ退出、0 = 退出させず注意だけ、`/opt ng.kickat`）、`NgBan`（退出に部屋バン。既定オン、`/opt ng.ban`）、`NgAnnounce`（退出を全員に知らせる。既定オン、`/opt ng.announce`）。設定タブの「会話」ページにも。
- **Discord の投稿にアイコン**（要望 9/21「ディスコのアプリのアイコンがないからそこも頼んだ」）: 部屋コードの自動投稿に PocketRoles のアイコンが付くようにしました（新しく投稿する時に付け、書き換えではそのまま）。設定 `[Discord] AvatarUrl`（既定は GitHub にある PocketRoles のアイコン。https の画像 URL で 512 文字まで。空ならウェブフックに設定したアイコン）。名前はウェブフックの名前のままです。Discord に URL を断られた時は、アイコンなしで投稿し直します。
- **高 PING の部屋の作り直しを既定でオフに**（要望 9/21「pingのことは既定でオフして」）: v0.5.4 で既定オン（80 ms）にした作り直しを既定オフに戻しました。設定は `[Lobby] PingRecreateLimit`（既定 0 = しない。`/opt maxping <ms>` と設定タブはそのまま）。v0.5.4 の `HostPingLimit` を 80 以外（自分で決めた値）にしていた人はその値を、さらに前の `MaxHostPing` を 0 より大きくしていた人はその値を引き継ぎ、古い行は設定ファイルから消えます。オンにした時の「15 秒答えがなければ作り直す」動きは同じです。オンにした時の判定は、当てにならない client.Ping ではなく、下の wire RTT（部屋を作った・入った時に測る往復時間）で行います（測れなかった部屋は判定せずにそのまま残します。設定タブの説明文 `opt.tip.lobby.maxping` の変更は `lang/*.json` の上書きコピーが必要）。
- **サーバーの近い・遠いとラグのログ**（要望 9/21「詳細ログ出したい」「どこのサーバー？」。9/21 のラグの相談でホストのログを調べたところ、遅かった試合は遠いゲームサーバーで、どのサーバーかはログのどこにも残っておらず、ゲームの PING 表示も 11 ms のサーバーで 73 ms、73 ms のサーバーで 85 ms と当てになりませんでした）: 常にオンで、設定はありません（`[Diagnostics] WireLog` とも関係なく動きます。出力先は `BepInEx\LogOutput.log`）。
  - **wire RTT**: ホストが HostGame（登録オンは HostModdedGame）／JoinGame を送ってからサーバーの返事（HostGame／JoinedGame）が届くまでの実時間。部屋の値はそのうち小さいほう。StartGame の往復と、試合後に同じ部屋へ戻った時の往復も別に記録します。近い／遠いの境界は 40 ms（固定）。
  - **ホストの画面に 1 行**: 自分で作った部屋では、入った直後に 1 回、「サーバー: 近い（11 ms）」または「サーバー: 遠い（73 ms）。一度退出して部屋を作り直すと、近いサーバーになることがあります。」（ホストの画面だけ。全員には送りません）。作り直しを勧める文には「※ 短時間に何度も部屋を作り直すと「意図的な切断」と見なされ、部屋作成が一時制限されます。」を付け、10 分に 1 回までにしました（その間の遠い部屋は「サーバー: 遠い（73 ms）」だけ）。`PingRecreateLimit = 0`（既定）でも出ます。試合後に同じ部屋へ戻った時は出しません。
  - **ログの `server:` 行**（部屋ごとに 1 回、ホストでも参加でも）: ゲームサーバーの IP:ポート、地域、部屋コード、wire RTT と判定（`= near (40 ms or less)`／`= FAR (over 40 ms)`。境界の値も書くので、ログだけで読めます）、比べるための client.Ping（最初の値が出るまで最大 6 秒待ちます）。試合後に同じ部屋へ戻った時は「rejoined」の短い行だけ。`/diag` にも `server:` の行（IP:ポート、region、lobby、wireRTT と near／FAR、各 RTT、client.Ping、試合中なら経過時間。部屋を出た後は `[left]` 付きで最後の部屋の値）。
  - **試合中の行**（ホスト、オンライン）: 30 秒ごとに 1 行、ホストの PING と、参加者ごと（名前と clientId）の受け取った移動の通信の数・大きなワープの数（Aegis の速度チェックが除外している 1 フレームのワープ・巻き戻し）。試合終了時に最後の区間を「game end」で出します。速度チェックが動かない区間は「jumps not measured」。止まっている人は移動の通信が少ないので、少ないだけではラグとは言えません。
  - **試合開始と会議開始**: 役職の送信（登録オンの部屋の 0.3 秒おきの個別送信にも対応）・会議の開始から、各参加者の SnapTo の返事までの遅れを 8 秒間集め、中央値・遅い人（中央値の 2 倍を超え、かつ 300 ms を超える人）・返事なしの 1 行と、全員の遅れの 1 行（遅い順）。会議の対象は生きていて接続中の人だけです。記録用の配列は最初に確保して使い回し、毎フレームの割り当てはありません。
  - F7／F8／F9 のログ行に、WireLog と同じ形の時刻（`HH:mm:ss.fff` と起動からの秒数）を付けました。
- **ランチャーがゲームのログを残す**（要望 9/21「でもふるいの消すと後から困らない？」）: BepInEx はゲームを起動するたびに `LogOutput.log` を上書きするので、ランチャーを開いた時と「起動」（開発モードは「mod 付きで起動」）の直前、開発モードの「更新チェック / 更新」でログを消す前に、前回のログを `BepInEx\PocketRoles\logs\LogOutput-<yyyy-MM-dd_HHmmss>.log`（ログの最終更新日時）へコピーします。同じログは 2 回保存せず、そのフォルダの Among Us が起動中の時は保存しません。失敗しても起動は止めません（`launcher.log` に記録するだけ）。開発モードの更新で保存できなかったログは消さずに、`LogOutput-<日時>.log` の名前で logs フォルダ（だめなら `BepInEx` フォルダ）へ移します。
  - 7 日より前のログは、ランチャーを開いた時に同じフォルダの月ごとの zip（`logs-<yyyy-MM>.zip`。同じ月の 2 回目からは `logs-<yyyy-MM>-2.zip`、`-3` … と新しい zip を作り、今ある zip は書き換えません）へ移します。新しい zip の書き込みと中身の確認が済んでから元の `.log` を消すので、途中で失敗してもログはなくなりません。1 回に移すのは合計 256 MB まで（残りは次に開いた時）、512 MB を超えるログは zip にせずそのまま残します。ランチャーを 2 つ開いても、ログの保存と zip への移動は同時には動きません。
  - **自動では何も消しません**。フォルダが 2 GB を超えたら、サイズの表示がオレンジになり、ログ欄にお知らせが出るだけです（9/22 の見直しで、日ごとの zip にして 30 日で自動で消すように変えました。上の「記録の自動削除」）。
  - 新しいボタン「ログのフォルダを開く」（友達モード・開発モードとも最後のボタン。フォルダが無ければ作って開きます）と、その右に「ログ: 123 MB」の合計サイズ。
  - **報告 zip**: 今のログ（ゲーム中でも入ります）に加えて、過去のログ 3 回分を入れます（1 件 8 MB まで。大きいログは最初の 1 MB と最後の部分だけ）。**設定ファイル `jp.pocketroles.mod.cfg` は入れなくなりました**（Discord のウェブフック URL などが入るため）。Discord のウェブフック URL は `<discord-webhook-masked>` に置き換えます（API キーの `<api-key-masked>` はこれまでどおり）。`system.txt` に過去のログの合計サイズと件数の行を追加。
- **スピードハック検知を軽く**（9/21、v0.5.4 に更新した後にラグの相談があったため。MOD が原因とは分かっていませんが、毎フレームの処理を減らしました）: 試合中に毎フレーム行う速さの確認で、プレイヤーの一覧を毎回作り直すのをやめ、死んでいる人・抜けた人・ホストを先に外し、止めている人はほかの状態を読む前に飛ばし、速度の設定を読むのは 2 秒の区切りの時だけにしました。判定の中身（倍率・秒数・瞬間移動の扱い・止める場面）は同じです。
- **判定リストを読めない形（ハッシュ）で保存できるように**（作者の決定 9/22「やる」: リストの中身を人に読ませない）: 定義ファイルの `[tools]`・`[dlls]`・`[dllwords]` に、チートツール名・DLL 名を**塩付きの HMAC-SHA256 ハッシュ**（`hashsalt=` の 1 行と `#h1 ...` の行）で書けるようにしました。コードは GPL で公開のままですが、リストの中身は公開しません。照合はホストの PC の中だけで、外部には何も送りません。MOD・トレイ・署名の道具が同じハッシュを出すことを共有ベクトル（`tests/aegis-hash-vectors.txt`）で確かめ、`build-release.ps1` がトレイの埋め込みコピーの一致とベクトルを確かめます。作者は元の平文を自分の PC にだけ置き（リポジトリの外）、`tools/sign-definitions.ps1 -UpdateHidden` でハッシュに変換します（KeepProcess/KeepDll・短すぎる語は除外。ハッシュ順に並べます）。古いクライアント（v0.5.4）は `hashsalt=` の行と `#h1` の行を読み飛ばします（v0.5.4 のトレイはこの 2 種類の行をコメントとして無視し、v0.5.4 の MOD は定義ファイルを読みません。どちらも誤検知なし）。チートツール名（`tool`/`toolp`）と内容ハッシュ（`sha`）・署名者（`signer`）のヒットは、組み込みの既知チート名と同じく**いつも部屋作成を止めます**（`[AntiCheat] SelfScan = false` でも。MOD 内の判定 `Kind.HiddenCheat`）。`[dlls]`/`[dllwords]`/exe 情報（`vi`）のヒットは「ゆるめられる」判定（お知らせだけになりえます）。どちらも Windows とゲーム自身のファイルの除外の後だけ調べるので、`GameAssembly.dll` や Microsoft 製などを誤検知しません。今の 26 個の平文はそのまま（当面。新しい平文のチート名は署名の道具が拒否します）。新しいチート名は公開の Issue/PR ではなく私的に報告してもらいます。（正直に: 塩は公開で長さも `n=` で分かるので、**短い言葉（NG ワードの多く・短いツール名・短い DLL 語）はふつうの PC で総当たりして数分〜数時間で分かってしまいます**。ハッシュは「ぱっと見て読めない」ようにする工夫で、長い名前ほど守れますが、短い語のしっかりした秘匿ではありません。HMAC を遅くすると 1 通ごとの照合が何百倍も遅くなるので、技術で埋める方法はありません。）
- **チャットの NG ワードの一覧も読めない形（ハッシュ）で保存できるように**（同 9/22「やる」。151 の NG ワードと 99 の許可語）: `[ngwords]`・`[ngallow]` にも `#h1 ng=` / `#h1 al=` のハッシュ行を書けるようにしました。チャットの判定は**今までとまったく同じ**（同じ正規化・ローマ字読み・`< >` 境界・許可語の順序）です。100 万行の差分テスト（今の平文の照合と、ハッシュ化した照合が、当たり・当たった場所まで完全に一致）で確かめました。画面のお知らせ・ログ・BAN の記録・`/ng test` は、**当たった部分の文字**（その人が打った文を、全角・半角、大文字・小文字、カタカナ・ひらがなをそろえた比べるための形。24 文字まで。この PC の事実で、一覧の言葉ではありません）だけを出します。`/ng list all`・`/ng` の状態は、読めない形のものは件数だけを「読めない形（ハッシュ）で保存」と出します。返事の宛名を調べる BAN 管理のコンソールもハッシュに対応し、宛名の NG 確認もチャットと同じ読み方（波ダッシュ・半角の半濁点をそろえる）になりました。1 通あたりの処理は、平文のとき約 15µs、全部ハッシュ化したとき x86 で平均 約 285µs・最長（100 字）約 708µs で、チャット 1 通に十分間に合います。ホスト自身の `NgWords.txt` は平文のままです。（この時点では平文の一覧が残っていましたが、9/23 の決定で全部ハッシュに変えました。次の項目を見てください。）
- **NG ワードの一覧をハッシュだけに**（作者の決定 9/23。この一覧は一度も公開していないので、v0.5.5 を出す前に平文を全部なくしました）: `aegis/definitions.txt` の `[ngwords]`・`[ngallow]` は**ハッシュの行（`#h1 ng=` / `#h1 al=`）だけ**になり、MOD に組み込みの一覧（`src/Chat/NgText.cs`）も**ハッシュだけ**になりました（同じ HMAC の仕組みで、組み込み用の塩を 1 つ埋めています）。**判定は前とまったく同じ**です（100 万行の差分テストで、当たり・当たった場所・当たったエントリまで一致）。平文の元は、リポジトリの外・同期フォルダーの外に置いた作者の私的リスト 1 つだけになり、`tools/sign-definitions.ps1 -HashLegacy -Sections ngwords,ngallow`（定義ファイル）と `-BuiltinNg`（MOD の組み込み）で作り直します。`[ngwords]`/`[ngallow]` は v0.5.5 から読む節なので、v0.5.4 のクライアントには影響しません（チートツール名の `[tools]`/`[dlls]` の 26 個は、必要な版が v0.5.5 以上になるまで平文のままです。設計どおり）。定義ファイルの中の説明、この CHANGELOG、README（3 言語）、BAN 管理コンソールの自己テストの例も、作り話の言葉（「ほげら」など）に書き換えました。ビルドした DLL のバイト列（UTF-8・UTF-16）にも、追跡下のファイルにも、NG ワード・許可語は 1 つも残っていません。1 通あたりの処理は、組み込みがハッシュになったぶん平文のときより遅くなります（この PC の 64 ビットで 1 通 約 160µs・最長（100 字）約 390µs）が、チャット 1 通には十分間に合います。（正直に: 塩は DLL と定義ファイルの中にあり、長さも `n=` で分かるので、**短い言葉は総当たりで分かってしまいます**。「ぱっと見て読めない・検索しても出てこない」ための工夫で、しっかりした秘匿ではありません。）
- **名前を変えたチートツールの検知**（作者の要望「名前を変えられると名前のチェックはすり抜けられる。なんとか見つける」）: 定義ファイルに、ファイル名に頼らない隠しエントリを書けるようにしました。`#h1 sha=` はチート本体の**中身の指紋（SHA-256）**、`#h1 vi=` は実行ファイルに埋め込まれた**バージョン情報**（元のファイル名・内部名・製品名・説明・会社名。完全一致か先頭）、`#h1 signer=` は**デジタル署名の署名者名**です。exe を別名にしても、中身・情報・署名者は変わらないので見つかります。MOD が、自分のゲームに読み込まれた DLL と Among Us.exe の横の DLL について、（サイズと更新日時の）キャッシュ付きで照らし、1 スキャンで調べるファイル数と合計サイズに上限を付け、Windows とゲーム自身のファイル、正しく Microsoft 署名されたファイルは調べません。中身ハッシュと署名者の一致は**いつも止め**、バージョン情報の一致は「ゆるめられる」判定です。ウィンドウのタイトルで調べるやり方は、「ほかのアプリの画面は見ない」という約束に反するので入れていません（作者の判断とプライバシー文の変更が先）。一覧は空で、書式・照合・署名の道具・テスト（架空の値）だけ用意しました。届かないもの: 作ったばかり・作り直したチート、手で読み込む注入、MOD を自分でビルドしてチェックを外した人。**部屋に入る人**のチートには効かず、そちらは試合中の行動チェック（CheatDetector）の担当です。
- **名前を変えたチートのアプリ（起動中のプロセス）も見つける**（作者の質問 9/23「プロセスの問題はどうするの？」）: トレイアプリの「実行中のチートツール」の確認（スキャン画面・ランチャーの「起動」の前の確認・常駐中の 30 秒ごと）が、プロセスの名前に加えて、定義ファイルに `#h1 sha=` / `vi=` / `signer=` があれば、起動中のアプリの**実行ファイル（ディスク上の .exe）**の中身の指紋・バージョン情報・署名者を、MOD と同じ共有の AegisHash・同じ規則（MOD の SelfScan の読み方と Microsoft 署名の確かめ方をトレイの C# 5 に移植）で照らします。exe の名前を変えても見つかります。中身の指紋か署名者が合えば、名前が当たった時と同じく赤い行で起動を止め、トレイは通知を出します（「○○.exe が起動中（名前を変えたチート）」。出すのはディスク上の今のファイル名で、一覧の中身は出しません）。バージョン情報だけが合った時は黄色の行（「心当たりがなければ終了してください（起動は止めません）」）とお知らせだけです（**起動中のアプリのバージョン情報は、ふつうのアプリがたまたま同じ製品情報を持っていることがあるのでお知らせ止まり。同じバージョン情報でも、ゲームの中に読み込まれた DLL なら MOD は退出させます**。ゲームの中に、ふつうのアプリの DLL が紛れ込む余地はないためです）。アプリの場所は `QueryFullProcessImageName`（`PROCESS_QUERY_LIMITED_INFORMATION` の問い合わせだけ。すぐ閉じます）で聞き、ほかのアプリのメモリ・モジュールの一覧・ウィンドウ・画面は見ません（「ほかのアプリの画面は見ない」の約束はそのまま）。何も送りません。Windows 自身のファイル（**場所だけでは決めず、持ち主が TrustedInstaller・SYSTEM・Administrators のものだけ**。Windows\Temp・\Tasks など誰でも書ける場所に置かれたユーザーのファイルは調べます）、ゲーム自身の実行ファイル（**ゲームのフォルダー直下の `Among Us.exe` と `UnityCrashHandler*.exe` だけ**。同じフォルダーのほかの exe は調べます）、**正しい署名が Microsoft のルート証明書につながるファイル**（署名者の名前だけでは信用しません）は調べません。結果は（場所・大きさ・更新日時・NTFS のファイル ID・変更日時／USN）で覚え、1 回に新しく読むのは 32 ファイル・64 MB まで（MOD と同じ。スキャン画面はゲームを起動する前なので 4 倍）、64 MB を超えるファイルは中身の指紋を取りません（バージョン情報と署名者は見ます）。覚えているものから、次にユーザーのフォルダーを Program Files より先に、小さいファイルから調べます。裏のスレッドで動くので、トレイもスキャン画面も止まりません（スキャン画面の行は結果を待ち、15 秒たっても来なければ名前の確認だけで進みます）。この PC で測った値: 初回（キャッシュなし・3 種類すべて・約 47 ファイル）約 0.3 秒、2 回目から 1 回 6〜13 ms、名前だけの時 約 5 ms。問い合わせを断られたプロセス（管理者として実行中のアプリなど）は、**そのプロセスを開かずに Windows に場所だけを聞き直す**ので、そのまま確認できます（この PC では、自分のセッションの約 215 個のうち 9 個がこの方法。場所がまったく分からなかったものは 0 個で、数だけを `aegis.log` に書きます）。見るのは**自分と同じ Windows セッションのアプリだけ**で、Windows のサービス（セッション 0）やほかのユーザーのアプリ（この PC では 130 個）は見ません（管理者で動かしても範囲は同じです）。定義ファイルにその種類がなければ、今までどおり名前だけを見ます（ハンドルも開かず、ファイルも読みません）。一覧は今も空です。ランチャーの起動前の確認はこのトレイのスキャンを使うので同じになります。MOD はもともと自分のゲームのプロセスしか見ないので変えていません。ステータス画面の説明文・README の「Aegis と個人情報」も「起動中のアプリの名前と実行ファイル」に直しました。届かないもの: **作り直した・再パックした・別の証明書で署名し直したチート**、**メモリの中だけで動くもの**・ほかのプロセスに注入されたもの、正しい署名が Microsoft のルートにつながるファイル（MOD と同じ規則）、**「起動」を押したあとに起動されたチート**（起動前の確認では止められず、常駐中の確認で見つけ次第お知らせを出すだけ）。これは「知っているものを見つける」仕組みで、すべてのチートを止められるものではありません。
- **実行中のアプリの確認のレビュー修正**（9/23 のレビュー。すり抜けと見落としをふさぎ、正直な説明に直しました）:
  - **ゲームのフォルダーは丸ごと素通りさせない**: 飛ばすのはゲーム自身の実行ファイル（フォルダー直下の `Among Us.exe` と `UnityCrashHandler*.exe`）だけで、そこに置かれたほかの exe は調べます（チートをゲームのフォルダーに置くだけでは隠せなくなりました）。
  - **場所を教えてもらえないアプリも確認**: 断られた時は、そのプロセスを開かずに Windows に実行ファイルの場所だけを聞き直します（見るデータは前と同じ「場所」だけ）。範囲は**自分と同じ Windows セッション**に限り、Windows のサービス（セッション 0）やほかのユーザーのアプリは見ません（管理者で動かしても範囲が変わりません）。この PC では、これまで確認できなかった 140 個ほどのうち、自分のセッションの分はすべて確認できるようになりました（残り 0 個）。
  - **Microsoft の署名は「名前」では信用しない**: 署名が正しいことに加えて、**証明書のつながりが Microsoft のルート（製品ルートか 2011 年のアプリ用ルートのどちらか）まで届くこと**まで確かめます（トレイと MOD の両方）。署名者の名前が「Microsoft」でも、自分で作った証明書ならふつうに調べます。
  - **Windows のフォルダーも「場所」では信用しない**: 持ち主が TrustedInstaller・SYSTEM・Administrators のファイルだけを Windows のものとみなします（メタデータだけを見ます）。ユーザーが書き込める場所に置かれたファイルはふつうに調べます（MOD 側も同じ規則）。
  - **覚え方を厳しく**: （場所・大きさ・更新日時）に **NTFS のファイル ID と変更日時（取れる時は USN）** を足したので、大きさと更新日時をそろえた差し替えが素通りしなくなりました。
  - **確認できなかった時に黙らない**: 実行ファイルが消えている・読めないアプリは、数を分けて記録し、スキャン画面に黄色で「実行ファイルを確認できませんでした」（3 言語）と出します。失敗した確認は覚えないので、次のスキャンでやり直します。
  - **遅い時も手ぶらにしない**: 15 秒の打ち切りでは、そこまでに見つかった分＋名前の確認を使います。30 秒より長くかかっている時は、トレイが名前だけの確認（約 5 ms）をして通知します。スキャン画面は、後回しにしたファイルがある時は緑ではなく黄色で出します。
  - **あとから起動したアプリ**: 2 秒ごとに起動中の番号だけを見て（約 0.05 ms）、**新しく増えた分だけ**場所を調べ、次のスキャンの列に並べます。すぐ終了したアプリでも、そのファイルを確認します。
  - **重さの配り方**: 署名の確認のぶんを先に取り置き、大きすぎるファイルでは行いません。後回しにしたファイルは古いものから順に必ず調べ、いちばん長く待った回数を記録します。
  - **KeepProcesses**（Steam・Discord・ブラウザーなど）は**中身の指紋が合った時だけ**当たりにします（署名者やバージョン情報の書きまちがいで、ふつうのアプリが止まらないように）。`sign-definitions.ps1 -UpdateHidden` は、`[tools-signer]` / `[tools-vi]` の値がこの PC に入っているふつうのアプリと同じ時と、大きすぎるファイルから作った指紋を、**件数と行番号だけ**で知らせます。
  - **記録**: `launcher.log` と起動前の確認には件数だけを書き（`Aegis: launch stopped (N items)`）、ファイル名はダイアログとトレイの記録（30 日で消える）にだけ残します。
  - そのほか: 裏のスレッドが失敗しても「確認中」が必ず解けるようにし、`build-release.ps1` の共有 AegisHash の照合を**大文字小文字を区別**するように直しました。文言（3 言語）・ステータス画面の説明の長さ・ファイル先頭の説明（ほかのゲームには、Windows に実行ファイルの場所を聞くだけで、メモリ・ウィンドウには触れない）も直しました。
- **実行中のアプリの確認の 2 回目のレビュー修正**（9/23 の 2 回目のレビュー。上の直しのうち 5 つに穴が残っていました）:
  - **Microsoft のルートの判定の旗のまちがい**（いちばん大きい直し）: 上の「Microsoft のルートまで確かめる」は、Windows の旗を 1 つ付けまちがえていて、**2011 年のアプリ用ルートだけ**を見ていました。この旗は「製品のルートに 2011 のルートを足す」ものではなく「2011 のルートに**置きかえる**」ものだからです。この PC の Microsoft 署名のファイル 40 個で測ると、**21 個（Windows・Office・Edge など、2010 年のルート／Microsoft Root Authority のもの）が Microsoft と認められず**、ふつうのファイルとして中身の指紋・署名者名・バージョン情報の照合にかけられていました。危険ではありませんが、(1)「Microsoft のファイルは誤検知させない」安全弁が半分しか効かず、(2) 1 ファイルにつき署名の確認と指紋で 2 回読むので、1 回のスキャンの読み取り枠（64 MB）をむだに使っていました。今は**製品のルートと 2011 のアプリ用ルートのどちらかに届けば Microsoft**と認めます（トレイと MOD の両方。40 個すべてが通ることを確認）。自分で作った「Microsoft Corporation」の証明書は、どちらの旗でも拒否されます（実測）。
  - **MOD 側にも同じ直しを**: キャッシュの鍵（NTFS のファイル ID・変更日時・USN）、失敗した読み取りを覚えない、署名の確認のぶんを先に枠から取り置く、の 3 つは**トレイにしか入っていませんでした**。MOD の `SelfScan` にも同じものを入れたので、大きさと更新日時をそろえた DLL の差し替えが MOD 側でも素通りしなくなり、最初に読めなかった DLL が「指紋なし」のまま覚えられることもなくなり、1 回のスキャンが 64 MB の枠を超えて読むこともなくなりました。
  - **MOD 用コピーのフォルダーの穴**: ゲームのフォルダー直下の `Among Us.exe` / `UnityCrashHandler*.exe` は、MOD 用コピーの中にあるというだけで中身を見ずに素通りしていました。今はそのフォルダーも本当にゲームのフォルダーであること（`GameAssembly.dll` と `Among Us_Data` があること）を確かめ、`UnityCrashHandler*.exe` は**もう一方のゲームのフォルダーのものと 1 バイトずつ照らした時だけ**飛ばします（照らす相手が見つからない時は、ふつうのファイルとして調べます）。
  - **黄色いお知らせの条件をそろえた**: 実行ファイルを読めなかった時のお知らせが、Windows のフォルダーにあるものだけ黙って見逃されていました（そこに来る時点で持ち主はシステムではないので、いちばん怪しいものでした）。読み取りに失敗した時と同じ条件にそろえました。
  - **NG ワードの長さの種類の上限**: 定義ファイルのハッシュの一覧が 24 種類より多くの長さを使うと、超えたぶんは黙って一切当たらなくなっていました。ほかの上限と同じように、読み込みのお知らせ（`notes`）に出します。
  - 文言: 中国語の文に使っていた日本語の中黒「・」を「、」に直しました（意味は同じです）。共有 `AegisHash` のコメントが指していたテスト用ファイルの名前も直しました（3 つとも同じ 1 文字だけ）。
- **必要な版の書きまちがい防止を強化**（作者「極力無くして」）: `tools/sign-definitions.ps1` は `minmod` を上げる時、GitHub の最新リリース（`/releases/latest`＝下書き・プレリリースではない）に、ランチャーが実際に落とす資産（`PocketRoles-<版>.zip`、Setup を除く最初の一致、アップロード済み・サイズ 0 でない）があることを確かめ、リリースのタグ・URL・公開日・資産の大きさを表示します。オフライン・API 制限・HTTP エラー・資産の名前違い・アップロード中を見分けて拒否します。
- **平文からハッシュへの一括変換 `-HashLegacy`**（移行）: 必要な版のフロア（コミット済みの署名付き `minmod`）が v0.5.5 以上になってから、残りの平文（`[tools]`/`[dlls]`/`[dllwords]`/`[ngwords]`/`[ngallow]`）を私的リストに取り込んでハッシュにし、平文の行を消します（v0.5.4 のトレイは平文のない `[tools]` を無効と見なすので、それまでは使えません）。署名はしません（version= を上げて別に署名）。
- **必要な版の書きまちがい防止を強化**（作者「極力無くして」）: `tools/sign-definitions.ps1` は、`minmod` を上げる時、GitHub の最新リリース（`/releases/latest`＝下書き・プレリリースではない）に、ランチャーが実際に落とす資産 `PocketRoles-<版>.zip` が正しい名前で・アップロード済み・0 バイトでないことまで確かめ、リリースの URL と公開日を表示します。オフライン・API 制限（403/429）・読めない、を見分けてメッセージを出します。`build-release.ps1` に「minmod を上げるなら先に zip を公開」の注意を追加。
- **定義ファイルに署名**（提案の「定義ファイルに署名を付ける（偽の定義ファイルを読ませる攻撃を防ぐ）」に 9/22「これ入れちゃおう」）: `aegis/definitions.txt` に作者の鍵で署名した `aegis/definitions.txt.sig`（RSA 3072・SHA-256）を付け、ゲーム内 Aegis とトレイアプリは**署名が合ったファイルだけ**を使うようにしました。署名がない・合わない時は、今の値（前に保存した署名済みのファイルか組み込み）のまま続けます。GitHub のリポジトリや途中の通信が乗っ取られても、偽の判定値・NG ワード・共有 BAN の一覧は読ませられません。署名は改行（CRLF / LF）と先頭の BOM の違いを除いた中身にかかるので、GitHub の raw と手元のファイルのどちらでも確かめられます。
  - 古いファイルに戻させない: MOD は今使っているものより**新しい版**（`version=`）にだけ入れ替えます（同じ版なら今のまま「最新です」）。MOD が受け付ける最低の版は v3（`MinDefinitionsVersion`。前に保存したファイルがなくても、それより古い版は使いません）。トレイも、同じ版は中身がまったく同じ時だけ保存します。
  - 保存したファイル（`aegis-rules-cache.txt`）も署名と一緒に保存し、起動のたびに確かめ直すので、書き換えられたファイルは使いません。
  - `/ac rules` に「・署名 OK」か、使わなかった理由（署名なし・不一致・無効にした鍵）。GitHub のファイルが古い版だった時は `/ac rules reload` の返事でそう出します。トレイのスキャン画面の「Aegis エンジン」の行は「検知ルール 26 件・定義ファイル v2・署名 OK」か「定義の署名なし・不一致（組み込みの定義を使用）」（黄色の注意だけで、起動は止めません）。
  - 署名の鍵を守る（要望 9/22「その鍵はpc保管用とusb保管用に2個作れるの？あとそれぞれにはパスワード保護つけたい」）: 作者用の `tools/sign-definitions.ps1` に、PC の鍵をパスワードで暗号化する `-Protect`、USB メモリなどへ別のパスワードで控えを作る `-Backup`、署名の時に鍵を選んでパスワードを入れる流れ（本人が黒い画面で入力。画面には `*` だけ。3 回まで）を足しました。暗号は PBKDF2-HMAC-SHA256（60 万回）で作った鍵による AES-256-CBC と HMAC（encrypt-then-MAC）。信頼する鍵を複数持てる形にし、漏れた鍵は無効にできます（無効にした鍵の署名は「Revoked」として使いません）。同じ版のまま中身を変えて署名し直すことはできず、`build-release.ps1` は署名が合わないとリリースしません。デスクトップの「PocketRoles 署名の鍵」フォルダに、鍵を守る・USB に控える・署名する の 3 つの .cmd と説明を置きました。
- **判定値を部屋ごとにずらす**（提案の「判定の基準を部屋ごとに少しずつランダムにずらす（ぎりぎりを狙わせない）」に 9/22「これ入れちゃおう」）: 定義ファイルは GitHub で誰でも読めるので、公開された値のすぐ下で使うチートを防ぐため、新しい部屋に入るたびに Aegis の判定値を項目ごとにランダムに上下させます。設定 `[AntiCheat] Jitter`（既定 10 %、0〜20、0 = ずらさない。`/opt anticheat.jitter <0-20>`、設定タブ「Aegisの判定値を部屋ごとにずらす(%)」。ホストだけ）。乱数は OS の暗号用の乱数で、部屋コードや時刻からは予想できません。「もう一度遊ぶ」で同じ部屋に戻った時は引き直しません。
  - ずらす項目（16）: スピードハックと速い移動の倍率、遠いベントの距離、早すぎるキル、遠すぎるキル、チャット連投・ロビーの色の高速変更・一瞬のタスク完了の回数と秒数、2 回で退出の間隔。ずらさない項目: 2 回で退出の回数と言い当て通知の人数（小さい整数なので 1 つずれると意味が変わるため）、ラグ対策の値（速さを測る秒数、瞬間移動を外す基準、会議直後のチャットの猶予）。
  - 退出につながる値（スピードハック・チャット連投・2 回で退出の間隔）は、厳しくする側へは設定の % の半分までしか動きません。スピードハックは 1 倍を超える分だけをずらすので、組み込みの 2.5 倍は 10 % で 2.425〜2.65 倍、20 % で 2.35〜2.8 倍です（ラグのある人の余裕が減るのは最大 5 %、20 % で 10 %）。
  - その部屋の値はホストだけが見られます（ログに部屋ごとに 1 行、`/ac rules lobby`（`/ac rules 部屋` でも））。`/ac rules` には「部屋ごとに ±10% ずらし中」の 1 行。ずらしがオンの間は、全員への退出のお知らせにスピードの倍率を出さず「ありえない速さで移動した(スピードハック)」にしました（ホストの画面には正確な値）。
- **MOD の中のセルフチェック**（提案の「MOD がゲームの中から、自分と同じゲームにチートの DLL やほかのプラグインが入っていないかを、起動時と試合中に定期的に調べます」に 9/22「これいいね」）: MOD が自分のゲームのプロセスを、読み込み時・60 秒ごと・メインメニューとオンラインのメニューが開いた時に調べ（裏のスレッド。自分のプロセスだけで、ほかのアプリは見ません）、次のものがあると**ロビーを作れなくします**（作成ボタン、自動再ホスト、`/move`・高 PING・時間切れの作り直しのすべて。ローカルの部屋も）。ランチャーやトレイを通さずに起動しても効くので、「PocketRoles とチートを一緒に入れる」ことを直接止められます。
  - 止めるもの: `BepInEx\plugins`（サブフォルダも）と `BepInEx\patchers` にある PocketRoles 以外の DLL、BepInEx が読み込んだほかのプラグイン、名前がチートと一致する DLL（読み込まれていれば場所を問わず）、Among Us.exe の横から読み込まれた差し替え DLL（version.dll など。システムの DLL と同じ名前で正しい署名のないものも）、ゲームフォルダの名前に menu・cheat・inject を含む DLL、BepInEx be.735 のものではない winhttp.dll。
  - 見つけた時: ポップアップ（日本語・中文・English）に「何が・どこにあるか・どう直すか（そのフォルダから外して Among Us を再起動）」。ロビーや試合の途中で見つかった時はホストにだけ 1 回知らせ、今の部屋と試合はそのまま続けて、次のロビー作成を止めます。止めた状態はゲームを閉じるまで続きます（もう読み込まれているため）。よその部屋に参加している時は何も出さず、メインメニューに戻った時にポップアップを出します。ログには 1 件ごとにファイル名と場所（画面ではユーザー名を伏せます）。
  - 知らせるだけ（止めません）: 署名のない DLL（Windows のフォルダ・Program Files・.NET・ゲームのフォルダのものは除く）と、使われていない PocketRoles の重複コピー。
  - 誤検知の逃げ道: 設定ファイルの `[AntiCheat] SelfScan = false`（既定 true。`/opt` にはありません）にすると、差し替え DLL（ReShade・DXVK の dxgi.dll / d3d11.dll など）・名前の言葉・winhttp.dll・下のゲームのコードの書き換えは、ホストへのお知らせだけになります。ほかのプラグイン・パッチャー・既知のチートの DLL は止めたままです（設定 1 行でチートが通らないように）。
  - **ゲームのコードの検査**（提案の「GameAssembly.dll のコードがメモリ上で書き換えられていないか」などに 9/22「足しちゃおう」）: メモリ上の GameAssembly.dll のコードを、ディスクのファイル（読み込まれた位置に合わせて直したもの）と比べます。MOD 入りのゲームは BepInEx（Harmony）と Il2CppInterop がもともと関数の先頭を書き換えているので、メインメニューが出た時点でその場所の一覧を Harmony と Il2CppInterop から読み取って基準にし、一覧の外の書き換えと、基準の後の変化を見つけたら（2〜3 秒後にもう一度見て、書き込みの途中ではないことを確かめてから）ロビーを作れなくします。60 秒ごとに裏のスレッドで約 20 ms。ログには書き換わった場所とジャンプ先の DLL（DLL の外なら「DLL の外のメモリ」）、ファイルの SHA-256。ゲームの起動中は GameAssembly.dll の名前変更や差し替えもできないようにしました。一覧が読めない時（BepInEx の版が違うなど）は止めずに知らせるだけです。Il2CppInterop の準備（今まではメニューの最初のボタンで行われていたもの）を MOD の読み込み時に行うようにしました（フックの数は同じ）。
  - **ドライバー名の確認**（同じ提案）: 読み込まれているカーネルドライバーの名前を、チートに悪用されることがあるドライバー 27 個の名前と比べ、当たればホストにだけ知らせます（MSI Afterburner の RTCore64.sys のように普通のツールも使うので、止めません）。すでに読み込まれている名前を読むだけで、何も入れません。
  - `/diag` の最後にセルフチェックの状態（チャットは最大 16 通に。ログには全部）。
  - 見つけられないもの: 手作業でメモリに読み込ませる注入（manual map）、ほかのプロセスからのメモリの書き換えやデータだけを変えるチート、ディスク上の GameAssembly.dll そのものの書き換え（SHA-256 はログに残るので、あとから比べられます）。相手の PC の中は、今まで通り見られません。
- **トレイ: 脆弱ドライバーの遮断**（同じ提案に 9/22「足しちゃおう」）: スキャン画面の「カーネルの保護」（メモリ整合性）のすぐ下に「脆弱ドライバーの遮断」（Windows の Microsoft 脆弱ドライバー ブロックリスト）の行を足しました。無効なら黄色の注意だけで、起動は止めません。メモリ整合性かスマート アプリ コントロールがオンの時と、Windows 11 22H2 以降の既定（レジストリに値がない）の時は有効として扱います。
- **ロビーの延長の修正と、1 人では廃村しない**（9/22 の実験（1 人のロビーで時間切れを待つ）とログの調査で見つけた不具合）:
  - **延長の自動受け入れは v0.5.4 まで一度も動いていませんでした**。2026.8.18 の本体は、サーバーからの延長の申し出（9/22 のログでは残り 59 秒で +180 秒）を読み捨て、延長のポップアップも出しません。MOD はそれを待っていたので呼ばれていませんでした。v0.5.5 からは MOD がサーバーの通信を直接読み、延長モード（`TimerMode = extend`、既定）では申し出が届いたらすぐに受けます。サーバーに断られた時は、残り 20 秒を待たずに次の手へ進みます。
  - 時間切れの時の自動の廃村は、本体の開始に必要な 4 人以上の時だけにしました（1 人での開始は本体の通常の流れではないため）。延長できなかった時、**ホスト 1 人なら廃村の代わりに部屋を作り直します**（「ロビーの時間が切れるので、部屋を作り直します（1人のときは廃村しません）。部屋コードが変わります。」。誰も入らないまま続けて `RehostMaxAttempts` 回（既定 3）まで。そのあとは部屋が閉じるのに任せます）。2〜3 人なら「ロビーの時間を延長できなかったので、まもなく部屋が閉じます（4人未満では廃村しません）。」と知らせて閉じるのに任せます。`TimerMode = haison` でも 4 人未満ならまず延長を試します。
  - 手動の廃村・開始（`/haison`、F7、`/start`、設定画面のボタン、テストモード）は今まで通りです。廃村から戻った直後、まだ誰もいなければ、誰かが入った時に本体がするのと同じ開始状態のリセットを 1 回送ります。
  - 送り主の分からない偽の通信への対策: ホストは、延長の申し出に書かれたホストが自分でなければ無視し、延長の返事は自分が延長を頼んでから 60 秒以内の最初の 1 回だけを受け付け、残り時間の通知は見込みから大きく外れていれば（短すぎる、または頼んでいないのに長すぎる）自動の動きには使いません。
- **確実なチートで試合を止める**（要望 9/22「いいよ」、VALORANT の Vanguard のように）: 登録オフの部屋の試合中に、確実な検知で人を退出させ、その操作で試合が本当に変わった時（キルできない役職のキルが決まった、ベントを使えない役職がベントに入った、持っていない変身・透明化・透明化の解除を使った）は、退出の約 1 秒後に廃村（F7）と同じ終わり方でその試合を終わりにします（`[AntiCheat] EndGameOnCheat`、`/opt anticheat.endgame`（`endgame`）、設定タブの「確実なチートで試合を止める」、既定オン）。
  - 終わりの画面は、インポスターが切断した時と同じもので勝ち負けが出ますが、Among Us には試合を無効にする仕組みがないので意味はありません。ホストは自動でロビーに戻り、ほかの人は終わりの画面で「もう一度プレイ」を押すと戻ります。試合後のまとめは出しません。試合中の全員向けの「ホストが試合を終了しました（廃村）」と、そのための最大 15 秒の待ちもありません。
  - 全員へは名前を出さない 1 行（「[Aegis] 不正を見つけたので、この試合を終わりにしました。見つかった人は部屋から出しました。さっきの勝ち負けは気にしないでください。」）を、参加者がロビーに戻ってから出します（試合にいた人の 6 割が戻り、最初の人が戻ってから 3 秒たった時。そこまでそろわなくても、だれかが戻って 3 秒たち、ホストが 12 秒ロビーにいれば出します。45 秒だれも戻らなければ出しません）。そのあとに戻ってきた人がいれば、全員がそろった時か 20 秒後に、もう 1 回だけ同じ行を出します（1 回だけだと、遅れて戻った人に届かないため）。名前入りの退出の行の代わりで、`AnnounceKick` がオフでも出します。ホストへの退出の行は、ロビーで「[試合中の記録]」を付けてもう一度出します。死亡中のホストの退出の行など、ロビーまで待たせていた全員向けの行も、同じく参加者が戻ってから出すようにしました。
  - 始まりの画面とその前の読み込みの間は終わりにせず、会議の結果・追放の画面の間は終わるまで待ちます（閉じてから 0.5 秒後）。30 秒たっても安全な場面が来なければ、自動では終わりにせずホストに廃村を案内します。送った終わりが 8 秒たっても届かない時や、送れなかった時は、本体の終了判定と船の動きを戻して（会議・通報・ふつうの終わり方がまた使えるように）、F7 がまた使えるようにします。F7 の廃村で終わりを送れなかった時も、同じように戻すようにしました。本体がすでに試合を終わりにしていた時（退出させた人が最後のインポスターだった時など）は、2 回目の終わりを送りません。
  - 自動で終わりにできなかった時と、待っている間に設定（または「チートの人を自動で退出」・定義ファイル）をオフにした時は、試合は続けて、いつもの名前入りの退出のお知らせ（「退出させたことを全員に知らせる」に従う。ホストが死亡中なら試合後）を出し、ホストにもそう知らせます。「この試合を終わりにしました」の行は出しません。
  - 止めないもの: 2 回で退出・お知らせだけの検知、試合が変わらない操作（インポスターのタスク完了、ホストにだけ届く変身・透明化のお願い、決まらなかったキル。退出はさせて、ホストの行に理由を「インポスターのタスクは勝ち負けに関係しないので」「キルが決まらなかったので」「ホストにだけ届いたお願いで」のように場合ごとに出します）、VIP 以上、登録ありの部屋（ゲームのルールの検知は登録オフだけ。両方で動く知らない通信の検知はお知らせだけで、試合を変えないため）。キルが決まったかどうかは、本体がキルを処理したあとで相手が本当に死んだかで確かめます（守護天使に守られた人へのキルでは止めません）。同じ瞬間に同じ人の別の退出（チャットの連投など）が先に入っていても、確実な検知のほうに入れ替えて、試合を止められるようにしました。
  - 1 試合 1 回、1 時間に 3 回まで（部屋を作り直しても数えます。`/ac test` は数えない）。通信に送り主が書かれていないので、なりすましで他人を退出させて試合を止めることも理論上はでき、その被害を小さくするための上限です（はめた人は部屋に残ります。上限があるのは試合を止めることだけで、退出と BAN には上限がありません）。定義ファイルの `[rules]` に `endgame = off`（`endgame.<ルール> = off`）で、退出はそのままで試合だけ止めなくできます（止める方向には変えられません。`/ac rules` に出ます）。
  - 別の作業（作者による BAN の解除）とのつなぎ: 解除から 30 日の間は同じルールの自動の BAN をホストに知らせるだけにする予定で、その間は試合も止めません（`AegisMatchStop.UnbanGrace`、今はつなぐ場所だけ）。
  - `/ac test <kill|vent|ability> <名前> kick` で本当に試合が終わり、ロビーの行に「[テスト]」が付きます（Aegis の BAN の記録・証拠・通報は残りませんが、この部屋への BAN は付きます）。`kick` の代わりに `stop` なら、だれも退出させずに試合を止める動きだけを試します（3 人の部屋では退出で 1 対 1 になり、本体が先に終わりにするため）。`kick 20`・`stop 20` のように秒数を付けると、その秒数のあとに試します（会議中に打って会議のあとで試す用、1〜120 秒）。止めない時の返事は理由を言葉で出します（「この試合ではもう止めました」「試合中ではありません」など）。`/ac` の使い方の表示と README のコマンド表にも `[kick|stop] [秒]` を足しました。
  - 前の試合の役職の控えを、試合の終わりと別の部屋に入った時に消すようにしました（次の試合の読み込み中、前の試合の役職で検知が動くことがあったため）。
  - README の廃村の説明の「勝敗なし」を、実際の終わりの画面（インポスターが切断した時と同じで、勝ち負けは出るが意味はない）に合わせて直しました。
- **部屋をまたぐ BAN（自分の BAN と共有 BAN）と公式への通報**（要望 9/21「全員バンするのは良いけどもしあとからその人が改心してやめたらならどうするの？」「Vanguardと似たバン期間の決定しない？悪質な場合は永久停止とか」、提案の「BAN の共有」「証拠にもとづく異議申し立て」に 9/22「これは予定じゃなくない？実装するじゃん？」）:
  - **自分の BAN**（`BepInEx\PocketRoles\aegis-bans.json`）: 確実な検知（キルできない役職のキル、ベントを使えない役職のベント、持っていない能力、インポスターのタスク完了）で退出させた人を、自分が立てる**すべての部屋**で BAN します。**1 回目 30 日、2 回目 180 日、3 回目から無期限**（`[AntiCheat] BanLadder`、既定オン。オフならその部屋だけの BAN で v0.5.4 と同じ）。期限が来れば自動で解けます。解除しても違反の回数は残り、次の BAN はその続きです。NG ワードやくり返しでの退出は、今まで通りその部屋だけの BAN です（証拠の記録は残ります）。
  - **ホストの BAN も記録**: `/ban <名前|番号> [日数]`（日数なしは今まで通り無期限で `Banlist.txt` にも。日数付きは期限があるので Aegis 側だけ）、`/aegis ban <名前|#番号> [日数]`（この部屋にいた人も名前で）、本体の BAN ボタン（無期限）。`/ban remove` と `/aegis unban` はどちらも両方を解除し、Aegis 側で解除した人が `Banlist.txt` に残っていれば、次に入ってきた時にその行を消して入れます。
  - **共有 BAN**: 署名された定義ファイルの `[bans]` に載った人は、PocketRoles を使うすべてのホストの部屋に入れません（`[AntiCheat] SharedBans`、既定オン。定義ファイルを取る `RemoteRules` がオンの時だけ）。載せるのは開発者が証拠を確かめて署名したものだけで、自動では何も共有しません。1 行は「ハッシュ・段階・期限の日（UTC でその日の終わりまで）か never・理由」。
  - **人の見分け方**: フレンドコード（なければ PUID）と PUID に決まった文字列を足した SHA-256（64 桁の値）を使います。フレンドコードそのものは、ファイルにも共有リストにも載せず、ログには値の頭 8 桁だけ。**公開される共有リストには PUID のハッシュだけを載せます**（9/22 の見直し: フレンドコードは単語と 4 桁の数字なので、そのハッシュは総当たりで戻せてしまうため。フレンドコードのハッシュはこの PC の記録と異議申し立ての照合にだけ使います。BAN 管理は、共有リストにだけある人も証拠の記録の PUID からたどって、貼ったフレンドコードで見つけます）。
  - **入ってきた時**（案 B。要望 9/22「あのriotサポートみたいな文？でもしごばんなら解除するよって文も出すか」→「Bで行こう」）: 登録オフの部屋には本人にだけ送る方法がないので、その人の画面の準備ができるのを最大 5 秒待ってから、**名前を出さずに**全員へ「[Aegis] 入室が制限されている人がいたため退出させました。…」（10 秒に 1 回まで）、3 秒後に退出させます（9/22 のあとの見直しで、本人に知らせて 30 秒後に退出に変えました。上の「入室制限中の人は本人に知らせて 30 秒後に退出」。9/22 の見直しでキックに。同じ部屋にまた来たら、お知らせなしですぐにこの部屋へのバン付きで退出）。ホストの画面には名前・BAN の番号・残りの期間。VIP 以上は退出させず、ホストにだけ知らせます。入室制限中の人には挨拶を送りません。
  - **公式への通報**（要望 9/22「どっちも応用して」）: 確実な検知で退出させる時は、退出の前に Among Us 公式の通報（チート・ハッキング）も自動で送ります（`[AntiCheat] AutoReport`、既定オン。同じ人へは 30 日に 1 回、全体で 1 時間に 5 件まで）。それ以外（NG ワード、くり返し、ホストの判断）は、ホストが `/aegis report <名前|#番号> [cheat|chat|harass|name]` で送ります（部屋にいる間だけ）。送った通報はログと `aegis-bans.json` に残ります（公式からの返事は読みません。`/aegis report` の返事も「通報しました」だけです）。どう扱うかは Innersloth が決めます。
  - コマンド（ホストのみ）: `/aegis bans [ページ]`（有効な BAN の一覧と共有リストの件数）、`/aegis ban`、`/aegis unban <名前|#一覧の番号|AEG-番号>`、`/aegis report`（`/ac bans|ban|unban|report` でも）。`/h host` にも 1 行。設定タブ「全般」に「確実なチートは長期BAN(30日→180日→無期限)」「確実なチートを公式に自動通報」「共有BANリストを使う」、`/opt anticheat.banladder|anticheat.autoreport|anticheat.sharedbans`（ホストのみ）。
  - 9/22 の見直し: `/aegis ban` と `/aegis report` は、名前が完全に一致する人・#番号・フレンドコードだけに当てます（名前の一部では実行しません）。#番号は部屋を出た人と今いる人で重なることがある（本体が空いた番号を次の人に配る）ので、当てはまる人が 2 人以上なら何もせずに候補と `@番号` を返します。この部屋にいた人の記録は、100 人を超えたら古い人から忘れます（前は新しい人を覚えませんでした）。手動の BAN の押し間違いは `/aegis unban <名前> mistake` で違反の回数も 1 つ戻せます（自動の BAN には使えません。本体の BAN ボタンのお知らせにも書き方を出します）。入室制限の退出はキックにしました（BAN 付きだと本体が全員に名前付きで「BAN された」と出すため）。同じ部屋にまた来た人は、お知らせなしですぐにその部屋への BAN 付きで退出させます。共有 BAN の写し（この PC の記録）は、共有リストからその行が消えたら解除済みにし、新しい BAN の長さや段階には使いません（段階は今のリストの行からだけ引き継ぎ、共有リストにある人に日数付きの BAN をしても無期限になりません）。`/ban <名前>` の `Banlist.txt` の行は、Aegis 側の BAN が解除された時に加えて、期限が切れた時（BAN 管理で期間を変えた時など）も次の入室で消します（その BAN より後に書かれた行は残します）。`/ban <名前> <日数>` は完全な名前か番号の時だけ実行し（部屋を出た「Player 2」を「Player」の 2 日と読まないよう、部屋にいた人の名前も見ます）、記録できなかった時や、もっと長い BAN が続いていて期間が変わらない時はそう返します。`/ban remove` と `/unban` による Aegis の BAN の解除は名前の完全一致だけにし、アドミンが解除できるのは手動の BAN だけ（自動の BAN はホスト）、履歴には実際に解除した人を残します。`/aegis bans` の 1 ページが返事の上限で切れていたのを直しました。BAN 管理の「取り込み元」が言語の切り替えですぐ変わるようにしました。`build-release.ps1` は署名せず確かめるだけにし（パスワードなしの鍵が残っていれば止まります）、`tools/sign-definitions.ps1` は同じ版に違う中身で署名しないよう、USB の控えの横の台帳（`-Backup` が一緒に控えます）と、git にコミット済みで署名の合う定義ファイルも確かめます。`BanLadder` と `AutoReport` の説明に、なりすましで他人をはめられる可能性（理論上）を書きました。
  - 直したこと（9/22、異議申し立ての 30 日のテストで見つけました）: 初めて BAN される人の BAN が、無期限として記録されていました（新しく作った記録が「無期限の BAN が続いている」と読まれ、短くしない決まりで期限が消えていたため）。Aegis の 1 回目の自動の BAN は 30 日、`/ban <名前> 7` のような日数付きの BAN はその日数になります。
- **証拠の記録と異議申し立て**（要望 9/21「メールで連絡したら開発者が見てあ、この人悪気ないなって思わせて解除されるってのはない？」、9/22「でも明らかに嘘で解除しようとしてたらどうしようそこが課題」「個人DMはパンクするからディスコで問い合わせるならチケット作ってやろう」「特に野良部屋ならどうやって説明しよう」）:
  - Aegis が退出・BAN させるたびに、証拠を 1 件 `BepInEx\PocketRoles\evidence\AEG-XXXXX.json` に書き出します（番号は BAN の番号と同じ。読み間違えにくい 32 文字から 5 文字）。中身は、何が起きたか（ルール、確実な証拠 / 状況証拠 / ホストの判断、時刻）、部屋（コード、地域、登録の有無、人数、ホストの通信の往復時間、版）、その人（名前と識別のハッシュ。フレンドコードそのものは入れません）、検知の数値とその部屋の判定値、役職の控え、その人についての直近 20 行（Aegis の検知、NG ワードは当たった言葉だけ、入室と退出。ほかの人のチャットは入れません）、通報したかどうか。1 回書いたら書き換えません。退出だけの時（NG ワードやくり返し）も残すので、あとから BAN を決める材料になります。
  - 異議申し立ての方針: 受け付けるのは「誤検知だと思う」という申し立てだけ。判断は証拠の記録で行い、説明の内容だけでは決めません。「反省しました」で早く解除することはしません（期限が来れば自動で解けます）。解除しても違反の回数は残ります（異議申し立てで、作者が証拠を確かめて誤りと分かった BAN は除く）。申し立ては BAN 1 件につき 1 回まで。フレンドコードが記録と一致した時だけ扱います。
  - 窓口: Discord「PocketRoles 役職部屋」の「🛡️｜異議申し立て」チャンネルのチケット（本人と Aegis チームだけが見られます）と、Discord を使えない人（中国など）向けのメール `pocketroles.report+help@gmail.com`。README の最初の方に「入室制限された人へ」（はじめの名前は「BAN されたら」）と「Aegis の今後の予定」を足しました。お知らせは「PocketRoles」で検索して「入室制限された人へ」を見るように案内し（English は「Restricted?」、中文は「致被限制进入房间的人」。中文だけは「在 GitHub 搜索」: 中国では Google が使えず、Baidu などで出るかは確かめていないため）、README 3 言語のいちばん上（言語のリンクのすぐ下）に 3 言語の名前を並べてそれぞれの README のその節へのリンクを置き、サイトの上の方にも同じ名前のリンクを置きました（節の目印は README もサイトも `#restricted`。サイトは前からの `#refused` も使えます）。9/22 の確認で、Aegis の名前で検索すると別のボードゲームや古いリリースのページしか出ず、「PocketRoles」だけならこのリポジトリの README がいちばん上に出たためです。English は「Mistake?」（登録オフの部屋の全員向けは長さの都合で「Wrong?」）のあとに案内し、全員向けは Among Us の「restricted」（クイックチャットだけのアカウントなど）と取り違えないよう「Players restricted here get removed.」に。中文は「入室限制中」をやめて「被限制进入」に。`/cmd id` の返事も 3 言語とも「作者へ（PocketRoles で検索）」にそろえました（English は「Erase code: … Send to the author (search PocketRoles)」、中文は「在 GitHub 搜索 PocketRoles」）。「入室制限された人へ」の連絡先に、記録を消してほしいだけの時の送り方（件名「記録の削除」）を足しました。
- **異議申し立ての解除をすべての部屋へ**（オーナーの決定 9/22「この説明もだよ、こっちで管理するから」、要望 9/22「もう一度 BAN するときの確認」「同じまちがいのくり返しを防ぐ」「意地悪なホストへの対応」「登録なしの部屋ですべて対応する？」）: 参加者にホストを探させない。作者が異議申し立てを受け入れると、PocketRoles（v0.5.5 以降）を使うすべての部屋で、PocketRoles で出したその人の BAN が解けます。BAN 管理の返事の例（受け入れた時。共有 BAN の時の文も。3 言語）は、ホストを探させない書き方に:「PocketRoles であなたに出された BAN は、PocketRoles（v0.5.5 以降）を使うすべての部屋で解けます（ホストの PC に届くまで。ふつうは 1 日以内です）。ほかのホストに連絡していただく必要はありません」。v0.5.4 以前の部屋では、そのホストが新しい版にするまで残ることがある、とも書きます。
  - **`[unban]`**: 署名付きの定義ファイルの新しい「解除の一覧」。1 行は `<コード> <yyyy-mm-dd> [AEG-番号 …]`（`/cmd id` のコード。受け入れた時刻を `yyyy-mm-ddTHH:mmZ` で書くと、その日のそれより後の BAN は残ります）、または `@日付` の下にコード。名前・フレンドコード・ルール名・メモは書きません。`[erase]` と同じく `RemoteRules` がオフでも効き、知らないコードは何もしません。ホストの PC は、ゲームの起動時・起動中の 1 日 1 回・その人が入ってきた時に、その日までに PocketRoles で出したその人の BAN（自動の BAN、`/ban`、`/aegis ban`、BAN ボタン、`[bans]` の行が消えた共有 BAN の写し、PocketRoles が書いた日付つきの `Banlist.txt` の行。入室の時にしか見つからないフレンドコードの行も、すでに解いた BAN の残りとして）を解きます。日付のない `Banlist.txt` の行（手で書いた行・ほかから持ってきた行）は PocketRoles の BAN ではないので残し、ホストの画面に「Banlist.txt の行は残っています（…外すなら /ban remove）」。そのあとに出した BAN と、一覧を知ってから出した BAN は解きません。`[bans]` に行が残っている間は、その人は BAN のまま（行を外す）。`aegis-bans.json` が壊れている間は書きません。載せるコードは、作者が本人と確かめた記録（証拠）のコードで、行には確かめた記録の証拠 ID を必ず書きます。名指しした記録を持つ PC で、記録のコードと行のコードがちがう時は、その行を使わず、ホストの画面に「作者の解除の一覧の 1 行が、この PC の記録 AEG-… と合わないので使いません」。同じコードの 2 行は、新しい日付と、両方の行の証拠 ID を使います。
  - **回数から外す**: 一覧に証拠 ID（AEG-…）が書いてある BAN（作者が記録を確かめた BAN）だけ、その PC で違反の回数を戻します（名指しした BAN ごとに 1 回だけ。前の申し立てや `/aegis unban … mistake` で戻した分は、もう一度戻しません）。作者が見ていない BAN は解くだけで回数は残ります（証拠なしの申し立てで、本当のチーターの回数が全部のホストで消えないように）。
  - **ホストへの知らせ**: 解けた BAN は、ホストの画面にだけ「[Aegis] 作者が異議申し立てを受け入れたので、○○ の BAN を解除しました（AEG-… / Banlist.txt / 共有）」（3 件まで、あとは件数）と「作者が見ていない BAN も解けることがあります。もう一度 BAN する時は、30 日の間は確認が出ます」。ロビーにいる時に出し、まだ出していない分はファイルに残して次のロビーで。同じ版で `[erase]` にも載っている人は、名前の代わりに短いハッシュを出します。`Banlist.txt` の行だけだった人の記録は、ハッシュと日付だけ（名前はホストに知らせたら消します。コードは残しません）。
  - **本人へのコード**: 登録ありの部屋では、入室制限のお知らせの直前に、本人にだけ「[Aegis] 異議申し立てに使うあなたのコード: …（作者にだけ送ってください）」（本人の言語で）。待っている間の最初の `/cmd id` は連投に数えません。登録オフの部屋でも、入室制限されて待っている人の `/cmd id` にその部屋で答えます（オーナーの決定 9/22「他の部屋って言うけどおとなしくやらなくない？見つけるかもわからないし」。ほかの人と同じ「名前: あなたのコード: …」を全員に。制限の言葉は入れません）。その答えはほかの人の `/cmd id` の答え（15 秒に 1 回、待ちは 4 つまで、同じコードは 60 秒に 1 回）や、翻訳など先に待っている全員向けのチャットより先に 1 回だけ出し（部屋から出され始めた後は出しません。出ていった部屋にも出しません）、退出はその答えが出てから 10 秒待ちます（30 秒から最大 10 秒のびます）。登録オフの部屋の全員向けのお知らせに 2 通目「[Aegis] 入室制限中の人は、出される前に /cmd id と打つと、異議申し立てに使うコードが分かります（名前といっしょに全員に見えます）」を足しました（1 通目で 86 文字近くを使うため、別の 1 通）。登録ありの部屋は今までどおり（コードは本人にだけ届く）。コードがなくても異議申し立てはできます（コードがあると早く見つかるだけで、作者はフレンドコード・名前・日時・部屋コードで記録を探し、記録がそのホストの PC にしかない時は、そのホストにランチャーの「ひとり分の証拠」の zip を送ってもらいます。解除を決めて `[unban]` に載せるのは作者だけで、使うのは作者の手元にある記録（作者の PC の記録、報告 zip から取り込んだ記録）だけです。オーナーの決定 9/23「だめ、自分だけ」で、管理人がほかの PC で確かめただけの証拠 ID とコードでは載せません。報告 zip に入る証拠は直近 90 日の分だけなので、BAN から 90 日すぎると、作者がその記録をもう持っている時しか解除できません（README・FAQ・サイトに「申し立ては早めに」）。この行を書いた時は 30 日でしたが、同じ 9/23 の決定「A」で 90 日にしました。90 日より前でも、その BAN がそのホストの PC でまだ効いていれば記録は残っているので、「ひとり分の証拠」の zip で送れます。管理人の「解除を提案」でも、記録がその PC にあれば「ひとり分の証拠」の zip を作者のメール（+host）に送ります（BAN 管理の案内と管理人キットの手順、3 言語。キットは「直近 90 日」のままにしました）。書いてほしいことに部屋のホストの名前（分かれば）を足しました）。「ほかの PocketRoles の部屋で打つ」という案内はなくしました（README・サイト・FAQ・返事の「情報が足りない」）。README の `Banlist.txt` の説明に「手で書いた行には証拠の記録がないので共有 BAN にできない。共有するかもしれない時は `/ban` か `/aegis ban`（証拠の記録が報告 zip に入る）」を足しました。`/cmd id` の返事は「あなたのコード: …。記録の削除や問い合わせの時は、これを作者へ」（English は「Your code: … - for the author (search PocketRoles)」）。コードを出せない時の返事も「コードを出せませんでした」に。
  - **もう一度 BAN する時の確認**: 解除から 30 日、一覧に載っている間（一覧から外すとすぐ終わり）に、その人を `/ban`・`/aegis ban`・BAN ボタンで BAN すると、ホストの画面にだけ「[Aegis] ○○: この人は、異議申し立てで作者が解除した人です。本当に BAN しますか？」。その時は BAN せず、部屋から出しもしません。60 秒以内に `/ban <名前> confirm` か `/aegis ban <名前> confirm` で BAN します（証拠の記録に「確認して BAN し直した」と残ります）。BAN ボタンだけは、押した時にゲームがこの部屋から出してしまう（この部屋だけの BAN）ので、ずっと続く BAN は 60 秒以内に `/aegis ban <名前> confirm`。モデレーター・アドミンの `/ban` はキック（BAN にはしない）になり、ホストに同じ案内。登録オフの部屋では、モデレーター・アドミンの `/ban` の返事（全員に見える）をいつも「○○ を部屋から出しました（くわしくはホストの画面に出ます）」にそろえ、くわしいことはホストの画面に出します（その人が特別だと分からないように）。うっかりを防ぐためのもので、意地悪は止められません（ホストのお約束で扱います）。BAN 管理の「BAN を追加」にはまだありません。
  - **30 日は自動で BAN しない**（オーナーの決定 9/22「同じまちがいの防止を、ほかのホストの PC でも効かせる」→ おすすめの案で）: 一覧の日付（作者が署名したもの）からちょうど 30 日の間、PocketRoles（v0.5.5 以降）のどの PC でも、Aegis はその人を自動で BAN しません（どのルールでも。NG ワードも）。退出になる時は、ふだんどおり部屋から出します（この部屋だけの BAN。全員へのお知らせはいつもと同じなので、ほかのプレイヤーには、ふつうの退出と同じに見えます。ホストの画面の文には、確実な検知でも「BAN を記録しました」と通報の一文が付きません。証拠の記録とログには 30 日の間だったことが残るので、読めばホストには分かります）。ただし `aegis-bans.json`・`Banlist.txt` の BAN、段階と違反の回数、Among Us 公式への通報はしません。証拠の記録はいつもどおり書き、`"action": "hold"` とメモ（「appeal window: …」と 30 日の終わり）を付けます。BAN 管理は、この記録を候補に出さず（報告 zip から取り込んだものも）、共有 BAN の「2 人以上のホスト」にも数えません（`"notice"` の記録も同じ。見直しで直しました: 取り込んだ記録は処分を見ずに候補になり、ホストの数にも入っていました）。証拠の画面と返事用のまとめでは「部屋から出しただけ（異議申し立てが通ったあとの 30 日の間）」と出ます（前は「退出のみ」でした）。作者が確かめた BAN（一覧に証拠 ID のある BAN）を持つ PC では、今までどおり、その BAN のルール（キル・ベント・能力・タスク・生存中のチャット・連投・スピード）では部屋から出すこともしません。ホストには「（異議申し立てで解除された人: ○○ まで自動の退出・BAN なし。退出は /kick）」付きの検知の知らせ、証拠の記録は `"action": "notice"`。`/ac` の一覧と `/ac test`（3 言語）にも出ます。30 日の終わりは一覧の日付だけで決まります（日付だけの行はその日の 0 時（UTC）から、時刻つきの行はその時刻から、ちょうど 30 日。前は日付の終わりから 30 日でした。確認の質問と、確かめた BAN のルールを止める期間も同じ終わりに）。始まりは一覧の日付の 1 日前です（ホストの PC の時計が遅れていても効くように。見直しで直しました: 先の日付の行は、受け取った時から始まって最長 33 日になっていました）。一覧がホストの PC に届く前（起動時と 1 日 1 回）に Aegis が自動で出した BAN は、30 日の中なら、届いた時に解いて違反の回数も戻します（見直しで直しました: 行の日付の終わりより後の BAN が、30 日の BAN と違反 1 回のまま残っていました。ホストが手で出した BAN は今までどおり残ります）。証拠のメモは 30 日の終わりだけにしました（解除の日付は書きません。署名済みの一覧がまだない PC で「accepted on ?」になっていたため）。その人が入り直しても、検知されても、ホストが BAN し直しても延びません。延びるのは、作者が新しい日付で載せ直した時だけです。日付のない行は今までどおり読まないので、解除も 30 日もありません（署名の前の確認でも「直すところ」）。`/ban`・`/aegis ban`・BAN ボタンは今までどおり（先に確認が出ます）。
  - **モード差**: 解除・回数・知らせ・確認は登録あり・オフどちらも同じ。ちがうのは、コードの届け方（登録ありは本人だけに届く。オフは本人だけに送る方法がない）と、試合中のルールは登録オフの部屋でだけ動くこと（登録ありの部屋で止まるのは連投くらい）。
  - **ホストのお約束**: README 3 言語の 3.4 に「BAN を意地悪に使わない」。守らないホストは、作者のサービス（これから作る部屋の一覧、共有 BAN への報告の受け付けなど）を使えなくします（証拠がそろった時だけ、先に知らせて言い分を聞きます）。
  - そのほか: 新しい定義ファイルで `[bans]` から外れた共有 BAN は、次の起動を待たずに 1 日 1 回の確認で解けるように。BAN 管理の方針に「受け入れた時」（コードは本人と確かめた記録から取る、証拠 ID を必ず書く、コードを見ればだれが一覧を調べられるか、同意をもらう、`[bans]` の行は前の版で外す、日付を新しくする時も証拠 ID を残す）、証拠の画面に記録のコード（`/cmd id`）、返事の「情報が足りない」に ④（分かれば）`/cmd id` のコード。署名前の確認スクリプトに `[unban]`（`-Imports` で行のコードと記録のコードを照合、説明のほかの `#` の行と証拠 ID のない行は直すところ）。BAN 管理で `[unban]` に載せる画面は、あとの作業です（それまでは手で書いて確認スクリプトで確かめてから署名）。
- **Aegis BAN 管理**（要望 9/21「その連絡して取り消しこっちでできるの？いちいち定義ファイル見て消すのが面倒」、9/22「でも間違えて解除しないように二重チェックしない？」「そういう専用画面があるアプリとかがあればいいんだけど」「トレイはわからないから」「いかにもそういう画面操作してるっていうのがいいね」）: 開発者の PC で BAN を管理する新しいアプリ `aegis\AegisBan.ps1`（起動は `aegis\AegisBan.cmd`）。Aegis と同じ暗い画面で、盾のまわりが回り、読み込みや処理の間は走査線が流れます。日本語・中文・English。
  - 一覧（名前、日付、ルール、段階と残りの日数、自動・手動・共有、証拠の番号、通報の有無）。有効・期限切れ・すべて、自分の部屋・全体で絞り込み。
  - 検索: 異議申し立てで送られてきたフレンドコードを貼ると、この PC の中で MOD と同じハッシュにして照らし、一致した行を目立たせます（貼ったコードは保存も記録もしません）。
  - 詳細: 証拠の中身、確実な証拠と状況証拠の区別、履歴、異議申し立ての方針。ボタン: 証拠をコピー（Discord のチケットに貼る日本語の文）、返事の例（3 言語 × 受け入れる・受け入れない・情報が足りない）、解除、全体の BAN に載せる、期間を変える、元に戻す。
  - **二重チェック**: どの操作も、実行すると何が起きるか（「全ホストの部屋に反映」など）を書いた確認画面が出て、5 秒たつまで実行ボタンを押せません。全体の BAN を変える時は、定義ファイルの作業コピーを書き換えて版を上げ、署名の画面（パスワードは本人がその画面で入力。アプリは扱いません）を開き、もう一度確認してから GitHub に反映します（git add・commit・push。main でない、関係のない変更がある、GitHub の方が新しい時は止めます）。この PC だけの変更（解除など）は署名なしでその場で。違反の回数と、この版が知らない項目は消しません。
  - 報告 zip の取り込み: 中の証拠の記録（`AEG-*.json`。zip の中のどこにあっても）と Aegis のログ行を「候補」に入れるだけで、自動では何も共有しません。
  - ランチャー（開発モード）は、署名の鍵がある PC でだけ、デスクトップに「Aegis BAN 管理」のショートカット（盾のアイコン `assets\Aegis.ico`）を作ります。
- **BAN 管理の管理人モードと提案**（9/22 に決めた運用: 管理人（Aegis チーム）にも BAN 管理の画面を渡して一時確認と「解除したい」などの提案をしてもらい、最後は作者が確かめて決める）:
  - 署名の鍵がない PC（ランチャーと同じ見分け方。ファイルの名前を見るだけで鍵は開きません）か、`AegisBan.cmd -Admin` で起動した時は **管理人モード（提案だけ）**。見た目は同じで、上に印が出ます。全体の BAN の操作は「解除を提案」「全体の BAN に載せるのを提案」になり、署名・GitHub への反映・定義ファイルの書き換えはしません。自分の部屋の BAN（`aegis-bans.json`）の操作はそのまま（ゲームの中の `/aegis` と同じ）。全体の BAN の一覧は、トレイアプリか MOD が取った署名付きの控えを読むだけ。「設定」に「提案者の名前」。
  - 提案の文: 理由（10 文字以上）を書くと、Discord のチケットに貼る決まった形の文（「【Aegis 提案 v1】」、種類・対象・証拠の強さ・ルール・提案者・理由・日時、「※ この文だけでは何も変わりません。最後に決めるのは作者です。」）をコピーします。最後の 1 行 `aegis-proposal v1 kind=… ev=… line=… strength=… at=…` がアプリの読む部分で、言葉は英字のまま変わりません（ラベルは 3 言語）。ほかの発言が前後にあっても、Discord の引用や折り返しがあっても読めます。フレンドコードの形の文字（名前#1234）は伏せます。証拠のない BAN は全体に載せる提案もできません。
  - 作者モードの「提案を貼り付け」: 提案の文を読んで、証拠 ID か共有 BAN の行（頭 16 桁）で対象を探して選び、証拠の横に提案（だれが・なぜ・いつ・証拠の強さ、提案のあとで変わったこと）を出すだけで、何も実行しません。名前だけでは探しません。
- **正当性の確認**（9/22 に決めた「手動の BAN 登録も、正当性を確かめてから」）: 全体の BAN に載せる時、証拠ファイルのない BAN は止めて、自分の部屋だけの BAN にするよう案内します。状況証拠（手動を含む）は、確認画面でルール（チート・チャット・迷惑行為・名前・その他）を選び、理由を 10 文字以上書くまで、5 秒たっても「実行する」を押せません（貼り付けた提案があれば一緒に表示）。確実な証拠はルールを表示。全体の一覧に入るのはルールの記号だけで、理由の文は定義ファイル・コミット・GitHub に出ません（MOD の読み方は変えていません）。
  - **BAN を追加（この PC）**: 「候補」の証拠の記録から、この PC の BAN を記録できるようにしました（MOD の `/aegis ban` と同じ段階の数え方で、今の BAN より短くしません。「元に戻す」で違反の回数ごと取り消し）。名前を打ち込んで BAN することはできません。
  - **操作の記録** `%LOCALAPPDATA%\PocketRoles\Aegis\ban-console\audit.log`: 全体に載せる・解除・期間の変更・元に戻す・追加・GitHub への反映・反映待ちを捨てる・提案を 1 行ずつ（時刻・操作・証拠 ID と名前・ルール・理由・提案者）。フレンドコード・PUID・ハッシュは書かず、フレンドコードの形の文字は伏せます。BAN の「履歴」のタブに、その人の行を表示。
- **報告 zip に Aegis の証拠の記録**: ランチャーの報告 zip（友達モード・開発モードとも）に、`BepInEx\PocketRoles\evidence\AEG-*.json` のうち直近 90 日のもの（200 件・5 MB まで）を `evidence/` フォルダで入れます。証拠の記録には名前・部屋コード・検知の内容と、その人についての直近のログ（言い当て通知ならその人の発言の最初の 40 文字）が入り、フレンドコード・PUID そのもの、IP アドレス、ほかの人のチャットは入りません（`src\Net\AegisEvidence.cs` で確認）。フレンドコードのハッシュは総当たりで戻せるので、zip に入れる時は `player.hash` を PUID のハッシュに置き換え（PUID がない記録は空に）、ログ行の `hash 1a2b3c4d…` も伏せます（形が違うファイルは入れません）。ログ（`LogOutput.log`・過去のログ・`launcher.log`・`launcher-state.json`）の中の PUID（32 桁の 16 進数）・フレンドコードの形の文字・ハッシュの頭も伏せます（v0.5.5 の MOD はもうログに書きませんが、前の版のログや、ホストが打った文字に残っていることがあるため）。報告 zip のダイアログ（3 言語）に件数と中身を書きます（0 件の時は短い 1 行）。`system.txt` に件数の行。zip の中の名前は `/` 区切りで作るようにしました。
- **管理人キット** `tools\make-admin-kit.ps1`: `Aegis-管理人キット-<版>.zip`（`aegis\AegisBan.cmd`・`aegis\AegisBan.ps1`・`assets\Aegis.ico`・`管理人キットの使い方.txt`（日本語・中文・English。できること・できないこと・提案のしかた・鍵やパスワードを頼まない・受け取らない））を作り、決めた 4 つ以外のファイルや、鍵・設定・証明書の種類、秘密鍵らしい中身があれば zip を消して止まります。`build-release.ps1` には入れていません（リリースの資産名に日本語を使えないため。管理人には Discord で直接渡します）。`aegis\AegisBan.cmd` は引数をそのまま渡すようにしました（`-Admin`）。
- **BAN 管理のテスト** `aegis\AegisBan.ps1 -SelfTest`: 画面を出さずに C# をコンパイルし、提案の文の読み書き（3 言語、Discord の折り返し・引用・ほかの発言つき）、正当性の確認、管理人モードの見分け方（鍵のファイルは開かないこと）、audit.log にフレンドコードの形や PUID が残らないこと、報告 zip の取り込み、BAN の追加と元に戻す、公開のコミットメッセージにフレンドコードのハッシュが出ないこと、証拠 ID が同じ別人の記録をつながないこと、署名と反映の手順が管理人モードで止まることを確かめます（104 項目。PASS / FAIL、終了コード 0 / 1）。
- **README に「Aegis と個人情報（よくある質問）」**（要望 9/22「MOD独自のアンチチートだけどみんな不安がらない？個人情報とか抜き取られたりとかそのへんの質問も載せといたらいいんじゃないかな」）: 3 言語の README の「BAN されたら」の後に、Aegis が調べるもの・調べないもの（参加者の PC は調べない、ホストの PC でもゲームまわりだけ）、送るもの（定義ファイルを受け取るだけ・公式の通報。チャット翻訳は既定でオン）、記録されるもの（Aegis の記録にはフレンドコード・PUID そのものと IP アドレスを入れない。ただし日数なしの `/ban` と `/vip`・`/mod`・`/admin` の名簿は PUID かフレンドコードをそのまま持つ。ログには名前と `puid-hash` だけ）、ハッシュを元に戻せるか、チャット、報告 zip、作者と管理人に見えるもの、記録の確認・削除（名簿とログの場所も）、常駐とカーネル（トレイアプリはランチャーを閉じてゲームも終わると終了）についての Q&A を追加。「よくある質問（かんたん版）」にも 1 行。
- **BAN 管理の見直し**（レビューで見つかったもの）:
  - GitHub に反映する時の公開のコミットメッセージに、フレンドコードのハッシュの頭 8 桁が入っていたのを直しました（総当たりでフレンドコードが分かってしまうため）。共有 BAN の行の ID（PUID のハッシュの頭）か証拠 ID だけを書き、古い版の反映待ちの行も、一覧にない 16 進数は `(id)` に置き換えてからコミットします。「履歴」の説明も画面の言語で出します。
  - 取り込んだ証拠の強さは自己申告として扱い、「確実」でもルールと理由を必須に（一覧は「確実（自己申告）」）。期間の変更と「元に戻す」で全体の BAN を長くする・戻す時も、全体に載せる時と同じ正当性の確認（証拠なしは止める）。
  - 証拠 ID だけで BAN と取り込んだ記録をつないでいたのを、同じ人（ハッシュが一致）の時だけに。「候補から外す」をした記録（reviewed）もつなぎます。貼り付けた提案は、証拠 ID と共有 BAN の行が別の人を指す時は行で選んで注意を出し、提案の名前も表示・比較します。
  - 確認画面のルールを「（選んでください）」に戻すと、貼り付けた提案のルールが残っていたのを直しました。
  - 提案の文: 名前・提案者・理由のバッククォートと `@` を害のない文字に、`aegis-proposal` の言葉も崩します。読む側は見出しか注意書きのあとの機械の行を使い、キーはその行とすぐあとの `key=value` だけの行から読みます（ほかの発言の `rule=` は読まない）。提案の行が 2 つ以上なら注意。
  - 署名・反映の各手順（CheckRepo・BuildWork・Sign・RecordPending・GitCheck・Place・Commit・Push）が、管理人モードでは自分で止まるように。
  - audit.log の結果はコード（`done.local`・`signed 12` など）で残し、画面では 3 言語で表示。書けなかった時は画面の下に黄色で知らせます。
  - 管理人モードの上の説明を短くし、表示が状態の表示（チップ）と重なる時は「…」で切ります。中国語の文の直し。
  - 提案のボタンの説明と管理人キットの使い方: 全体に載せる提案には報告 zip が必ず要ること、報告 zip はチケットに貼らず作者のメールに直接送ること、どのチケットに貼るか（解除は本人の異議申し立てのチケット、全体に載せるのは自分で開いたチケット）、本人確認は記録がある PC で行うことを書きました。
- **BAN 管理の本人確認と記録の見直し**（要望 9/22「あと管理画面に入るときのスキャン手薄すぎない？」）:
  - **いつもロック中で開く**: BAN 管理は「🔒 ロック中（見るだけ）」で開き、一覧・証拠・履歴・提案は見られますが、変える操作（解除・全体に載せる・BAN の追加・期間の変更・元に戻す・GitHub への反映・反映待ちを捨てる・提案・報告 zip の取り込み・候補から外す・フォルダの設定）は本人確認のあとでだけできます。右上に「🔒 ロック中」「🔓 解錠中・作者・署名の鍵・あと 14 分」のボタン。15 分さわらないと自動でロックし、閉じるとロックします。
  - **本人確認**: Windows Hello（顔・指紋・PIN）。作者モードには「この PC に署名の鍵のファイルがあること」と「鍵での確認」の両方が要ります（前は鍵のファイルの名前があるだけで作者モードでした）。「署名の鍵で確認」は、BAN 管理が作った番号に、黒い画面で鍵のパスワードを入れて鍵で署名します（`tools\sign-definitions.ps1 -Unlock`。BAN 管理はパスワードを扱いません）。BAN 管理は埋め込みの公開鍵（無効にした鍵は除く）・番号・5 分以内・この PC とこの Windows ユーザーを確かめ、確かめた署名を `owner-proof.txt` に残して、その後 30 日はこの PC で Windows Hello だけでも作者として開けます（開くたびに署名を確かめ直すので、作ったりコピーしたりしたファイルでは開けません）。鍵での確認がなければ管理人として開きます。Windows Hello が使えない PC の管理人は、画面の 4 けたの数字で開けます（うっかり防止だけ。記録に「確認なし」）。署名・反映の各手順は、作者として解錠していなければ自分で止まります。
  - **読み込みのスキャン**に「取り込んだ証拠とログの照合」「操作の記録（書き換えの確認）」「アプリの外で変わった BAN」「本人確認（ロックの状態・署名の鍵・Windows Hello）」の行を足しました。
  - **操作の記録（audit.log）**: どの行にも Windows のユーザー名・PC の名前・作者か管理人か・本人確認の方法、全体の BAN の変更には署名した鍵の keyid とコミットの頭 12 桁。各行が前の行の SHA-256 を持つ鎖になり、`audit.head` に行の数と最後の行を控えます。開くたびに確かめ、書き換え・行の削除・終わりの切り取りがあると、画面の上に赤く「履歴が書き換えられています（n 行目）」と出します。本人確認・フォルダの設定の変更も記録します。
  - **前回からの変化**: 開いた時に、この Windows ユーザーがこの PC で前に閉じてから記録に増えた行（だれが・いつ・何を）と、**アプリの外で変わった BAN**（`aegis-bans.json` を BAN 管理が書くたびと閉じる時に取る控えと比べ、追加・削除・変更を「ゲーム（MOD）」か「アプリの外（ファイルの直接の書き換えなど）」に分けたもの。記録にも残します）を出します。開いている間のゲームの変化は画面の下に知らせます。
  - **返事の例**: サポートの返事の形（宛名 さま（名前が分からない時はなし）→ お礼 →「PocketRoles Aegis チームの（担当者）です。」→ 結果 → このあと →「この制限は PocketRoles の部屋だけのもので、ほかの部屋や Among Us のアカウントには影響しません。」→ 質問はチケットかメールへ →「今後とも PocketRoles をよろしくお願いいたします。」→ 署名）に作り直しました（日本語・中文・English）。受け入れない返事はルールの種類と期限だけで、検知のくわしい内容は書きません。上に BAN の番号（自動）・宛名（問い合わせに書いてあった名前。下の「返事の例の BAN の番号と宛名」）・担当者（「設定」に「担当者の名前」を追加）・メールの件名の例と、本文・件名のコピーのボタン。長い行が画面の端で「…ご了」と切れていたのを直し、ほかの画面の複数行の文も必ず折り返すようにしました。
  - **取り込んだ証拠のルール**（偽の証拠への対策）: 報告 zip から取り込んだ記録は、同じ zip のログに MOD がその記録を書いた行（`AegisEvidence: AEG-… written`、処分とルールも同じ。自動の検知なら記録の中の検知の行が 5 分以内でその前に）がある時だけ証拠として数え、合わないものは「ログと合わない」として全体の BAN に使いません。取り込む時に zip ごとにホストの名前を付け（同じ PC から来たと分かる zip は 1 人）、2 人以上のホストが同じ人を報告した時だけ全体の BAN に載せられます。取り込んだ記録だけにもとづく全体の BAN は、これまでの回数にかかわらず段階 1・30 日までです。取り込みでは Aegis のログ行が 5000 行を超えても、証拠を書いた行は全部残します。
  - **窓口**: プレイヤーの異議申し立ては `pocketroles.report+help@gmail.com`、ホストと管理人からの報告 zip や依頼は `pocketroles.report+host@gmail.com`（提案の説明・管理人キットの使い方・README を直しました）。管理人キットの使い方に「管理人キットは作者から直接受け取ったものだけを使ってください。GitHub と作者以外から来たものは使わないでください（偽物に注意）。」と、鍵・パスワード・トークンをだれにも教えないこと、本人確認と取り込みのルールを足しました（3 言語）。
  - `-SelfTest` は 238 項目に（本人確認の署名の確かめ方、ロック中は何も変えないこと、Windows Hello を呼べること、記録の鎖、前回からの操作、アプリの外の変化の見分け、返事の文、画面の文の折り返し、取り込んだ証拠のルール）。
- **BAN 管理の本人確認と記録の見直し（レビュー 2 回目）**:
  - **取り込んだ証拠でほかの人を載せられないように**: 報告 zip の記録で、`player.hash` と `player.puidHash` が別の人になっているもの（ランチャーの報告 zip ではいつも同じなので、書き換えられた記録）は「ログと合わない」にし、全体の BAN にもこの PC の BAN の追加にも使いません。前は、2 つのハッシュのどちらかが合えば同じ人として数えていたので、正直なホストの報告を別の人（全体の一覧に書く PUID のハッシュの人）の 2 人目に数えられました。ホストの数は、全体の一覧に書く PUID のハッシュがまったく同じ記録だけで数えます（この PC の記録も同じ）。同じ記録（証拠 ID と PUID のハッシュが同じ）を 2 回・別の名前で取り込んでも、最初に取り込んだ時の 1 人です。
  - **ログの行で人も照合**: v0.5.5 の MOD の `AegisEvidence: … written` の行にある `puid-hash 1a2b3c4d` が、記録の人の PUID のハッシュの頭と合うことも確かめます（同じログのほかの行にこの印があるのに、この行にだけない時も合わないとします。前の版の MOD のログはこれまで通り）。
  - **全体の一覧に書く人**: 全体に載せる・全体の BAN を長くする（期間の変更・元に戻す）時は、書く行のハッシュが、つないだ証拠の人の PUID のハッシュとまったく同じでなければ止めて、操作の記録に残します（`aegis-bans.json` の PUID のハッシュを別の人のものに書き換えられた時など）。「アプリの外で変わった BAN」も、PUID のハッシュが別の値に変わったことを見つけます（控えにはハッシュそのものは入れません。MOD が空の欄を埋めるのは知らせません）。
  - **段階と長さ**: 取り込んだ記録だけで期限切れ・解除済みの全体の BAN を戻す時（期間の変更・元に戻す）も段階 1 からにし、期間を選ぶ画面にも 30 日より長い選び方を出しません（今の BAN の方が長い時は、短くするのは選べます）。「候補」から全体に載せる時、もっと長い（無期限の）全体の BAN がある人は、取り込んだ記録では載せ直さず（短くなるため）、段階も下げません。
  - **記録の書き換えの確認**: この Windows ユーザーが前に閉じた時の行の数に加えて鎖の値も `seen.txt` に控えて比べ、同じ長さに作り直した鎖や、鎖を全部消して `audit.head` も消したものも「履歴が書き換えられています」と出します。前の版の記録（鎖のない行だけ）には最初に `audit.head` を作り、それからの書き換えを見つけます。控えより 1 行多い時は、黙って直さず黄色で知らせます。BAN の記録の控え（`bans-snapshot.json`）が消えていた時も、黙って作り直さず知らせて記録に残します。
  - **時計**: PC の時計を戻して、期限の切れた 30 日の記録（`owner-proof.txt`）を使えないように、このアプリが前に書いた時刻（操作の記録・`seen.txt`・`audit.head`）より前の時刻では使いません。
  - **鍵のパスワードを入れるスクリプト**: 鍵での確認と署名には、このアプリと同じ場所の `tools\sign-definitions.ps1` を先に使い（`settings.txt` やフォルダの設定で別のスクリプトに向けられないように）、黒い画面の最初にそのスクリプトの場所と SHA-256 の頭 16 桁を出します（「設定」にも同じ値）。署名の鍵がある PC では、リポジトリとゲームのフォルダは作者として解錠している時だけ変えられ、変えた場所は操作の記録に残ります。本人確認でモードが変わってフォルダの設定が捨てられた時は、そう知らせます。
  - **自動ロック**: 15 分の自動ロックを止めるのは、手順が動いている間だけにしました（手順が終わった「GitHub に反映」のボタン付きの画面を開いたままにしたり、ゲームが `aegis-bans.json` を書いて読み直したりしても延びません）。
  - **鍵での確認の黒い画面**: 本人確認の文（「本人確認できませんでした」など）にし、うまくいった時は Enter を待たずに自動で閉じます（Enter を待つ間に 5 分を過ぎて、正しい応答がはじかれていたため）。Windows Hello の呼び出しがエラーで終わった時も 4 けたの数字で開けるようにしました（鍵のない管理人が開けなくならないように）。
  - **文の直し**: 右上のボタンに「押して解錠」「押すとロック」。鍵はあるのに鍵で確認していない時の「設定」のモード・全体の BAN のボタンを押した時・モードが変わった時の説明（作者になるには 🔓 でロックして「署名の鍵で確認」）。返事の例: ルールの種類をかっこの中のかっこにしない（「PocketRoles のルールのうち「不正なプログラム（チート）の使用」にあたる」）、受け入れた返事は「今回の制限は…影響していません。」、無期限や期限の過ぎた BAN には「期限より早い解除は…」を書かない、英語と中国語の言い回し。「ログと合わない」の説明は、よくある原因（報告 zip に入るログは直近 3 回分だけ）を先に書き、試合のすぐあとに報告 zip を作ることを提案の説明・管理人キットの使い方にも書きました。アプリの外の変化の行は「見つけた PC」として出します。英語の「·」の区切りなど。管理人キットの使い方の「GitHub」は公式の wakayamachannel/PocketRoles だけと書きました。
  - **窓口**: 3 言語の README の「困ったらメール」と第 28 章の表に `pocketroles.report+host@gmail.com`（Aegis: チーターの報告・共有 BAN の依頼。報告 zip を添付）を足しました。メールの取り込み（`tools\ReportFetcher`）は `+host` のメールを `reports\aegis` に分けます。
  - ソースの中の見えない制御文字（U+0001）を `\u0001` と書くようにしました。
  - `-SelfTest` は 288 項目に（2 人分の識別子を持つ記録、ログの `puid-hash`、同じ記録を 2 回取り込んだ時、書き換えた PUID のハッシュ、戻す BAN の段階、長い BAN を短くしないこと、作り直した鎖・消した鎖と控え・足された 1 行、控えが消えた時、時計を戻した時、黒い画面の表示、ボタンの文、ソースに制御文字がないこと）。
- **返事の例の BAN の番号と宛名**（要望 9/22「あと自動でばんidを文章に足してよ いちいち例からキーを変えるのがめんどい」「なまえわかるのに」）:
  - **BAN の番号は自動で入る**: 「受け入れる」「受け入れない」の本文（「BAN の記録（AEG-7F3K2、2026-09-20）を確認しました」）と件名に、選んだ BAN の番号が自動で入ります。手で入れたり書き換えたりしなくて大丈夫です。「情報が足りない」は本人か確かめる前の返事なので、件名にも本文にも番号を入れません（ほかの人のフレンドコードを書いて問い合わせた人に、BAN があることや番号が届かないように）。番号・日付・ルール・期限は、返事で伝える 1 つの BAN から取ります。
    - この PC の BAN が効いている時（全体の BAN にも載っている時も）: その BAN の証拠 ID と開始日。証拠 ID がなければ、この BAN 管理の操作の履歴（解除・期間の変更・元に戻す・追加、この行から載せた時）に残っていた証拠 ID（この PC だけの BAN にも使います。ほかの記録から全体の BAN に載せた時の証拠 ID は、その共有 BAN のものなので使いません）。
    - この PC の BAN が終わっている（期限切れ・この PC だけ解除）のに全体の BAN が残っている時と、全体の BAN の人が入ってきた時にゲームが作る記録の行・全体の一覧だけにある人: この BAN 管理でその共有 BAN に載せた時の証拠 ID と載せた日 → 全体の一覧だけにある人は、その人の BAN の記録のいちばん新しい証拠 ID（退出だけの kick の記録は使わず、取り込んだ記録はログの確認が通ったものだけ。載せた時の記録とは限らないので、画面にそう出ます）→ 全体の一覧の行の頭 8 桁（「共有 BAN 1a2b3c4d」。定義ファイルと GitHub のコミットの文に出ている番号）。この PC の古い BAN の証拠 ID と開始日は使いません（前は、6/1 のこの PC の BAN の番号と日付に、あとから載せた全体の BAN のルールと期限が混ざっていました）。
    - 全体の BAN を解除して一覧から消えた記録の行: この BAN 管理の履歴に残る行の番号。
    - 前は、証拠のない共有 BAN の行で本文に「—」と出ていて、日付の分からない行も「—」でした。
  - **分からない時は入れない**: 番号が見つからない BAN（証拠のない古い版の BAN や、30 日たって最小限にした記録で全体の一覧にもないもの。この BAN 管理の履歴に証拠 ID があればそれを入れます）は、本文と件名に番号を入れず、画面にだけ「この BAN の番号（証拠 ID・共有 BAN の番号）が見つからないので、本文と件名には番号を入れていません」と出します。日付が分からない時は日付だけ外します（「BAN の記録（AEG-7F3K2）を確認しました」。番号も日付もない時は「BAN の記録を確認しました」）。どこから取った番号かも画面に出します（「情報が足りない」の時は、その返事には入れないことも）。フレンドコード・PUID・ハッシュ全体は返事に入れません（全体の一覧の番号も頭 8 桁だけ。フレンドコードのハッシュの行（前の版の行や手で書いた行）の番号は、だれの行か分かる時は使いません。だれの行か分からない一覧だけの行は、公開の定義ファイルにある番号なので使います）。
  - **受け入れる返事の共有 BAN**: 共有 BAN と分かっている時は「この BAN は共有 BAN（PocketRoles を使うすべての部屋に入れない BAN）でした。」と言い切ります（前は「共有 BAN だった場合は」の仮定の形でした）。
  - **検索**: 検索欄に証拠 ID を入れた時、全体の一覧だけにある人の行も、返事に入る番号と、この BAN 管理の履歴に残る証拠 ID で見つかるようにしました。返事の番号をそのまま貼っても（「共有 BAN 1a2b3c4d」「（AEG-7F3K2）」）見つかります。
  - **宛名**: 欄の名前を「宛名（問い合わせに書いてあった名前）」にし、名前を入れるとすぐ「〇〇 さま」になります。下の説明に「さま」（中文は「您好！」、English は「Hi」）が自動で付くことを書きました。「受け入れる」「受け入れない」では最初にゲームの記録の名前が入り、説明も「最初はゲームの記録の名前が入ります。問い合わせのメールやチケットの名前と違う時は直してください」になります。「情報が足りない」は本人か確かめる前の返事で、BAN された時の名前も聞くので、記録の名前は入れず、そう出します（名前を自分で入れた後は、種類を変えてもそのままです）。空の時、日本語の返事は宛名の行なしで「このたびは PocketRoles へお問い合わせいただき、ありがとうございます。」から始まります（中文は「您好！」、English は「Hi there,」のまま）。前の、名前のない時の決まった宛名の行はなくしました（記録の名前は最後に見てから 30 日で消えるので、名前のない返事が多くなるため）。記録の名前が NG ワードに当たる時は入れず、「記録の名前は NG ワードに当たるので入れていません」と出します。
  - `-SelfTest` は 332 項目に（証拠 ID から・ゲームが作る共有 BAN の記録の行は載せた時の証拠 ID と載せた日・全体の一覧だけの人は BAN の記録（kick・ログの確認が通らない記録は使わない、いちばん新しいもの）か行の番号・解除中や解除後の行の番号・最小限にした記録は番号なし（履歴に証拠 ID があればそれ）・捨てた操作は使わない、この PC の BAN が終わって全体の BAN が残る時は全体の BAN の番号・日付・ルール・期限だけ、ほかの記録から載せた時の証拠 ID をこの PC の BAN に使わない、フレンドコードのハッシュの行の番号を使わない、「情報が足りない」の件名に番号が入らない、共有 BAN を言い切る形、貼った番号での検索と履歴の証拠 ID、どの返事にも「—」や見本の番号・空のかっこ・ハッシュ全体・フレンドコードのハッシュが入らないこと、名前が空なら日本語はお礼から・入れた名前はすぐ「〇〇 さま」・NG ワードの名前は入れない、記録の名前は「受け入れる」「受け入れない」だけ・入れた名前は種類を変えてもそのまま、画面の宛名の説明・種類ごとの番号の行・言語の切り替え）。
- **ほかのホストの記録で共有 BAN にする条件**（9/22 の決定「確実な検知ならホスト 1 人で OK」「いいよ」。前は 2 人のホストが同じ人を報告した時だけで、「同じ人があらわるかわからない」）: BAN 管理で、取り込んだ記録（ログと合うもの）を全体の BAN に使えるのは、次のどれかの時にしました。どれも段階 1・30 日まで、ルールと理由・5 秒の確認・署名の手順・操作の記録の鎖は今までどおりです。
  - **確実な検知ならホスト 1 人で可**: `KillRole`・`VentRole`・`AbilityRole`・`TaskImpostor` の自動の記録で、同じ zip のログにもその検知の行が `(Certain)` で記録を書いた行より前にある時（定義ファイルでゆるめた段階・`/ac test`・記録の「確実」の文字だけは数えません。zip にホストの名前も必要）。記録もログも同じ zip の中身で、作られた記録は見分けられないので、画面では琥珀色にし、確認画面のルールは選んでおかず、そのホストから初めての zip か・そのホストの報告がもとの全体の BAN の件数（直近 30 日・180 日）・同じ人のほかの記録と名前が違う時の注意を出します。ホスト 1 人の記録で全体の BAN にできるのは記録 1 つにつき 1 回です（期限のあとに戻す・載せ直すには 2 人目の報告か作者の判断。今の全体の BAN が段階 2・3 の時の載せ直しも作者の判断）。
  - **確実な検知だけの共有の上限**（2026-09-23 のオーナーの決定。共有 BAN の一覧の 1 番「確実な検知だけの共有は 30 日に 2 件まで」に、オーナーが「いいよ」と答えました。`OWNER-DECISIONS.md` の D-39）: ホスト 1 人の確実な検知だけで全体の BAN にできるのは、1 人のホスト（その記録を最初に取り込んだ時の名前と、同じ PC の目印で結び付いた名前）につき直近 30 日に 2 件までにしました（`GateRules.CertainCap = 2`・`CertainCapDays = 30`、30 日の窓で数え、古いものが外れると自動で空きます）。3 件目からは、2 人目のホストの報告か、作者の判断（確認画面の「作者の判断」の欄に 10 文字以上）が要ります。信頼する管理人の報告には上限はありません（作者がはっきり信頼しているため。文にもそう書き、上限を越えた信頼する管理人に「上限です」とは出しません）。数えるのは `audit.log` の行と `journal.json` の操作で（`reporters.txt` では数えません。同じ操作は 1 件）、共有・期間を長くする・元に戻すで `gate=certain` の行に、新しい項目 `host=`（その記録を最初に取り込んだ時の名前。PC の目印は書かず、取り込んだ記録から取ります）を足し（信頼する管理人・作者の判断の行にも付きます。履歴に「取り込み元」と出ます）、`journal.json` の操作にも `gate` を書きます（`audit.log` に書けなかった行や、切り詰められた `audit.log` の分も数えるため）。`host=` のない前の行は、その証拠 ID の記録を最初に取り込んだ時の名前で数えます。解除した・元に戻したものも 30 日は数えます（載せて、解いて、また載せる、で枠が空かないように）。反映の前に元に戻したものも数えます（元に戻すは署名した操作として残るため。数えないのは次の「捨てる」だけです）。反映の前に「反映待ちを捨てる」にしたもの（鎖で確かめた `discard` の行・`journal.json` の「捨てた」）は数えません（どの部屋にも届いていないため）。`audit.log` の head のあとに足された行も数えます（件数を増やすだけなので）。`audit.log` の鎖が合わない時（書き換え・切り詰め・head がない）は、件数を確かめられないので、どのホストも上限と同じ扱いにします（2 人目のホストか作者の判断。信頼する管理人は上限なし。証拠の画面・確認画面・しるしにそう出ます）。作者の判断で進めた分（`gate=override`）は数えません。作者の「誤りの件数を 0 にする」は誤りの件数だけで、この件数は戻しません（0 件にする確認画面にも「確実な検知だけの共有の件数（直近 30 日に n/2 件）は戻りません」と出します）。証拠の画面・確認画面・「取り込み元のホスト」の画面に「このホストの確実な検知だけの共有: 直近 30 日に 2/2 件」（上限の時は 1 件空く日、この共有で何件目になるか）と、琥珀色で「このホストの確実な検知だけの共有のうち、あとで解除したもの: 直近 30 日に N 件」（あとで解除した・元に戻したもの。作られた zip でも同じ zip の記録とログは合うので、確実な検知の誤りは使用停止の件数に数えませんが、ここで見えるようにしました。**この件数を使用停止に数えるか、その分の枠を 30 日より長く持つかは、まだオーナーに聞いていません**。今はどちらもしません: 数えず、30 日で枠が空きます）、しるし「確実な検知だけの上限（2 件）」、管理人の提案の画面にも理由を出します。2 人以上のホストがそろった記録には件数を出しません（上限は関係ないため）。管理人の PC では共有しないので件数を数えず、「この件数は作者の PC で数えます」とだけ出し、確実な検知の提案にもそう添えます。確実な検知で進める確認画面では、そのホストから初めての zip の注意を琥珀色にしました。ホストは取り込む時に付けた名前と PC の目印（zip の中のファイルから作るので手がかりにすぎません）で見分けるので、別の名前で取り込んで目印のファイルも変えた zip は 0 件から数えること、件数はこの PC の記録で数えること（署名する PC が 2 台あれば別々）も「取り込み元のホスト」の画面と README に書きました。
  - **2 人以上のホスト**（今までどおり）、または **信頼する管理人** の報告（設定の「取り込み元のホスト…」で、作者がホストの名前ごとに付け外しします。その記録を最初に取り込んだ時の名前で決まり、信頼は報告 zip の扱いだけで、提案や権限は変わりません。取り込みの画面で信頼する管理人の名前を付けると注意が出ます）。
  - **作者の判断**: どれにも当てはまらない時は、確認画面の「作者の判断: ホスト 1 人の報告で十分だと考えた理由」に 10 文字以上書いた時だけ進めます（上の理由とは別の欄で、例を出します）。理由は `audit.log` の新しい項目 `override=`（共有 BAN にできた理由は `gate=`: certain / hosts / trusted / override）に、だれ・いつと一緒に残り、履歴にも出ます。ホストの名前がない zip と、取り込み元のホストの記録が読めない時は、作者の判断でも進められません。管理人の提案は作れ、提案の画面に「作者が理由を書いた時だけ」と出ます。
  - **誤りだった報告と使用停止**: 全体の BAN の解除・「元に戻す」・期間を短くする時の確認に「この BAN は誤りだった（検知や報告がまちがっていた）」（既定はオフ。その BAN のもとになった取り込んだ報告がある時だけ。取り込み元が 2 人以上なら名前ごとのチェックも）を足し、チェックすると、その BAN を載せた時の操作に記録した、実際に使った報告（`backers`）の取り込み元に「誤りだった報告」を付けます（同じ操作では取り込み元ごとに 1 件。この PC の記録で載せた BAN には付けません）。数えるのはホスト自身の判断による報告だけで、確実な検知（ログと合うもの）の記録の誤りは検知の誤りなので数えません（その記録だけで載せた BAN にはチェックの欄も出ません）。定義ファイルの `[unban]` は、行のコメントに `mistake` か `誤り` と書いた時だけ、その証拠 ID を数えます（反省などで早めに解いた申し立ては数えません。ファイルは読むだけ）。**直近 30 日に 3 件**（9/22 の決定「30日に5回で良くない？」の 5 件を、2026-09-23 のオーナーの決定で 3 件に。オーナーの言葉は「３番はでもごかいはやりすぎかな適切だと思う回数にして承認する」＝ 5 件は多いので、いいと思う数にして承認、です。3 件に決めてオーナーに伝え、そのままになりました（`OWNER-DECISIONS.md` の D-39）。`Reporters.Threshold = 3`。1〜2 件は、2 人のホストが一緒に報告したチャットの件のような正直なまちがいもありえますが、1 か月に 3 件はくり返しと見ます。確実な検知でログと合う記録の誤りは今までどおり数えません）で、その取り込み元（同じ PC の目印で結び付いた別の名前も）は使用停止になり、記録は全体の BAN にも 2 人目のホストにも使えません（信頼する管理人・確実な検知でも同じ）。**直近 30 日の件数が 3 件を下回ると、自動でまた使えます**（印は残さず、証拠の画面に戻る日の目安を出します。作者が「誤りの件数を 0 にする」で早めに戻すこともできます）。もとになった解除を捨てた・元に戻した時は数えません（その解除を元に戻す時の確認は、その解除がなかったものとして判断します）。使用停止になった時は、その取り込み元の報告がもとの、今有効な全体の BAN を一覧にします。2 件までは表示だけです。誤りだった報告の行（ホストの名前・PC の目印・日付・証拠 ID・どの解除か）と「0 件にする」の行は、読み込むたびに `reporters.txt` から 30 日で消し、信頼する管理人の行は信頼をやめた時に消します（プライバシーポリシーの案 3.5・第 5 条に合わせて）。`audit.log` の行は鎖のため残りますが、30 日より古いものは数えず、画面にも出しません。
  - **記録の置き場所**: 信頼・信頼をやめる・「誤りの件数を 0 にする」は、`audit.log`（書き換えの確認がある鎖）の行があるものだけを使います。`reporters.txt` は控えで、`audit.log` にない行は使わず件数を出します。`audit.log` の鎖が合わない時は書き換えより前の行だけ、どちらかのファイルが読めない時はホスト 1 人の記録を使いません。読み込みのスキャンは、操作の記録の確認を取り込んだ証拠の照合の前にしました。
  - **表示**: 証拠の画面に「このホストの扱い」「全体の BAN に使えるか」の行としるし（「ホスト 1 人で可（確実・zip のログと一致）」「信頼する管理人」「⚠ 使用停止中（誤りだった報告 N 件）」「ホストの名前なし」「作者の判断が必要」）、最初に取り込んだ時の名前・PC の目印がない zip・`[unban]` に載っている記録の注意。候補の一覧の取り込み元を、信頼は紫・使用停止中は赤に。「取り込み元のホスト」の画面（設定から。押すと設定を保存してから開きます）で、ホストごとの状態・誤りだった報告の証拠 ID・その報告がもとの今有効な全体の BAN を見て、信頼する・やめる・誤りの件数を 0 にする（作者モードだけ、本人確認と 5 秒の確認、`audit.log` と `reporters.txt` に 1 行ずつ。0 件にする時は、同じ PC の目印で一緒に 0 件になる名前も出します）。報告 zip の取り込みの画面と、共有 BAN にできない時の説明の文も新しい条件に。
  - `-SelfTest` は 439 項目に（9/23 の決定: 誤りだった報告 2 件は表示だけ・3 件で止まる・古いものが外れると自動で戻る、確実な検知だけの共有は 2 件まで・3 件目は止まる・2 人目のホストか作者の判断（理由が要る・`gate=override` は数えない）なら進める・信頼する管理人は上限なし・30 日の窓・同じ PC の目印の別の名前も数える・「0 件にする」では戻らない・`audit.log` の `host=` から数え `host=` のない前の行は取り込んだ記録から・捨てた共有は数えず解除した共有は数える・読み込みから上限と信頼する管理人まで。見直し: 数えるのは `gate=certain` の共有・期間・元に戻すだけ（`gate=hosts`・`trusted`・`override` は数えない）・元に戻すの行も数える・`journal.json` からも数え同じ操作は 1 件・捨てた操作は数えない・あとで解除したものを 3 言語で出す・上限を越えた信頼する管理人に「上限です」を出さない（3 言語）・2 人のホストの記録には件数を出さない・管理人の PC では「作者の PC で数えます」（3 言語）・`SetOverride` を通した作者の判断・鎖が合わない時は上限と同じ扱い（読み込みから切り詰めた `audit.log` まで）・head のあとに足された行は数える・0 件にする確認の文・解除した分の日数の文字。以下は前から: 確実な検知でホスト 1 人・ゆるめた段階や記録だけの「確実」・`/ac test`・時刻のずれ・名前のない zip では作者の判断でも使えない・記録 1 つで 1 回・段階 1 と 30 日・ルールを選んでおかない、チャットなどホスト 1 人は止まる・2 人で使える、信頼する管理人・最初に取り込んだ時の名前・やめた後、記録が読めない時、作者の判断は別の欄の 10 文字の理由が要り確認画面に出て `audit.log` に残る、直近 30 日に止まる件数（今は 3 件）・30 日を過ぎた分が外れると自動で戻る（印は残さない）・確実な検知の誤りは数えない・30 日より前の誤りは同じ記録のあとの誤りの邪魔をしない・`reporters.txt` の 30 日の削除・0 件にする・同じ解除は 1 件・同じ BAN の `[unban]` は 1 件・捨てた解除や元に戻した解除は数えない・解除を元に戻す時の判断・同じ PC の別の名前・同じ記録を別の名前で送っても数えない、もとになった報告は載せた時の記録だけ・今の載せ方だけ、`[unban]` のコメントの `mistake`、`audit.log` にない信頼は使わない・鎖が合わない時、名前ごとの「誤りだった」のチェック、報告 zip から読み込みまで）。
- **BAN 管理の本人確認の見直し（乗っ取りへの備え）**（オーナーの質問 2026-09-23「あとこれもし乗っ取られたりしたら Windows hello で認証されて管理渡らない？」）:
  - **正直なところ**: このロックが止められるのは「この PC の前に座れる人」までです。**「この PC であなたの Windows ユーザーとしてプログラムを動かせる人」（ウイルス・遠隔操作）には効きません。** `AegisBan.ps1` は署名のないただのテキストのファイルで、`AegisBan.cmd` は `-ExecutionPolicy Bypass` で動かすので、確認の行を消せば Windows Hello は一度も出ません。`audit.log` の鎖も鍵を使わない SHA-256 なので、同じ人には作り直せます。**みんなを守っているのはこのロックではなく「署名の鍵のパスワード」です**（署名していないものは、どのホストにも届きません）。この 2 つを `aegis\AegisBan.ps1` の頭と `Session` の説明にそのまま書きました。鍵の守り方は `scratchpad\console-lock\鍵の守り方.md`。
  - **ほかの人に届く操作は、そのつど鍵での確認**: 全体の BAN に載せる・全体の BAN の解除・全体の BAN の期間の変更・全体の操作を元に戻す・GitHub への反映・取り込み元のホストを信頼する／やめる／「誤りの件数を 0 にする」は、**この起動中に「署名の鍵で確認」をしていなければ実行しません**（`Session.KeyProven`。Windows Hello ＋保存した記録だけでは足りません）。ボタンを押すと先に確認の画面が出て、黒い画面で鍵のパスワードを入れます（1 回の起動につき 1 回だけ。全体の BAN はどのみち署名でパスワードを入れるので、手間はほとんど増えません）。見ることと、**この PC だけの BAN の操作（解除・期間・元に戻す・追加・取り込み・候補から外す・提案）は今までどおり Windows Hello だけ**でできます。署名・反映の各手順も、鍵の確認がなければ自分で止まります。
  - **Windows Hello だけで開ける日数を 30 日 → 7 日に**（`UnlockProof.TokenDays`。日数が何と引き換えかはコードのコメントに書きました）: 上の変更で、この記録が運ぶのは「見ること」と「この PC だけの操作」だけになったので、短くしても手間は増えません（鍵での確認は署名のたびにどのみち行います）。1 日まで縮めても、乗っ取った人は「窓の開いている日」を待つだけなので、週 1 回にしました。
  - **保存した鍵の記録を今すぐ無効にする**: 「本人確認」の画面に赤いボタンを足しました。**ロック中でも押せます。** 押すと `owner-proof.txt` を消し、`audit.log` に「保存した鍵の記録を無効にした」の行を残します。そのあとは Windows Hello だけでは作者モードで開けず、戻せるのは「署名の鍵で確認」（鍵のパスワードが要る）だけです。作者モードで開けてしまった人がこっそり取り消すことはできません（行を消すと鎖が合わなくなって、次に開いた時に赤く出ます）。無効にした日は読み込みの「本人確認」の行にも出ます。
  - **解錠の回数が見えるように**: 解錠は前から `audit.log` に残していましたが、次に開いた時の「前回からの変化」の先頭に **「前回からの解錠: 3 回（Windows Hello 2、署名の鍵 1）」** と出すようにしました（心当たりがなければ上の「無効にする」を押してください、という案内つき）。鍵の確認がなくて止めた操作も記録します。
  - **画面のロック・スリープ・ユーザーの切り替え・ログオフで自動ロック**（`Session.LocksOnSwitch`）。自動ロックまでの時間も 15 分 → **10 分**にしました（手順が動いている間は止まるので、署名や反映の邪魔はしません）。
  - **4 けたの数字は、鍵のある PC では出しません**: 答えが画面に出ている数字なので、本人確認にはなりません（鍵のある PC では「署名の鍵で確認」がいつでも使えるので、締め出されません）。鍵のない管理人キットの PC では今までどおりです。
  - **鍵があるかの判断を正直に**: `protected-keys.txt` があるだけで「この PC に鍵がある」としていたのをやめ、**その控えが指す鍵のファイルが今あるときだけ**にしました。これで「鍵を USB にだけ置く」が効きます（USB を挿していない間は管理人モードで開き、何も署名できません）。
  - `-SelfTest` は 461 項目に（鍵の確認がない作者モードでは署名・反映の手順が全部止まる・鍵の確認があれば通る、7 日の窓は定数から、画面のロックなどで閉じる条件、鍵のある PC では 4 けたを出さない、ほかの PC・ほかの Windows ユーザーにコピーした記録、時計を戻した時、無効にするとその前の記録が全部使えなくなること・そのあとの新しい確認だけが効くこと・無効にした行を消すと鎖が合わなくなること、解錠の回数の文（日本語・English）、`protected-keys.txt` が指す控えがない時）。
- **MOD のログに PUID・フレンドコードを出さない**: v0.5.5 の MOD は、ログにプレイヤーを名前と `puid-hash 1a2b3c4d`（PUID のハッシュの頭 8 桁）だけで書き、PUID・フレンドコードそのもの（とフレンドコードのハッシュの頭）は `Banlist.txt` などの名簿のファイルにだけ残します。報告 zip を作る時にログを伏せるのは、前の版のログのためにそのまま続けます。ホストが打った文字の中のフレンドコードは、後ろに句読点が付いていても（`name#1234,` など）伏せます。調査用の `[Diagnostics] WireLog`（既定オフ）をオンにした時だけは、通信の中身が 16 進数のままログに入るので、伏せられない識別情報が入ることがあります。
- **投票の言い当て通知**（9/21 に決めた v0.5.5 の予定。インポスターが見えるチートは、何も言わずに投票だけ合わせることもできるため）: 登録オフの部屋で、生きているクルーが**手がかりのない会議**（その試合のインポスターがまだキル・ベント・変身・透明化のどれもしておらず、死んだり抜けたりしたインポスターもいない会議。1 回目の会議や、何も起きないうちにボタンで開いた会議）で、まだ何もしていないインポスターに何試合も投票していたら、ホストの画面にだけ知らせます（ルール `VoteCallout`。退出はさせません）。
  - 票は投票の終わりに 1 回だけ、ホスト自身が持っている投票の記録から読みます（チートが送る偽の結果は使いません）。当たりに数えないインポスターは会議チャットの言い当て通知と同じです（ホスト、もう何かしたインポスター、この試合でほかの人が先に名前を出したインポスター、前の 2 試合のインポスターや 2 試合以上インポスターだった人、相方がその色に変身していた場合）。生きているクルーへの票は外れ、スキップは数えません。
  - 知らせるのは、当たりが 3 回以上で 2 試合以上にまたがり、外れがその 3 分の 1 以下の時。そのあとは当たりが 2 回増えるごとに。15 人・インポスター 3 人（1 人はホスト）・10 試合の見積もりで、でたらめに投票する普通の人が知らされるのは 0.02〜0.8 %、役職が見えるチーターは 43〜100 % でした。ボイスチャットで名前が出てから入れた票は見分けられません（チャットで先に名前が出ていれば数えません）。
  - ホストが生きているクルーの間はネタバレになるので、死んだ後か試合後に出します（その間は `/ac` の一覧にも出しません。トレイにはこのルールを出しません。トレイの検知ルールは 26 件に）。設定は今の「インポを言い当てた人を知らせる」（`[AntiCheat] Callout`）と同じスイッチです。定義ファイルで `votecallout.lobby`（組み込み 3、3〜10。部屋ごとにずらしません）と `level.VoteCallout = off` を変えられます。`/ac test vote <名前>` で表示を試せます。
- **3 人の部屋でインポスターが 0 人になる不具合を修正**（9/21 の v0.5.4 の実機確認で見つけた不具合）: 本体は 3 人の部屋ではインポスターを出さず、クルーの特殊役職の確率が高いと 3 人とも特殊役職になるので、登録オフの部屋で MOD がインポスターを足せる「役職のない人」が残らず、試合がすぐクルーの勝ちで終わっていました。登録オフの部屋で本体がまだインポスターを出していない時は、ほかに役職のない人が足りない場合だけ、その人の特殊役職を渡さずに止め、MOD のインポスター補充でインポスターにします（本体は役職をランダムな順番で配るので、最後に回ってきた人です。ホストの画面には今まで通り補充のお知らせが出ます）。指名で「クルー」にした人とクルー希望のホストは、ほかに誰も残っていない時だけ止めます。その試合では特殊役職が 1 つ少なくなります。登録ありの部屋、4 人以上、テストモード、2 人以下は変わりません。3〜6 人・指名・「次の自分」の組み合わせで 30 万回試して、インポスターのいない試合は 0 回でした。

- **隠した NG ワードが、同じファイルの人が読む行に書かれていないか確かめる**（9/23 のレビューで見つかった穴）: `tools\check-hashed-lists.ps1` は**エントリの行しか見ず、コメント行は素通し**でした。実際に見つかった漏れは、まさにコメント行の中の例でした（記号をはさんで書いてあり、読むときに記号は取り除かれるので、隠しエントリと完全に一致していました）。`tools\check-hidden-leak.ps1` を足して、コメント・説明文・例の 1 行ずつを正規化し、隠しエントリのすべての長さと突き合わせます。`check-hashed-lists.ps1` の最後から呼ぶので、push の前の確認にそのまま入ります。**出すのは行番号・長さ・取り除いた文字の種類と数だけで、ワードそのものは 1 文字も出しません。**当たった先が「英数字だけ、かつ語の切れ目の印つき」のエントリのときは数えません（そのエントリは記号を取り除いた形では照合しないので、チャットでは当たりません）。**止められないこと**: 空白だけをはさんで書いた形は、となり合うふつうの語がつながっただけの形と区別できないので数えません。見るのは `aegis\definitions.txt` の中だけです。

- **定義ファイルを version=3 に**（9/23 のレビュー）: 中身を変えたのに `version=` が 2 のままでした。MOD は同じ版なら今使っているものを使い続けるので、このまま署名して配っても、すでに v2 を持っているホストには新しい中身が届きません。`aegis\definitions.txt` の `version=` と `src\Net\AegisRules.cs` の `MinDefinitionsVersion` を、どちらも 3 にしました（`build-release.ps1` はこの 2 つが違うとリリースを止めます）。**この版で配るには、合流のあとに 1 回だけ署名が要ります**（今の `.sig` は中身と合っていません。`build-release.ps1` が署名を確かめて止めます）。

- **ハッシュの「長さの種類」の上限に近づいたら知らせる**（9/23 のレビュー）: 照合できる正規化後の長さの種類は 24 までで、`[ngwords]` と `[ngallow]` が**この枠を一緒に使います**。埋まったときに先に落ちるのは**あとから入れる `[ngallow]`（誤検知よけ）のほう**なので、ふつうの会話が止められる向きに壊れます。今までは 24 を**超えてから**しかログに出ませんでした。20 種類に届いた時点でも出すようにし、注意書きに「枠は 2 つの一覧で共用」「先に落ちるのは許す言い回し」と書きました（この版で 9 → 14 種類。残り 10）。

- **秘密の見張りを、コミットの前にも動かす**（サーバーの安全の見直しで「すぐやるべき手」の 2 番目。9/23「始めよう」）: 今までリリースのときだけ動いていた「公開してはいけないものが混ざっていないか」の確認を `tools\check-secrets.ps1` にまとめ、`.githooks\pre-commit` から**コミットの前にも**動くようにしました。合流のコミットは git が `pre-commit` ではなく `pre-merge-commit` を呼ぶので、そちらも置いてあります（これが無いと `git merge --no-ff` は見張りを 1 文字も出さずに通ります）。`git cherry-pick` と `git revert` は、git がコミットの前の仕掛けをそもそも呼ばないので、`.githooks\pre-push` が **push の前に、送るコミットを 1 つずつ**確かめます（`--no-verify` で飛ばしたコミットと、別の PC で作ったコミットも、ここで受け止めます）。入れ方はこの作業コピーで 1 回だけ `git config core.hooksPath .githooks`（やめるときは `git config --unset core.hooksPath`、1 回だけ飛ばすときは `git commit --no-verify` / `git push --no-verify`）。**`.githooks` の入っていない枝の作業コピーでは、git は仕掛けを見つけられず、何も言わずに素通りします。**ほかの作業コピーでも効かせたいときは、**この枝を先に取り込む**か、取り込むまでの間だけ `git config core.hooksPath` に **`.githooks` フォルダーの絶対パス**を指してください（相対の `.githooks` は、その作業コピーの根から探されます）。絶対パスで指した作業コピーには `tools\check-secrets.ps1` が無いので、仕掛けは**自分の隣にあるほう**を使い、見るリポジトリは `-Root` でその作業コピーを渡します。見る形は、今までの秘密鍵（XML・PEM・パスワード付きの `.prkey`）に加えて、**Webhook のアドレス**（Discord・Slack・そのほか）と **API キー**（GitHub・Google・Slack・AWS・npm・`sk-` で始まるもの・翻訳）、キーらしい値の代入、置いてはいけない名前のファイル（`.prkey`・`.pfx`・`.p12`・`.snk`・`private*.xml` など）の 18 種類です。見つけても画面に出すのは**ファイルの名前と形の呼び名だけ**で、中身は出しません。形の中に本物の値は 1 つも書いていません。`build-release.ps1` は同じスクリプトを呼ぶだけになりました。`-Test` を付けると、作り話の値 19 件と、当たってはいけないふつうの文 15 件で、見張りが正しく動くことだけを確かめられます（リポジトリのファイルは見ません）。`sk-` で始まる API キーの形は、前に「英数字でないこと」を足しました（`task-`・`risk-`・`disk-` のうしろに長い語つなぎが続くだけで当たり、ふつうの文書でコミットが止まっていました）。中身が文字でないと git が見なすファイル（`.png` など）も読むようにしました（今までは読まずに「見つかりませんでした」と言い切っていたので、名前を変えて置いた控えが素通りしていました）。**止められないこと**: 形の違う秘密は見つかりません。`--no-verify` で飛ばせます。一度 push した秘密は履歴から消せないので、そのときは鍵・トークンを作り直してください。

- **リリースのファイルに署名するようにしました**（2026-09-23）: `build-release.ps1` が `dist\SHA256SUMS.txt`（添付する zip の SHA-256 の一覧）に加えて `dist\SHA256SUMS.txt.sig`（オーナーの署名）を作ります。鍵は Aegis の定義ファイルに署名しているのと同じもので、新しく作るものはありません。ビルドの最後にパスワードを 1 回聞きます（`-NoSign` で省けますが、その `dist` は公開できません）。GitHub のリリースには 4 つとも添付します。GitHub が出す digest も `SHA256SUMS.txt` も zip と同じリリースの中にあるので、zip を差し替えられる相手には両方まとめて直せます。署名の鍵だけはこの PC から出ないので、Client は署名で確かめられます（Client 側の実装は `support\CLIENT-SUMS-VERIFY-SPEC.md`、リリースの手順は `support\RELEASE-SIGNING.md`）。`tools\sign-definitions.ps1` に `-SignRelease` と `-VerifyRelease` を追加しました（定義ファイルの署名とは、`version=` の行と `# format=` の行で互いに断るようにしてあります）。署名の前に、一覧に書いた zip をもう一度読み直して指紋が合うかを確かめるので、作り直したあとの古い一覧に署名することはできません。

- **チャット翻訳と自動通報を、はじめからオフに**（9/23 の決定）: 規約と公式サイトには「ホストがオンにするまで動かない」と書いてありましたが、実際のコードは両方ともオンで始まっていました。チャットを翻訳会社に送ることと、人を自動で通報することは、ホストが自分で選んでからにします。
  - `[Translate] Enabled` の初期値を `true` → **`false`**。オフの間、チャットはどこにも送られません。オンにするには `/opt translate.enabled on`、設定タブ「会話」の「チャット翻訳」、または設定ファイルの `Enabled = true`。止める時は `/opt translate off`。
  - `[AntiCheat] AutoReport` の初期値を `true` → **`false`**。オフの間、Among Us 公式へ自動で通報する人はいません。オンにするには `/opt anticheat.autoreport on` か設定タブ。オフのままでも `/aegis report <名前>` で 1 人ずつ通報できます。
  - **設定が読めない時もオフ**にしました（前は自動通報だけ、値が読めないとオンとして動いていました）。設定ファイルがない・壊れている・値が空や「yes」のように読めない時は、どちらもオフです。
  - **今の設定ファイルはそのまま**です。すでに `true` と書いてあるホストの部屋は、今までどおり動きます（BepInEx は既存のファイルの値を書き換えません）。オフにしたい時は、上のコマンドか設定ファイルで変えてください。
  - **更新した人には、1 回だけお知らせします**（9/23 の決定「前の版から残っているものは、ホストが触っていない時だけオフにする」に対する回答）: 前の版から更新しただけの人は、設定ファイルの値が優先されるのでチャット翻訳がオンのままです。ところが設定ファイルには、「ホストが自分で選んだ `true`」と「前の版の初期値のまま残った `true`」が **まったく同じ形** で書かれます（BepInEx が書く `# Default value:` の行も、前の版ではどちらも `true`）。版の名前も、変えた日付も、設定ファイルには残りません。見分けられない以上、勝手にオフにすると、翻訳を使っている部屋の参加者にだけ黙って影響が出ます。そこで **値は変えず**、更新後にはじめて部屋を作った時に、ホストの画面にだけ 1 回、何が変わったか・今この PC で何がオンか・どうすればオフにできるかを出します（3 言語）。
    - `/opt upgrade off` = 前の版から残っていたものだけをオフ（ホストが自分で変えた設定には触りません）、`/opt upgrade undo` = 元に戻す、`/opt upgrade keep` = このまま、`/opt upgrade`（`show`）= 今の状態。ホストだけが使えます。
    - `[AntiCheat] AutoReport` は v0.5.4 に無かった設定なので、v0.5.4 からの更新では最初からオフで入ります（お知らせにも出ません）。前の版で自分でオフにしていた人、この版でオンにした人にも出ません。
    - この確認は **1 回だけ**です。結果は `BepInEx\PocketRoles\opt-defaults.txt` に残り、次からは何もしません（**やり直せません**。1 回目の起動で設定ファイルの `# Default value:` の行が書き換わるので、記録を消してもやり直せません。あとからオフにする時は `/opt translate.enabled off` ／ `/opt anticheat.autoreport off`）。お知らせのあとにホストが設定を変えたら（チャット・設定タブ・設定ファイルの直接編集のどれでも）、その設定は「ホストが選んだもの」として記録し、`/opt upgrade off` では二度と触りません。
    - 前の `NgKickAt` の引き継ぎ（テスト版の `2` を新しい既定の `3` に直す）と同じやり方で、設定ファイルは最初の保存より前に読みます（保存すると `# Default value:` の行が今の版のものに書き換わるため）。
    - **9/23 の見直しで直したこと**（5 件）: ①お知らせを画面に出せなかった時に「出した」と記録してしまい、二度と出なくなっていた（記録は、本当に画面に出た時だけ動く `Chat.LocalWhenReady` の新しい引数でとるようにし、捨てる時のログは文章を作らないようにしました）。②「記録のファイルを消すと、もう一度確認できます」は事実ではなかった（1 回目の起動で設定ファイルの手がかりが消えるため。README 3 言語・CHANGELOG・記録のファイルの先頭から消しました）。③記録は、最初の `Bind` より前、設定ファイルを読んだその場で書くようにしました（`stage = scan`。起動の途中でゲームが落ちても、手がかりが失われません）。④記録は一時ファイルに書いてから置き換え、読む時も欠けていたら使いません（停電などで途中まで書かれた記録を「ホストが自分で選んだ値」と読みちがえていました）。⑤記録を残せなかった時は、`/opt upgrade off` が「`/opt upgrade undo` で戻せます」と言わず、`/opt upgrade keep` も「もう聞きません」と言いません（設定そのものはきちんと変わります）。
  - オフの時は、登録オフの部屋の挨拶と `/cmd about` から「翻訳あり」の文を外しました（していないことを書かないため）。説明は 3 言語（README・設定タブの説明・言語ファイル）に入れ直しました。
  - **途中でオンにした時も、部屋の全員にお知らせします**（9/23 の見直し）: 「翻訳あり」の案内は入室の挨拶にしか入らないので、すでに部屋にいる人には何も伝わっていませんでした。既定をオフにしたことで「あとからオンにする」が普通の流れになったため、オンにしたその場で同じ 1 行を流します（登録オフの部屋は全員向けの公開 1 行、登録ありの部屋は 1 人ずつ、その人の言葉で）。
  - **ホストの画面の「翻訳はオフです」は、1 回だけに**（9/23 の見直し）: 前は部屋を作るたびに出ていたので、翻訳を使わないホストには出続けていました。ゲームを起動してから 1 回だけにし、`[Translate] OffNotice = false`（`/opt translate.notice off`、設定タブ「オフの時のお知らせ」）で完全に消せるようにしました。翻訳をオンにして、また戻した時は、もう一度だけ出ます。
  - **チャットからオンにした時の返事に、1 行の注意を付けました**（9/23 の見直し）: `/opt anticheat.autoreport on` には「通報は取り消せません。なりすましで無実の人を通報してしまうこともありえます」、`/opt translate.enabled on` には「部屋のチャットの文章が Google（または DeepL）に送られます」。今までこの注意は設定タブの説明にしかなく、README が勧めるチャットの操作では一度も出ていませんでした。
  - ゲームなしのテスト `tools\OptDefaultsTests` を追加（初期値がオフか、値が読めない時にオフへ倒れるか、既存のファイルの値が残るか、上の 3 つの直しが入っているかを、本物の BepInEx の設定ライブラリとソース・言語ファイル・README で確かめます）。更新のお知らせについても、v0.5.4 の設定ファイル・ホストが自分で変えたファイル・まっさらなファイル・ファイルなし・壊れたファイル・2 回続けて起動した時・戻すコマンドを、本物の `Options.Init` を動かして確かめます（ゲームは起動しません）。

- **試合中の `/cmd n` は「最後に送った役職説明」だけを返す**（9/23 の設計レビュー §5.5、要望 9/23「直していいよ」）: 生きている人は試合中（会議の外）にチャットを打てませんが、改造したクライアントなら打てます。今までは打てば**その時の状態**で役職説明を作って返していたので、ジャッカルフレンズが「ジャッカルはまだ生きているか」を、マッドスタントマンが「耐えられるキルの残り」を、会議を待たずに知ることができました。
  - これからは、**生きている人が試合中（イントロ・追放の画面も含む）に送った `/cmd n`** には、その試合でその人に**最後に送った役職説明**をそのまま返します。役職説明を送る場面は、開始時・会議の始め（`[Chat] RoleInfoAtMeeting` がオンの部屋だけ。オフの部屋では最後まで開始時の文のままです）・崇拝で役職が変わった時です。控えは役職を配った時点で作るので、控えがないのは配る前の一瞬だけで、その時はその時の状態を読む行（生きているジャッカル、耐えられるキルの残り、崇拝の残り・崇拝した相手）を外した説明だけを返します。会議の説明をそのまま返す時は、その文にあった行（呪い中など）もそのまま入ります（本人がすでに受け取った文そのものです）。
  - 場面の判定は `MeetingHud` だけでなく `ExileController`・`IntroCutscene` も見ます（ほかの機能と同じ判定。追放の演出中に会議の画面が残っていても「試合中」として扱います）。
  - **返す中身は今まで通り**: 死んでいる人（幽霊はもともとチャットを読めます）、会議中（みんなが打てます）、ホスト自身の画面。ロビーと試合の終わりの画面は今まで通り「試合が始まってから使えます。」です。控えは試合ごと（試合の開始・終了で消します）。
  - **連投の制限**（新しい制限。どの場面でもかかります。ホストは対象外）: 同じ人への `/cmd n` の返事は 10 秒に 1 回までにしました（間は黙って捨てます。`/cmd id` と同じやり方）。返事は最大 5 通なので、2 秒ごとに打たれるとホストのチャットが埋まるためです。10 秒を数えるのは**実際に返事を出した時だけ**で、「MODはオフです」「この部屋は追加役職なし」「試合が始まってから使えます」のような決まった文では数えません。試合の開始・終了でも数え直します（ロビーで打った 1 回のせいで試合が始まった直後の 1 回目が消えないように）。
  - ほかの `/cmd` も全部調べました。その時の状態を返すのは `/cmd n` だけです（`/cmd l` は**前の試合**の結果、`/cmd r`・`/cmd s` は部屋の設定、`/cmd time` はロビー専用、`/cmd guess` は会議の投票中以外はプレイヤーを見る前に断る、`/cmd h`・`/cmd about`・`/cmd lang`・`/cmd id` は決まった文か本人の情報）。
  - 役職説明の各行には「その時の状態を読む行か／役職を配った時に決まる行か」の印を付け、外す処理は 1 か所にまとめました。ゲームなしのテストがこの外す処理そのものと、印の付け忘れ（行を直接つなぐ書き方が戻っていないか）を見ます。
- **地域の自動選択は「部屋を作る」画面でだけ**（9/23 の設計レビュー §8、要望 9/23「直していいよ」。既定はオフのまま）: 今まではオンラインメニューを開いた時に測って切り替えていたので、**部屋コードで参加するだけの人の地域まで変えてしまう**おそれがありました。これからは「部屋を作る」の画面を開いた時だけ測って切り替えます。参加（部屋コード・部屋さがし）では測定も切り替えもしません。切り替えた時は、開いている「部屋を作る」画面の地域の表示もその場で更新します。
  - 計測には数秒かかるので、**切り替えてよいかを計測が終わった時にもう一度確かめます**: 「部屋を作る」の画面がまだ開いていて、どこにも接続を始めていない時だけ切り替えます。画面を閉じて部屋コードで参加し始めた人の地域は変わりません（接続の途中は本体の「接続済み」の印がまだ立たないため、これがないとすり抜けます）。計測中にその画面で手で地域を選び直した時は、手で選んだほうを残します。
  - 切り替えられなかった時は覚えておいて、**次に「部屋を作る」の画面を開いた時に、計測をやり直さずその場で切り替えます**（`region.later` の文もそう直しました。10 分のキャッシュのせいで「次にホストする前に切り替わります」が最大 10 分間うそになっていました）。
  - 文章: 設定ファイルの説明・設定タブの説明・README 3 言語（計測に数秒かかること、すぐ「作成」を押すとその部屋には間に合わないこと）、`region.later` の 3 言語、新しい `region.manual`（手で選んだ地域はそのまま）。`/region` の文（`cmd.region.*`）は 9/23 の 1 回目の修正で直したままです。

- **中文の翻訳の直し**（[_Mutie0607](https://github.com/Mutie0607) さん、ありがとうございます）: 中国語を母語とする _Mutie0607 さんの PR #1（`lang/zh-CN.json` の 168 行）を取り込みました。MOD だけの役職名・言葉は PR の訳にしました（マッドメイト 狂信徒、崇拝者 传教士（崇拝する は 传教）、ジャッカルフレンズ 跟班、第三陣営 独立 / 独立阵营、マッド系 狂信徒系・狂信徒市长・狂信徒特技演员・狂信徒鹰眼、ライター 执灯人、オポチュニスト 投机主义者、スピードブースター 加速者（PR の中の 增速者 も 加速者 にそろえる）、廃村 废局、便利ホスト 简易房、斬撃・恋人の後追い・次の自分・票数の説明などの言い回し）。本体の言葉は公式の用語のまま（幻象师・钻通风口・驱逐・`/vset` の名前）、モデレーターは「版主」のまま（「主持」は公式でホストの見出しの言葉）、役職の説明の頭の陣営（伪装者。・独立阵营。・伪装者阵营的船员。）と子ども向けの言い方（自爆身亡・当场死亡）と実際の動き（スニッチから見た名前の色、魔女は次の会議で呪った全員）は残しました。PR が触っていない行（v0.5.5 で足した行を含む）・MOD 内の中国語・README 3 言語・設定の説明も同じ名前に。前の名前（伪装者狂粉・内鬼狂粉・狂粉市长・疯狂特技演员・鹰眼狂粉・崇拜者・豺狼之友・点灯人・增速者・投机者）とその頭（`疯`・`鹰`・`崇`・`豺狼之`）、ファントムの 幻术师 は、`/cmd r`・`/set`・`/assign`・`/opt <役職>.count`・`/cmd guess`（幻术师 は `/next` も）でそのまま通ります（`狂` 1 文字は今はマッドメイト）。今のホストの `BepInEx\PocketRoles\lang\zh-CN.json` で前の版の文章のままの行は、次の起動で新しい名前になります（`lang\defaults-history.tsv` generation 3。自分で書き換えた行はそのまま）。PR のファイルを自分で入れたホストも、取り込む時に直した PR の文章（幻术师・主持 など 56 行）が次の起動で今の文章になります（generation 5）。用語のチェッカーと `tools/LangTool` のテストも前の名前を探します。 <!-- terms-ok -->
- **スニッチの説明の直し**: タスクを全て終えたスニッチからは、インポスターの名前が赤く、ジャッカルの名前が青く見えます。役職の説明は 3 言語とも「インポスターとジャッカルの名前が赤く見えます」と書いていたので、「インポスターは赤、ジャッカルは青」に直しました。中国語の侍の斬撃の通知（`kill.slash`）も、巻き込まれた人は通知のあとに順に倒れるので「随后将接连倒下」に。前の版の文章のままの行は次の起動で新しくなります（`lang\defaults-history.tsv` generation 4）。
- **中国語をゲームの公式の翻訳の用語に**（中国のプレイヤーの指摘 9/22「Modの中国語見たけど、公式との少し違うんだ、例えばインポスターは内鬼じゃなくて伪装者」）: ゲームの Addressables から取り出した公式の中国語（簡体字）の用語集に合わせて、言語ファイル・MOD 内の中国語・README・Aegis とランチャーの中国語を直しました（内鬼 → 伪装者、通风管 → 通风口、跳管 → 钻通风口、放逐・投出 → 驱逐、好友代码 → 好友编号、房间码 → 房间代码、幻影 → 幻象师、噪音制造者 → 大嗓门、追踪者 → 侦察员、审判官 → 法官、生命体征 → 生命监测器、管道 → 通风口、快捷聊天 → 快速聊天、隐身 → 消失（幻象师の能力）、ありえない「举报」→ 尸体报告、ホストの意味の「主持」→ 房主、`/vset` の本体の設定名も公式の名前）。MOD だけの役職名・言葉は、上の「中文の翻訳の直し」で PR #1 の訳にしました。プレイヤーが打つ言葉は前の言い方も通ります（`/cmd guess`・`/next` の 内鬼・幻影・幻术师 など、投票点中の検知の「内鬼」「伪装者」、`/set` の 内鬼狂粉 など前の役職名）。日本語も本体と違った名前を直しました（サイエンティスト → 科学者、ヴァイパー → バイパー、守護天使の「守護」→ 護衛、`/vset` の「会議時間」→ 議論タイム）。入力では繁体字の公式名（偽裝者・魅影・警示者・追蹤者 など）も通ります。`/cmd r`・`/set`・`/assign` に本体の役職名（伪装者・インポスター・impostor など）や `伪装者` の頭（伪・伪装）を打つと、マッドメイト（伪装者狂粉）にはならずに「本体の役職です」と返します（前は頭の一致でマッドメイトになっていました）。ゲームマスター機能の中国語名は、公式ではホスト全般を指す「游戏主持」をやめて「GM（只观战）」に。用語のチェッカーは `tools\terms\check-terms.ps1`（公式の翻訳表の取り出しは `tools\terms\run.ps1`。ゲームの文章はリポジトリに入れません）。 <!-- terms-ok -->
- **中国語の「部屋に入れない」の言い方を、公式サイトと同じ「限制进入」にそろえました**（9/23 に公式サイト・規約・プライバシーポリシー・問い合わせの中国語を直した時、ゲームの中の中国語はそのままでした。中国のホストがサイトで「共享限制进入」を読んで設定画面を開いても、その言葉が無い状態でした）: 中国のプレイヤーが「Among Us か Steam のアカウントを止められた」と読んでしまう言い方をやめ、PocketRoles の部屋に入れないことは、プレイヤーにもホストにも **限制进入**（名簿は 共享限制进入名单・限制进入名单）と出します。直したのは、設定画面の項目名（「使用共享限制进入名单」「确定作弊长期限制进入(30天→180天→永久)」「版主可踢出/限制进入」）と「?」の説明、`/aegis bans`・`/aegis ban`・`/aegis unban`・`/ban`・`/perm`・`/ac`・`/h` の返事、入室をお断りするお知らせ（ホストの画面に出る行も「被限制进入」にそろえました）、申し立ての返事のひな形（チケットに貼って本人が読むもの）、README.zh-CN.md です（言語ファイル 40 行、MOD の中の中国語 37 か所、返事のひな形 29 か所、README.zh-CN.md 131 か所）。**サイトの言葉と設定画面の言葉が同じになったので、ホストはサイトで読んだものをそのまま設定画面で探せます。** Among Us のアカウント停止の話、本体の BAN ボタンとその通知、README の 3.4 の見出し（本体の話）はそのままです。作者と管理人（Aegis チーム）だけが使う「Aegis BAN 管理」の画面の中国語と、その画面の言葉を説明する README の行は、アプリと説明が食い違わないように次の作業でまとめて直します（残っている数は用語のチェッカーが毎回最後に出します）。前の版の文章のままの行は次の起動で新しい文章になります（`lang\defaults-history.tsv` generation 7）。`tools\terms\check-terms.ps1` と `tools/LangTool` のテストに「プレイヤーとホストには見せない言い方」として足しました（外す所は許可リストに理由付きで。README のようにプレイヤーやホストが読む所が混ざるファイルは、ファイルまるごとではなくアプリの画面を指す行だけを許可します。言い方そのものは、どのファイルにも平文では書かず `\uXXXX` で書きます）。
- **MOD の言語をゲームの言語に合わせる**（同じプレイヤーたちの報告 9/22: 中国語のゲームなのに設定タブが日本語、「言語がしっかり中国語になっていない」）: `[General] Language` の既定を `auto`（ゲームの言語: 中国語（簡体字・繁体字）→ 中文、日本語 → 日本語、それ以外 → English）に。これまでの設定ファイルはだれでも `Language = ja`（前の既定）なので、**1 回だけ**、設定ファイルが前の既定のまま `ja` でゲームが日本語以外なら `auto` に切り替え、次の部屋でホストの画面にだけ 1 行知らせます（「PocketRoles 的语言已跟随游戏设为简体中文。输入 /lang ja 可改回日语」。ほかの人には送りません）。最初の起動がメインメニューの前で終わっても、`BepInEx\PocketRoles\language-migration.pending` の印で次の起動に持ち越します。設定タブ・歯車パネル・`/opt lang`・`/lang default` に `auto`。ランチャーは新しいホストにインストールした時にランチャーの言語を `Language` に書きます（設定ファイルがある時は触りません）。設定タブの項目名・見出し・役職名は、言語ファイルに無い行も中国語で出し（中国語の表で見つからない文章は日本語ではなく英語に）。
- **言語ファイルの文章の更新**: これまでは足りないキーを足すだけだったので、直した文章が今のホストの `BepInEx\PocketRoles\lang\*.json` に届きませんでした。前の版が内蔵していた文章と一字一句同じ行だけを新しい文章に置き換えます（自分で書き換えた文章には触れません。前の版の文章に自分で戻していた行は、そのファイルで初めて反映する時に 1 回だけ新しくなります。ランチャーや zip での入れ直しでは、これまでどおり lang の json ごと上書きされます）。新しい文章と足りないキーは一時ファイルから 1 回で書き込み、書けた時だけ反映済みにします（中身が空のファイルは反映済みにしません）。前の版の文章は git の履歴から作った `lang\defaults-history.tsv`（文章のハッシュ）で見分け、どこまで反映したかは `lang\defaults-applied.txt` に書きます（新しい版ごとに 1 回）。作り直しとテストは `dotnet run --project tools/LangTool -- history` / `-- test`（ゲームなしのテスト 200 項目: 言語の選び方・1 回だけの切り替えと持ち越しの印・役職名の読み取り（伪装者 がマッドメイトにならない）・文章の更新・v0.5.4 と PR #1 の中国語ファイルの更新・表とソースの突き合わせ）。README 3 言語。

## v0.5.4 — 2026-09-21

- **Aegis のトレイアプリ**（要望 9/21「windowsの隠しインジケーターにそういうアイコンとかあったらいいのに」「ランチャーを起動するときにそのアンチチートがいかにもスキャンしてるみたいな感じできる？」）: ランチャーを開くと Aegis のスキャン画面（回る輪と走査線、1 行ずつ確認）が出て、終わると Windows の通知領域（隠れているインジケーター）に常駐します。MOD 本体とは別のアプリ（`aegis\Aegis.ps1`。.exe なし）で、ランチャーを閉じると一緒に終わります。
  - 確認する 12 項目: 検知ルール、ゲームの版、BepInEx、MOD 本体の指紋（ランチャーが入れた・ビルドした DLL と同じか）、ほかのプラグイン、ゲームフォルダの注入 DLL（version.dll など）、Aegis の設定、BAN リスト、セキュアブート、TPM 2.0、テスト署名・デバッグモード（とメモリ整合性）、起動中のチートツール。
  - 常駐中: 待機中は青緑の盾、ゲーム中は緑（監視中）。MOD の Aegis が退出させた・検知した時に画面の右下へ Aegis の通知（ゲーム中で Windows の通知が止まっていても出て、入力は奪いません）。言い当て通知はネタバレになるので出しません。記録は `%LOCALAPPDATA%\PocketRoles\Aegis\events.log`。アイコンの右クリックで状態の窓・記録・スキャンのやり直し。
  - **起動前の検査**（要望 9/21「もし一個でも無効になってたら起動させない？」）: ランチャーの「起動」の直前にスキャンし直し、チートにつながる異常（見知らぬプラグイン・注入 DLL・起動中のチートツール・テスト署名／デバッグモード・MOD 本体の改ざん）があれば起動を止めます。赤い項目ごとに原因と直し方を表示します（スキャン画面の「直し方」とランチャーのメッセージ。要望 9/21「それぞれ別の要因を教えたら」）。セキュアブートと TPM はチートと直接は関係しないので注意だけで、起動は止めません。Aegis 自身のエラーでも起動は止めません。
  - **定義ファイル**（要望 9/21「アンチチートは常にアップデートしていきたい 抜け穴をやられないために」）: チートツール名と注入 DLL 名の一覧 `aegis\definitions.txt`。ウイルス対策ソフトの定義ファイルと同じで、ランチャーを開くたびに GitHub（main の `aegis/definitions.txt`）から最新を取り、次のスキャンから使います（この PC の情報は送りません）。ゲーム・Steam・VALORANT/Riot・Discord など常用のプログラムや短すぎる名前に当たる行は無視します。起動中のチートツールは 30 秒ごとに見直し、Among Us 以外のゲームのツール（Cheat Engine など）も見つけます（プロセス名を見るだけで、ほかのアプリには触れません）。
  - 署名や常駐ドライバは使いません（要望 9/21「署名とか全部PC常駐はなくてただ強力にしてほしい」）: 見るのはこの PC のファイルとプロセスの一覧だけです。
- **ゲーム内の Aegis を強化**（要望 9/21「Vanguardみたいに強力に」）: 登録オフの部屋で新たに見つけるもの（試合は止まらず、その人だけが対象です）:
  - チャットの連投（3 秒に 5 通。入力もクイックチャットも）→ 2 回で退出。
  - スピードハック（設定の速さの 2.5 倍を超える移動が 2 秒続く）→ 2 回で退出。1.8 倍はホストに知らせるだけ。ベント・はしご・昇降機・ジップライン・瞬間移動の直後は数えません。
  - 名前の付け直し（部屋の中での 2 回目の名前確認・試合中の名前確認）と試合中の色変更: 届いた要求はその場で捨ててホストに知らせます（他人になりすまして送れるので退出はさせません）。ロビーで 3 秒に 20 回以上の色変更も知らせるだけ。
  - ベントから遠すぎる位置でのベント（上限は部屋の速度に合わせて 1.5 + 速度 × 0.8）→ 知らせるだけ。
  - 登録オフの部屋に、ホストしか送らない通信（役職・名前・色の設定など）が届いた → 送り主が分からないので「偽装された通信」として知らせるだけ。
  - 2 回で退出するルールは、5 分以内の 2 回だけを数えるように（10 秒以上あけた 2 回）。
  - `/ac test chatflood|name|color|speed|ventfar <#番号|名前> [kick]` で表示を試せます。
  - 9/21 に実機（PC＋エミュレーター＋スマホ 2 台、登録オフ 4 人）で、PING が 100〜500 ms と大きく揺れる状態で 7 分遊び、誤検知 0 件を確認。
- **高 PING の部屋を自動で作り直す（既定でオン）**（要望 9/21「ラグすぎるこれも改善して」「pingの改善も頼んだ」）: 公式のアジア地域でも、部屋を作るたびに割り当てられるサーバーが変わり、近いサーバー（PING 8〜10 ms）と遠いサーバー（100 ms 以上。9/21 は 112〜179 ms、スマホでは 500 ms 超え）があります。PC の回線（8 ms）や MOD の通信量（試合全体で小さなデータ 61 回）ではなく、サーバーの距離が原因でした。部屋を作った直後、まだ誰もいない間に PING が 5 秒続けて上限（既定 80 ms）を超えたら「作り直しますか？」と聞き、**15 秒答えがなければ自動で作り直します**（続けて最大 3 回）。今までは既定でオフで、答えるまで待っていました。設定は `[Lobby] HostPingLimit`（旧 `MaxHostPing`。0 より大きい値にしていた人はその値を引き継ぎ、古い行は消えます）、`/opt maxping <ms>`、0 でオフ。9/21 に確認: 179 ms の部屋 → 15 秒後に自動で作り直し → 8 ms の部屋（エミュレーターも 141 ms → 10 ms）。

## v0.5.3 — 2026-09-21

- **Aegis（イージス）アンチチート**（ホスト側のチート検知。名前は 9/21 に決定）（要望 9/21「アンチチート作ってみない？」「チートは早く部屋から消えてほしい」）: 参加者の端末には何も入れられないので、ホストに届く通信から「普通の Among Us では絶対に起きない操作」を見つけます。登録オフ（便利ホスト）の部屋の試合中に動きます。
  - **1 回で退出**（この部屋へのバン付き、`[AntiCheat] AutoKick`、既定オン）: キルできない役職のキル、インポスターがインポスターをキル、ベントを使えない役職のベント、持っていない能力（変身・透明化）の使用、インポスターのタスク完了。
  - **2 回で退出**: 生きている人の会議の外でのチャット（9/15 の公開部屋で「生存中ってチャット打てないよね」と言われていたもの。会議・追放画面・イントロの直後 8 秒は数えず、3 秒後に生死を確かめ直します）。
  - **ホストの画面に知らせるだけ**（ラグやなりすましの可能性があるもの）: 死んでいる人のキル、会議中や追放画面でのキル、クルーのサボタージュ、クールダウンより明らかに早いキル、遠すぎる位置からのキル、ありえない速さのタスク完了（一瞬で全タスク）、生きている人の死体の通報や死んだ後の通報、試合中の瞬間移動、生きている人の守護、持っていないタスクの完了、普通の Among Us にない通信（改造版やチートツールの可能性）。
  - 退出させた時は全員のチャットに「[Aegis] 〇〇 はありえない操作(…)をしたので退出になりました」と 1 行（`[AntiCheat] AnnounceKick`）。VIP・モデレーター・アドミンは自動では退出させません（ホストに知らせるだけ）。
  - 役職は試合開始時にホストが配った役職の控えと、いまの役職の両方が「できない」と言う時だけ退出させます（試合中に届く役職変更の通信はチートで偽装できるので、片方だけでは退出させずホストに知らせるだけ）。2 回で退出のチャットは同じ試合の中の 2 回だけを数えます。ホストが死亡中は、全員へのお知らせを試合後のロビーで出します（生存者には死んだ人のチャットが見えないため）。通信の読み取りは本体の処理に影響しないよう、読み取った位置を必ず戻します。
  - `/ac`（`/aegis` でも。ホストのみ）: 記録の一覧、`/ac clear`、`/ac on|off`、`/ac kick on|off`、`/ac test <kill|vent|ability|task|chat|sabotage|killcd|protect|distance|rpc|taskburst|report|teleport|killphase|callout> <#番号|名前> [kick]`（チートツールなしで判定と通知を試す。`kick` を付けた時だけ本当に退出させる）。設定タブ「全般」に「チート検知(登録オフ)」「チートの人を自動で退出」「退出させたことを全員に知らせる」、`/opt anticheat|anticheat.kick|anticheat.announce`（旧 `/opt kick` は自動退出の切り替えになりました）。
  - 通信には送り主が入っていないので、なりすましで別の人を犯人に見せかけることは理論上できます。見つけられないもの: 視界や壁抜けで見えるチート（相手の端末の中だけで起きるため）。誰がインポスターか見えるチートも通信では見つけられないので、次の「言い当て通知」で発言から手がかりを出します。
- **言い当て通知**（要望 9/21「コマンドでいんぽを見られた」→「1 が欲しい」）: インポスターが見えるチートは、本体が全員に送っている役職の情報を画面に出すだけで何も送信しないため、通信からは見つけられません。唯一の手がかりは本人の発言なので、登録オフの部屋の会議で、生きているクルーが「まだ誰も知りえないインポスター」を言い当てたらホストの画面にだけ知らせます（退出はさせません。上手な推理や勘との区別はつかないため）。
  - 数えるのは「まだ誰も知りえないインポスター」だけ: 生きている、ホストではない（ホストは希望でいつもインポスターになれるため）、この試合でキル・ベント・変身・透明化をまだ見せていない、ほかの人（ホストやインポスター本人も含む）がまだ名前を出していない、この部屋の直前 2 試合でインポスターではなく 2 試合以上インポスターだったこともない（試合後の結果一覧で全員に見えていて、指名で毎回インポスターになる人もいるため）。変身で化けた色の人の名前は外れに数えません。
  - 知らせる時: その試合で外れ 0 のまま 2 人以上言い当てた時、またはこの部屋の 2 試合以上で合わせて 3 人以上言い当てた時（外れが言い当ての 3 分の 1 以下。その後はさらに 2 人増えるごとに）。ホストがいつもインポスターの部屋では 1 試合に数えられるのはたいてい 1 人なので、何試合も続けて当てる人を拾う作りです。
  - 色（赤・あか・レッド・red・红、コーラル・コラール・こーらる・ロズ・マルン・バナ など日本語・英語・中国語の言い方）と名前全体を読みます。「インポ」「怪しい」「sus」「内鬼」などの言葉か、「とりま青」「コラールだ」「赤 青」のように色だけの発言、「青じゃない？」のような問いかけを名指しとみなします（「、」「。」で区切った文ごとに判断するので「赤は怪しくない、青インポ」は青だけ）。「怪しくない」「インポじゃない」「red isn't sus」「緑白」「青は白」は外し、「いったん」「投票しろ」「できる」「エンジン」などの中の文字は色や言葉として読みません（テスト 162 通り）。
  - ホストが生きているクルーの間は、インポスターの名前が出てネタバレになるので、ホストが死んだ時か、試合後にロビーに戻ってから出します（その間は `/ac` の一覧にも出しません）。設定タブ「全般」の「インポを言い当てた人を知らせる」、`/opt anticheat.callout`、`[AntiCheat] Callout`（既定オン）。`/ac test callout <#番号|名前>` で表示を試せます。クイックチャット（「〇〇 が怪しい」など）も読みます。
  - 9/21 の公開部屋で黒の人が「インポ赤　青　コーラル」と言った件は、ログを見直すと、青はホスト（毎回インポスター）、コーラルは前の試合もインポスター、赤は実はクルー（ローズが赤っぽいマルーンに変身してキルしたのを見た可能性）で、チートではなく推理と見るのが妥当でした。この通知はその発言では出ません（数えられるインポスター 0 人・外れ 1）。
- **挨拶と `/cmd about` でお知らせ**: 登録オフの部屋の入室時の挨拶を「ようこそ! 普通のAmong Us(役職なし)です。何も入れなくてOK。Aegisアンチチートを導入しています。翻訳あり。説明 /cmd about、困ったら /cmd h」に（要望 9/21「Aegisアンチチートを導入していますみたいにしない？」）。`/cmd about` の MOD の説明に「Aegisアンチチート(ありえない操作をした人はすぐ退出)」と翻訳の説明を追加。登録ありの部屋の挨拶と説明にも「Aegisアンチチートを導入しています」。退出のお知らせ・ホストへの通知・`/ac` の一覧・設定タブにも Aegis の名前。
- **登録オフの部屋でも日本語→外国語の翻訳**（要望 9/21「日本語から外国語にかえれないの？」→「外国人がいるときだけ全部に使用」）: 登録オフの部屋は参加者に個別のメッセージを送れないため、これまで翻訳は外国語→日本語の片方向でした。外国語の人（`/lang` か自動判定で外国語になった人）が部屋にいる間だけ、ほかの人の発言（ホストの発言も）をその人の言葉に訳して **全員に** 流します（言語ごとに 1 行。外国語の人がいない時はチャットは増えません。幽霊のチャットは流しません）。`[Translate] ForeignInCompat`、設定タブ「登録オフでも外国語へ翻訳」、`/opt translate.compat`（既定オン）。`/lang` の返事・自動判定の案内・`/cmd h` の説明も「ほかの人の発言もあなたの言葉に訳して流します」に。
- **翻訳の記号**: 翻訳サービスが返すダッシュ（—）などゲームのチャットが受け付けない記号を消した時に前後の単語がくっついていた（「next match—everyone」が「matcheveryone」）のを、使える記号（- や '）に置き換え、置き換えられない記号は空白にするように。

## v0.5.2 — 2026-09-17

- **次の試合の役を他の人にも指定 `/next <名前|#番号> impostor | crew | auto`**（要望 9/15「いんぽを指定するのは？人に」）: ロビーのチャットで `/next 太郎 impostor`（`/next impostor 太郎` の順でも、`/next #3 crew` のように番号でも可）と打つと、次の 1 試合だけその人がインポスター／クルーになります。`/next 太郎 auto` でその人だけ解除、`/next reset` で自分の分も含めて全部解除、`/next` だけで現在の指定。設定タブ「ホスト」に「次のインポ: なし／名前」ボタン（参加者を順に 1 人だけ指名。複数人やクルー指定はチャットから）。仕組みは v0.5.1 の `/next`（自分の役）と同じで、本体の役職選択の中で役職メッセージの宛先を入れ替えるだけ: 参加者には普通の配役に見え、インポスターの人数は変わらず、登録オフ（便利ホスト）の部屋でも登録ありの部屋でも効きます。指名はインポスター枠の人数まで（自分の `/next impostor` も 1 人分）。多く指名した時は先に指名した人が優先で、余った人は今回見送りの通知。指名した人が部屋を出ると自動で解除。本体が指名した人を選ばなかった 3 人部屋などでは、v0.5.1 のインポスター補充がその人を優先します。自分の指定を入れずに人を指名した場合、本体が自分をインポスターに選ぶとその人に譲って自分はクルーになります（自分もなりたい時は `/next impostor` も一緒に）。登録ありの部屋では本体側のチームだけを決め、MOD の役職（ヴァンパイアやシェリフなど）はその上から今までどおり抽選されます。結果はホストの画面にだけ「今回: 太郎 = インポスター（花子 と入れ替え）」と出て、参加者には何も送りません。左上の表示に「次:インポ=太郎」。 本体がインポスター役を出し切らない小さい部屋（3 人など: 本体はインポスターを 1 人も出さず全員にクルーの特殊役を配る）では、指名した人（`/next impostor` の自分も）に来たクルーの特殊役を送らずに保留し、v0.5.1 の補充がその人を最初に昇格させます（9/17 に PC＋スマホ＋エミュレータの 3 人で確認）。
- **翻訳: 中国語と判定する字を 67 字追加**: 9/15 の公開部屋で中国語の「最终导航室」が訳されませんでした（かながなく、判定表に 终・导 がなかったので日本語の漢字だけの行と同じ扱い）。日本語に同じ字がない簡体字（终 导 术 图 层 领 岛 极 动 达 观 视 单 卫 战 阳 阴 铁 铜 锁 闭 闪 阵 陆 险 验 顶 顺 须 项 预 颜 飞 鸡 龙 齐 齿 龄 轮 转 轻 软 较 载 运 连 进 远 违 适 选 递 逻 遗 变 疗 讯 仓 怀 谎 监 摄 频 网 举 备 樱）を追加。
- **登録オフの部屋の翻訳は片方向だと案内**: 登録オフ（便利ホスト）の部屋では参加者個別のメッセージが送れないため、外国語の人の発言は日本語に訳して全員に流れますが、日本語のチャットをその人向けに訳すことはできません。`/lang` で外国語を選んだ時の返事、自動判定の案内、`/cmd h` の翻訳の行に「あなたの発言は日本語に訳されて全員に流れます。日本語のチャットはあなた向けには訳されません。」を追加（`translate.compat.oneway`、`help.translate.compat`）。
- **`/cmd about`（MOD の説明）**（要望 9/14「modの説明も欲しい」）: 誰でも打てます。登録オフの部屋では「この部屋(役職なし)でMODがしていること: 入室時の挨拶と案内, 外国語チャットの自動翻訳, 試合後の結果一覧(途中で抜けた人も含む), 部屋の時間切れ防止」「ゲームの中身は普通のAmong Usで, 役職や特殊ルールはありません」の 2 行、登録ありの部屋では役職 MOD の 1 行案内。`/cmd h` の 1 行目にも `/cmd about` を追加。
- **死亡・追放時の役職表示をホストの画面だけに**（要望 9/14「ホストだけ分かればいい」）: `[Roles] RevealRoleToAll`（`/opt revealall off`、設定タブ「役職表示を全員に」、既定オン＝従来どおり全員向け）をオフにすると、「〇〇 は ジャッジ でした」「追放された 〇〇 は …」がホストの画面だけに出て、参加者には何も送りません。表示自体をやめるのは従来どおり `/opt reveal off`。設定タブに「死亡・追放時に役職を表示」の行も追加。
- 登録オフ用の設定「互換モード: シェリフ/ジャッカル許可」（`[Compat] AllowRiskyRoles`、`/opt compat.risky`）を削除。v0.4.4 で登録オフの部屋の MOD 役職を全部やめた（参加者個別のメッセージがサーバーに拒否されるため）あとも設定だけ残っていて、オンにしても何も起きませんでした。登録オフの部屋で配れるのは本体の役職だけです。
- **試合中に抜けた人の役職をホストの画面に表示**（要望 9/14）: 誰かが試合中に抜けた瞬間、ホストの画面だけに「抜けた 〇〇 は クルー でした。」と出ます（`[Roles] RevealRoleOnLeave`、既定オン。登録オフでも可、他の人には見えません）。全員に見せるときは `[Roles] RevealLeaveToAll`（`/opt revealleaveall on`、会議中に抜けた分は追放画面のあとに送信）。
- **守護天使の守れる回数**（要望 9/14「守護天使が無限につけられてインポスターが不利」）: 登録ありの部屋では `[Vanilla] GuardianAngelUses`（`/opt gauses 1`、設定タブ「守護天使の回数」、0 = 本体どおり無制限）で守護天使 1 人あたりの守れる回数を制限します（回数を超えた守護はホストが応答せず、何も起きません）。登録オフの部屋は守護がホストを通らないので制限できません。代わりに `/vset gacd 600` で守護のクールダウンを 10 分にすれば実質 1 回になります。
- **ホストのシールド（登録あり・合言葉つき）**: `[Host] ShieldKills` 回だけホストへのキルが失敗します（相手のクールダウンが戻るだけで、マッドスタントマンと同じ挙動）。`[Host] ShieldKey` に合言葉を入れた時だけ有効で（`/opt host.shieldkey <合言葉>` → `/opt host.shield 1`、設定タブに行が出るのは解除後の次回起動から）、MOD 本体には合言葉のハッシュしか入っていません。登録オフの部屋ではキルがホストを通らないので効きません。

## v0.5.1 — 2026-09-14

- **インポスターの人数を設定どおりに補充**: 本体（2026.8.18）の役職選択は、インポスター役職（シフター・ファントム・ヴァイパー）を 100% にしていると素のインポスターの補充を食いつぶし、設定より少ないインポスターで試合を始めることがあります（9/14 の公開部屋: 14 人・3 人設定・シフター 1 が 100%・ファントム 1 が 100% → インポスター 2 人だけ、3 人目の枠に選ばれた人はクルーに）。本体がインポスターの枠に選んだのにクルーを送ってしまった人を、その場でインポスターに戻します（本体が選んだ人をそのまま復元。参加者には最初からインポスターとして届きます）。それでも本体の上限（7 人未満 1 人、9 人未満 2 人、それ以上は設定値）で決まる人数に足りない時は、素のクルーから補充します。9/14 に登録オフ・登録ありの両方で実機確認。登録オフ（便利ホスト）の部屋でも登録ありの部屋でも効き、昇格があった時はホストのチャットに「インポスターが 3 人中 2 人しかいなかったので、〇〇 をインポスターにしました。」と出ます（`assign.impostors.filled`）。v0.4.1 からの「インポスター 0 人なら 1 人昇格」の保険はこの処理に統合しました。
- **次の試合の自分の役 `/next`**（要望 9/14）: `/next impostor` / `/next crew` / `/next auto` / `/next <本体の役職名>`（例 `/next shapeshifter`）で次の 1 試合だけ自分の役を決められます。設定タブ「ホスト」に「次の自分: おまかせ／インポスター／クルー」ボタン。テストモード不要で、登録オフ（便利ホスト）の部屋でも効きます。仕組み: 本体の役職選択の中で、希望の役が他の人に出たときに役職メッセージの宛先を自分と入れ替え、その人には自分がもらうはずだった役を渡す（参加者には普通の配役に見え、インポスターの人数も変わらない）。本体の役職を指定した時はその役の人数と確率をその選択の間だけ 1 / 100% に上げます。左上の表示に「次:自分=…」が出て、試合が始まると使い切り、部屋を出ると解除。ゲームマスターモード中は不可。
- **登録オフでもタスクの配布数を拡張**（要望 9/14）: 登録オフの部屋で `/vset common|short|long` に本体の範囲を超える値を指定すると、サーバーに同期される設定値は本体の範囲のまま、実際に配る個数だけがその数になります（`ShipStatus.Begin` の間だけホストの設定を差し替えて戻す）。`[Compat] CommonTasks / ShortTasks / LongTasks`（0 = 設定どおり。設定タブの「配るコモン数(登録オフ)」など、`/opt compat.tasks.common` 等）。`/vset show` に「配る個数（登録オフ）」の行。
- ランチャー: 起動中のタスクバーのアイコンが PowerShell のものになっていたのを、ランチャー独自の ID（AppUserModelID）を付けて PocketRoles のアイコンが出るように。

## v0.5.0 — 2026-09-13

- 役職を 9 つ追加（SuperNewRoles 準拠の「薄い陣営」向け役職）: マッドメイヤー・マッドスタントマン・マッドホーク・崇拝者（インポスター陣営・クルー枠）、ジャッカルフレンズ（第三陣営・クルー枠）、イビルホーク・イビル猫又・シリアルキラー・侍（インポスター枠）。すべて既定では人数 0
- マッドメイヤー: 会議の投票が複数票分（投票アイコンも票数分表示され、他の人からはメイヤーに見える）。`[MadMayor] Votes / KnownToImpostors`（`/opt madmayor.votes|known`）、短縮形 `mmy`
- マッドスタントマン: キルされても設定回数までは死なず、キルした側はクールダウンだけが戻る（追放・アサシンの推理は防げない）。インポスター側のキラーには役職名と残り回数、シェリフ・ジャッカルには「耐えました」とだけ通知。`[MadStuntman] Lives / NotifyStuntman`（本人への通知は既定オフ）、短縮形 `stunt` `madstunt` `mst` `ms`
- マッドホーク: 視界が広いマッドメイト（停電中も倍率がかかる）。`[MadHawk] VisionMultiplier / SpeedMultiplier`（`/opt madhawk.vision|speed`）、短縮形 `mh`
- 崇拝者（クルー枠・本人のクライアントはインポスター）: キルボタンで相手を崇拝してマッドメイトにする（`[Worshipper] Uses / Cooldown`）。インポスター側のキラー（インポスター側のラバーズ含む）を崇拝すると自爆、回数を使い切った後はキル失敗の演出のみ。崇拝で人数が減るため、その場でインポスター勝利（相手が最後の残りタスクを持っていた場合はクルー勝利）になることがある。`KnownToImpostors` オンのインポスターには Ⓦ（アサシンは `worshipper` で当てる）。短縮形 `ws`
- ジャッカルフレンズ（第三陣営・クルー枠）: ジャッカルの名前が青く見え、ジャッカルが勝つと勝つ（キル・ベント・サボ不可、タスクは偽物）。ジャッカルが配られた試合だけ配られる。`[JackalFriends] KnownToJackal / SheriffCanKill`（`/opt jackalfriends.*`）、短縮形 `jf`
- イビルホーク（インポスター枠）: 視界が通常のインポスターより広い（インポスターの視界 × `[EvilHawk] VisionMultiplier`、既定 2、常時）。`/opt evilhawk.vision`、短縮形 `eh`。アサシンが当てるときは `impostor` ではなく `evilhawk` / `eh`
- イビル猫又（インポスター枠）: 会議で追放されると自分に投票した人から 1 人を道連れにする（追放画面の約 2.5 秒後に死亡。追放で試合が終わる時は道連れなし）。`[EvilNekomata] VotersOnly / ExcludeImpostors / Announce`（`/opt evilnekomata.*`）、短縮形 `neko` `nekomata`
- シリアルキラー（インポスター枠）: キルクールダウンが短い代わりに、前のキルから一定時間キルしないとその場で自滅（自分で自分をキルする演出）。タイマーはキルのたびにリセット、会議中は停止、既定では会議後にリセット。残り時間は本人の名前タグ（5 秒刻み）と、残り 10 秒（制限が 20 秒未満なら半分）のチャットで通知。イントロの数秒後と会議の約 2 秒後にキルボタンを短いクールダウンに合わせ直す（シールドの演出が一瞬出る）。`[SerialKiller] KillCooldown / SuicideTime / ResetAtMeeting`（`/opt serialkiller.cooldown|time|meetingreset`）、短縮形 `sk`
- 侍（インポスター枠）: キルが斬撃になり、キルした相手に続いてその瞬間に周囲にいた人が約 0.3 秒間隔で次々に死ぬ（既定で仲間は巻き込まない。勝敗判定とベイトの自動通報は斬撃が終わるまで待つ）。`[Samurai] KillCooldown / Range / Stagger / HitTeammates`（`/opt samurai.*`）、短縮形 `sam`
- マッド系役職の共通ルール: `[Sheriff] CanKillMadmate`、`[Madmate] KnownToImpostors`（Ⓜ。マッドメイヤーは `[MadMayor] KnownToImpostors`）、インポスター勝利の人数判定はマッドメイト・マッドメイヤー・マッドスタントマン・マッドホーク・崇拝者（と崇拝された人）に適用。設定の説明文・`/cmd r sheriff` / `madmate` の文言、シェリフの役職説明を更新（`lang/*.json` は上書きコピーが必要）
- マッドメイト: バニラ役職オンでエンジニアがマッドメイトになった時（`/assign` で付けた場合・崇拝された場合）、ベントから追い出さなくなった
- テストモード: `/test on` 中は勝敗判定の人数をログに出す（`WinConditions(test): outcome=… crewForCount=…`）。会議の開票時、メイヤー系の票の重みは `Meetings.VoteWeightOf` で共通化
- 説明書 3 言語に 9 役職の説明・設定・見え方を追記。README の版数表記を 0.5.0 に更新（0.4.2 / 0.4.3 では未更新だった）
- v0.4.4 と v0.4.5 の修正・新機能（下記）をすべて取り込み
- **バグ探索（2026-09-10、エージェント 110 体・確定 30 件）の修正**: ①死んだホストの「一時的に生存扱い」の解除が、同じフレームの予約処理と重なると戻らず、ホストが生存のまま扱われることがあった ②恋人の後追い・侍の巻き込みで死ぬことが確定した人を生存として数え、直後に負けるはずのインポスターが勝ちになることがあった ③ホスト自身の役職別クールダウン（シェリフ・ジャッカル・放火魔・魔女・崇拝者・シリアルキラー・侍）がイントロ後・会議後・自分のキル後にロビーのキルCDに戻っていた ④AFK キック／シリアルキラーの警告・会議後のリセットが複数人分同じフレームに出ていた ⑤登録オフの全体チャットで改行を「 / 」に変える分だけ末尾が切れていた ⑥ホストのヘルプにキー重複で観戦の行が出ず案内の行が 2 回出ていた ⑦設定タブの「−」が最小値が刻みに乗っていない項目で最大値に回り込んでいた ⑧アサシンの推理キルが結果のキル数・役職公開に入らず、投票終了の再判定が結果表示後にも走っていた ⑨/cmd n・/cmd r・/who・/opt の一覧の上限で英語の説明が切れていた ⑩死亡後の役職一覧の色が最初の 1 人しか付かなかった、会議直前の死亡で一覧が 2 回出た ⑪逆スケルドをオフにしても選択中のまま次の試合が鏡になっていた ⑫廃村の取り消しが無言だった ⑬ホスト移譲で引き継いだ部屋を Discord に「役職あり」と投稿していた ⑭シェリフ・シリアルキラーの設定表示、KickOnForgedRpc の説明、/opt のキー一覧、中国語の設定名、言語ファイルの欠けたキーなど。
- **公開部屋のテスト（2026-09-13、14 人の便利ホスト部屋）からの修正**: ①試合結果に最後まで残っていた人しか載らなかった（14 人中 5 人）→ 開始時の全員を載せ、途中でぬけた人は「退」印（勝者にはならない） ②結果を陣営ごとにまとめ、インポスター（陣営）の行を最初に。登録オフでは 1 通が名前の途中で切れないよう行ごとに分けて送る（v0.4.6 は改行分だけ末尾が切れていた） ③翻訳: 漢字だけの日本語（「最終通信」「最終十字路配線」）を中国語と判定して日本語に「翻訳」し、その人の表示言語まで中国語にしていた／英単語 1 つ（always, ake …）で英語に切り替え、以後その人の `/cmd` の返事が英語 4 行で全員に流れていた → 文字種を自前で判定（かな＝日本語、中国語専用の字＝中国語、漢字だけは 8 文字未満なら部屋の言語、英単語 1 つは翻訳しない）、案内の言語の切り替えは同じ外国語 2 回目から・部屋の言語で書いたことのある人は切り替えない、登録オフの切り替え通知には部屋の言語の説明を添える ④ホスト自身の外国語チャットも部屋の言語の訳を流す（`[Translate] BroadcastToAll` オンの時） ⑤参加者向けの案内（挨拶の 2 行目・`/cmd h`・`/cmd s`・`/lang`）の文言を平易に（「コマンド」「ja|zh|en」をやめ「English は /lang en」の形に）、翻訳の見出しを「訳」→「翻訳」、結果の凡例を「勝った人／死んだ人」に。`lang/*.json` は上書きコピーが必要
- **本体役職の秒数も範囲拡張**（2026-09-13 の要望）: サイエンティストのバイタル CD・表示時間、エンジニアのベント CD・ベント内時間、シェイプシフターの CD・変身時間、ファントムの CD・透明時間、守護天使の CD・守護時間、トラッカーの CD・遅延・追跡時間、ノイズメーカーの警報時間、ヴァイパーの溶解時間、探偵の疑える人数、ジャッジのタスク割合、バイタルを使えるクルー残数、クルーのベント回数を、役職設定ページの矢印と `/vset`（`vitalscd` `vitals` `ventcd` `venttime` `shiftcd` `shift` `phantomcd` `phantom` `gacd` `ga` `trackcd` `trackdelay` `track` `noise` `viper` `detective` `judge` `vitalscrew` `ventuses`）で本体の上限・下限の外に設定できます（秒は 0〜600）。本体の範囲は役職の設定データから読みます。**登録オフの部屋でも使えます**（公式サーバーはこの項目を検証しません: バイタル 40/45 秒・変身 CD 3 秒で同期・入室ともキックなし。登録オフでの丸めは役職の秒数には掛けません）。`/vset show`（`/vset` だけでも）で現在値の一覧
- 試合結果の投稿は、ロビーに戻ってから最初の参加者が入るまで（最大 1 分）待ってから送るようにしました（v0.4.6 はホストが戻った 2 秒後に送るため、全員がまだ終了画面にいると誰にも届かなかった）

## v0.4.6 — 2026-09-10

- **新機能: 死亡後の役職一覧（ホストだけに表示）**（`[Roles] HostGhostRoleList`、既定 on、`/opt ghostlist on|off`）: ホストが死ぬと 1.5 秒後に、自分の画面だけに【役職一覧】として生存者と死亡者の役職が出ます。会議が始まるたびに再表示し、以後の死亡は「【死亡】名前: 役職（生存 N 人）」を 1 行ずつ。`/who` でいつでも再表示（生存中は使えません）。登録オフ（便利ホスト）の部屋でも本体の役職名（インポスター・ジャッジ…）で動きます。他の人には一切送りません（チャット通信なし）。
- **新機能: 登録オフ（便利ホスト）の部屋でも試合結果を投稿**: 試合が終わってロビーに戻ると「前回の結果: インポスター勝利 / ×名前:ジャッジ 勝名前:ヴァイパー … / キル数: 名前=2」を全員向けに投稿し、`/cmd l` でも見られます（本体の役職・勝者・死亡・キル数）。役職ありの部屋の結果にも **キル数の行** が付きました。登録オフでは参加者の `/cmd s` は 4 通の役職一覧ではなく 1 行の案内だけになりました（役職はないので）。
- **新機能: ロビーの AFK キック**（`[Lobby] AfkKickMinutes`、既定 0 = しない、`/opt afkkick <分>`）: ロビーでその分数だけ動きも発言もない人に 30 秒前に「動くか発言しないと退出になります」と伝え、それでも動かなければ退出（BAN ではありません）。ホスト・VIP・モデレーター・アドミンは対象外、開始カウントダウン中と試合中は何もしません。登録オフの部屋でも使えます。
- **修正: v0.4.5 の「キル直後の会議を 3 秒待つ」の穴。** 待っている間に通報者本人がヴァンパイアの噛みつきで死ぬと会議が消える／待っている間のキルで暗転が再発する → 待ち中はキルを断り、噛みつき・シリアルキラーの死亡を止め、待ち明けにもう一度条件を確かめてから会議を始めます（キルが割り込んだら 1 回だけ延長）。待ち時間は 1.6 秒では足りず 3 秒に。
- **修正: ホスト移譲で引き継いだ他人の部屋（登録なし）や LAN で、アンチチートがキルを全部捨てていた**（死体が出ない・通報が通らない）。役職の処理が動いていない部屋では何も落としません。
- **修正: 通報時にまとめて実行される死（噛みつき）から連鎖する死（ラバーズの後追い）が会議前に落ちなかった**／守護天使に守られたキルで死亡処理が走っていた。
- **修正: 登録オフの部屋の全体チャットに色タグ（`color=#…`）がそのまま見える、複数行の文が 1 行にくっつく**（` / ` 区切りに）。
- **修正: 役職ありの試合で「X は クルーメイト でした」と間違う**（ホストの表示用役職を拾っていた）。追放後の役職公開と魔女の呪い通知は追放画面が終わって 2 秒後に（画面内に出すと暗転の原因）。
- **修正: 試合終了時に送りかけの分割パケットが残り、勝敗画面に幽霊役職が混ざる**ことがあった。
- **修正: 自動開始（残り時間ルール）**が延長のあと止まったまま／オフ→オンで再開しない。開始ボタンの保護と開始処理の直列化はホストの時だけ（`/mod off`・かくれんぼ・移譲された部屋では触らない）。
- **修正: アドミンが `tr.*`（テロリストの略）経由で翻訳設定を変えられた**（役職の数・確率だけに限定）。`/welcome show` が登録オフの部屋で実際と違う文面を見せていた。`/move` の 30 秒後の作り直しが試合開始で取り消されない／失敗しても登録フラグが変わっていた。
- **修正: Discord 投稿**: 失敗した投稿・編集を再試行（429 の retry_after に従う）、前の部屋の返事を無視、サーバー切断時に「閉じました」に、`?thread_id=` 付き URL でも編集できる、オンラインの部屋だけ。
- **修正: 登録オフ・廃村の試合で死者のチャットが翻訳で生存者に流れていた。** 幽霊の発言は幽霊にだけ。
- 検証: PC ホスト＋エミュレータ 2 台で、役職ありの部屋（死亡→一覧、会議→再表示、追加の死亡 1 行、`/who`、結果とキル数）と登録オフの部屋（死亡→本体役職の一覧、ヴァイパーのキル、終了→結果投稿、`/cmd h` `/cmd r` `/cmd s`）を通しで確認。

## v0.4.5 — 2026-09-09

- **修正: キルの演出中に会議が始まると、参加者の画面が暗転したまま戻らない。** 実際の部屋で、ヴァンパイアに噛まれた人（シェリフ）の死が緊急ボタンと同じ瞬間に確定し（噛みつきは本人が自分を倒す形で実行されるので本人には「シェリフの自爆」に見える）、その 0.02 秒後に会議が始まって画面が真っ暗のまま固まりました。ホストはキル（噛みつき・道連れ・普通のキルすべて）から 3 秒以内は会議の開始を待つようにしました
- **Discord に部屋コードを自動投稿**（`[Discord] WebhookUrl`）: Discord のチャンネルの「連携サービス → ウェブフック」で作った URL を設定に貼るだけ（Bot 不要）。部屋を作ると「🔑 部屋コード **ABCDEF** — 3/15人 募集中（役職あり）」を 1 回投稿し、入退室・ゲーム開始・終了のたびに同じ投稿を書き換えます（5 秒に 1 回まで）。部屋を閉じると「閉じました」に。ゲームが落ちた・タスクマネージャーで閉じた場合も、次に部屋を作ったときに前の投稿を「閉じました」に直します（`BepInEx\PocketRoles\discord-last.txt` に投稿の番号を控えます）。`[Discord] Text` で文面を変えられます（`{code} {count} {max} {state} {kind}`、`@here` も可）。URL はチャットからは変更できません（設定ファイルのみ）

## v0.4.4 — 2026-09-09

- **修正: 登録オフ（便利ホスト）の部屋で、ホストが「普通のクルー」だった試合が開始直後に真っ暗のまま固まる（イントロが始まらない）。** 2026.8.18 の本体は特殊役職の人にしか役職通知を送らないため、登録オンの部屋では mod が「普通のクルー／インポスター」も全員に通知して防いでいましたが、登録オフの部屋ではその処理を止めていました。登録オフ（と廃村の使い捨て試合）でも本体と同じ形式の一斉通知を行うようにしました。固まった試合を F7 で強制終了した直後にサーバーから「Hacking」で切断された事例（15 人）への対処でもあります
- 登録オフの部屋の挨拶を分かりやすく: 「ようこそ! この部屋は普通のAmong Us(役職なし)です。何も入れなくてOK、そのまま遊べます。困ったら /cmd h」。`[Chat] CompatWelcomeText` で自分の文にできます。参加者向けの `/cmd h` は登録オフ専用の短い説明に、`/cmd n` の返事も「役職なしの普通の Among Us です」に変更
- **修正: 登録オフの部屋で、キルされた人が幽霊として壁をすり抜けて見える／死体があるのに通報が通らない（「死体なし」扱い）。** 登録オフ（ホスト権限なし）の部屋では 2026.8.18 のバニラ参加者がキルを `MurderPlayer` として直接送りますが、mod のアンチチートがそれを「偽装 RPC」として捨てていたため、ホストだけがキルを認識していませんでした。登録オフの部屋ではアンチチートの RPC 破棄を行わないようにしました（登録オンの部屋は従来どおり）
- **修正: 登録オフの部屋で入室のたびに全員へ挨拶が流れ、混雑時は 1 分に 10 回以上「ようこそ」が出ていた**（「静かにー」「通知溜まるー」と言われる原因）。登録オフの挨拶は `[Chat] CompatWelcomeInterval`（既定 60 秒）に 1 回だけに
- 新設定 `[Roles] RevealRoleOnDeath`（`/opt roles.reveal on|off`、既定 off）: キル・追放された人の役職を全員に知らせる（「X は シェリフ でした」。役職なしの部屋では本体の役職名）
- 友達に編集を任せるときの安全策: アドミンが使えるのは既定で「役職の数・確率・役職設定、挨拶文・ルール文、キック/BAN・モデレーター/VIP 管理」だけに。試合の強制開始・`/vset`・ロビータイマー設定は `[Permissions] AdminLobbyControl`（`/opt perm.adminlobby on`）を付けた時だけ。`/backup`（設定をまるごと保存）と `/restore`（`/backup` 時点へ戻す）／`/restore startup`（ゲーム起動時の状態へ戻す）を追加。登録オフの部屋では `/welcome <文章>` が入室時の 1 行挨拶を変更
- `[Vanilla] ClampInUnregistered`（既定 on）: 登録オフの部屋を作るときにバニラ範囲へ丸める処理を切れるようにしました（off = タスク数などの拡張値をそのまま使う。サーバーに切断されるリスクはホスト持ち）。`lang/*.json` の文言変更は上書きコピーが必要です

## v0.4.3 — 2026-09-09

- **登録オフ（便利ホスト）の部屋で、参加者が入室した瞬間にホストが「Hacking」で切断される不具合を直しました。** 原因は、入室時にホストが流す歓迎チャットに、バニラのチャット欄では打てない文字（`[` `]` `！` `（` `）`）が含まれていたことでした。公式サーバーは「プレイヤーが打てない文字を含む公開チャットをホストが流す＝不正」と見なしてホストを切断していました。これからは便利ホストの部屋で流す全チャットを、バニラで打てる文字だけに自動で整えます（全角→半角、`[` `]`→`【` `】` など）。**登録オフの部屋でも、もう入室で落ちません。逆スケルドも登録オフの部屋で出せます。** 役職ありは従来どおり登録オンの部屋で
- 便利ホストの部屋で名前を付けて返信するとき、これまでの `@名前` を `名前:` に変えました（`@` もバニラでは打てない文字のため）
- 調査用に、送受信パケットを 1 行ずつ記録する隠しオプション `[Diagnostics] WireLog`（既定オフ）を追加しました

## v0.4.2 — 2026-09-09

- 逆スケルド（Dleks）を AUR と同じ方式に作り直しました。設定上のマップは常に The Skeld（MapId 0）のままで、ホスト側でマップの実体（ShipPrefabs[0] と [3]）だけを入れ替えます。以前はゲーム開始時に MapId を 3 に書き換えていて、登録オフの部屋でサーバーがホストを「Hacking」として切断する一因になっていました。新方式ではサーバーにバニラ以外の値が一切渡らないので、登録オン・オフどちらの部屋でも Dleks を出せます（`[Lobby] DleksWhenUnregistered` は不要になり削除）
- 登録オフ（便利ホスト）の部屋を作るとき、キルクールダウンや視界などの設定を自動でバニラの範囲・刻みに丸めるようにしました。登録オンの部屋で使える拡張範囲の値（例: インポスター視界 10 倍、緊急会議クールダウン 27 秒）は、登録オフの部屋では公式サーバーの検証に落ちてホストが切断されるためです
- 部屋作成時に、現在のバニラ設定が公式の検証を通るかをログに出すようにしました（不具合調査用）
- 既知の不具合: 登録オフ（便利ホスト）の部屋は、参加者が入ってきた瞬間にホストが切断される場合があります（Dleks とは無関係の、以前からある互換モードの制限）。役職ありは登録オン（既定）の部屋で遊んでください

## v0.4.1 — 2026-09-09

- 役職を 4 つ追加: ラバーズ（第三陣営・2 人 1 組）、放火魔（第三陣営）、魔女（インポスター枠）、アサシン（インポスター枠、会議中の `/cmd guess`）。シェリフは放火魔も撃てるようになった
- 設定: `[Lovers] AllowImpostor / WinAsLastThree`、`[Arsonist] DouseCooldown / CanVent`、`[Witch] SpellCooldown / SpelledSeeMark`、`[Assassin] GuessesPerMeeting / CanGuessFirstMeeting`（設定タブ「役職」ページ、`/opt lovers.* arsonist.* witch.* assassin.*`）。`[Roles] Lovers.Count` は 0 / 1（組数）
- 名前タグの印: ラバーズは相手に ♥、魔女は呪った相手に †（`SpelledSeeMark` で本人にも）、放火魔は油をかけた相手に ♨。役職チャットにラバーズの相手の名前、会議の再送に魔女の呪い中の一覧
- 会議中の死亡（アサシンの推理）に対応: 会議画面でその場で ✕、投票済みの票は取り消し、メイヤー集計からも除外。魔女の呪いとラバーズの後追いは追放画面の約 2 秒後に実行（ヴァンパイアの噛みつきと同じ遅延死亡の仕組み）
- アサシンは `[Chat] PlayerCommands` / `AllCommands` がオフの部屋では配られない（ホストに案内）。短縮形 `lv` `ars` `wt` `as` を追加。説明書 3 言語に 4 役職の説明・設定・見え方を追記
- **修正（重大、v0.4.0 から）: 参加者のシェリフ・ジャッカル・放火魔にキルボタンが出なかった。** 2026.8.18 のクライアントは最初に受け取った役職の後の上書きを無視するため、役職の見え方を「最初の割り当て」として各参加者に送る方式に変更（本体の役職放送は止めてホスト側だけ実行）。スマホ 2 人テストで放火魔のインポスター表示・キルボタン・油かけを確認
- 修正: 自分がインポスター扱いの役職（シェリフ等）の参加者が死んだときの幽霊役を本人にはインポスターの幽霊に（クライアントと食い違って画面が壊れるのを防ぐ）。幽霊役は 1 人 1 通で送信
- 修正: ホスト自身がシェリフ・ジャッカル・放火魔（クライアント上はインポスター扱いの役職）や、`/assign` でインポスター枠の役職になったとき、ホストの画面がクルーのまま（キルボタンなし・本物のタスク）になっていた。2026.8.18 では割り当て後の `CoSetRole(canOverride)` がホストに効かないため、ホストの役職表を `RoleManager.SetRole` で直接書くようにした（v0.4.0 から存在した不具合。参加者側は 2 人テストで確認）
- **修正（重大、v0.4.0 から）: 参加者が 2 人以上いると開始直後にホストがサーバーから切断されていた（"Hacking"、ban ポイント付き）。** 原因は複数の参加者宛てのメッセージを 1 つのパケットにまとめて送っていたこと。1 パケット 1 宛先に変更し、全参加者宛ての送信（役職の見え方・幽霊役・会議前の名前）を 0.3 秒間隔に（TOHE と同じ）
- **修正（重大、v0.4.0 から）: インポスターが 1 人もいない試合になっていた。** 本体の特殊役職を出さない設定（既定）で Shapeshifter / Phantom / Viper の枠を 0 にすると、本体がインポスターを配らなくなる（2026.8.18 の役職選択の仕様）。特殊役職は本体に配らせて素のインポスターに格下げする方式に変更。本体がインポスターを配らなかった試合では平のクルー 1 人を自動で昇格させる安全網も追加。テストモード中は 3 人でもインポスターを 1 人割り当てる
- 修正: メインメニューの MOD パネルが本体のポップアップ（「ルームから追い出されました」など）に被っていた。ポップアップ表示中はパネルを隠す
- 登録オフ（便利ホスト）の部屋では逆スケルド（Dleks）をマップ選択に出さない・開始しない（登録オフで Dleks を選んだ直後にサーバーがホストを Hacking として切断した実例への対処。README §25.4）
- 入室時の挨拶を初見向けに短く（「役職MOD部屋です。何も入れなくてOK。役職は試合が始まると自分の名前の上に出ます。」＋役職説明の見方 `/cmd r 役職名` の 2 行。3 言語既定オン `[Chat] WelcomeAllLanguages = true`、設定の羅列は挨拶から外して `/cmd s` へ。`[Chat] WelcomeIncludeSettings` は既定オフ、ルール行は `/rules` で設定した時だけ）。シェリフ・ジャッカル・放火魔の通知（開始時・会議時・`/cmd n`）に『本体の表示はインポスターですが本当の役職は…』を追加。参加者向けコマンド `/cmd s`（`settings` / `設定`）を追加、`/cmd h` に案内

## v0.4.0 — 2026-09-08（初公開）

v0.1〜v0.4 を 1 つにまとめた最初の公開版です（旧名 HostRoles → PocketRoles）。

**v0.4e（仕上げ・公開直前の修正）**
- 2 人テストで発見: タスクを数える役職が誰もいない試合（例: シェリフ＋マッドメイトだけ）で本体が「タスク 0/0＝完了」と判定し、緊急会議の要求を黙って拒否していた → タスク総数が 0 のときは 1 として扱い、本体の終了要求を拒否した後は本体の判定フラグを戻すようにした
- ログの「ブラックボックス記録」: 詳細トレースを常に裏で記録（直近 400 行）し、開始が固まった時・会議要求が通らなかった時に自動でログへ書き出す。`/diag dump` で手動書き出し
- 本体 2026.8.18 の役職割り当て（特殊役職だけを通知し、素のクルー／インポスターには通知しない方式）に対応: MOD が素の役職も通知するので、ホストが素のクルーでもイントロが正常に流れる（公開前の最終検証で発見・修正）
- 本体の特殊役職（サイエンティスト・エンジニア・探偵・ジャッジなど）は既定で配らない（`[Roles] VanillaRoles`、設定タブ「本体の特殊役職も配る」、`/opt roles.vanilla on` で併用）
- 説明書・設定ラベルから「+25」の業界用語を外し「MOD部屋登録（公式ルール・役職に必須）」に統一。メニュー右窓の説明文を短縮、設定画面「▼ バニラ設定」展開時のボタン配置を詰めた
- 監査で確認した 19 件を修正（作成画面の Dleks 選択、翻訳 TargetLang、全角数字のコマンド引数、キルクール 0 秒、テストモードの勝敗状態、ランチャーの展開済み zip・Steam ライブラリの文字コード・途中で止まったコピーの修復・OneDrive デスクトップ、LICENSE 全文 など）
- 案内部屋の支援: ロビー左上に部屋コードを大きく表示（`/code on`。既定オフ）、`/announce` でコードをコピーして手順表示、便利ホスト部屋から役職部屋への移動案内 `/move`（`[Guide]` 設定 3 つ）、歯車メニューのヒント
- 便利ホスト（登録オフ）の部屋では役職・個別メッセージを配らずバニラ進行に（未登録部屋の個別メッセージでホストが切断されるため）
- チャット翻訳の併用モードが既定（全体流し + 外国語の人への個別訳。同じ人に 2 通届かない）。翻訳自体も既定オン（チャットの文章が Google、または `deepl-key.txt` にキーがあれば DeepL に送られます。設定タブ「会話」の「チャット翻訳」か `/opt translate off` でオフ）
- 高 PING の部屋の作り直しを確認ダイアログ式に（`/rehost yes|no`）。既定オフ（`MaxHostPing = 0`）
- 設定タブ: ページ名を「役職 / ロビー / 会話 / 見た目 / ホスト」に、バニラ 3 ボタンを「▶ バニラ設定（ゲーム設定・プリセット・ロール）」に折りたたみ、ホストページに操作ボタン列（今すぐ開始 / キャンセル / 廃村 / 会議終了 / テストモード / 設定を表示）
- 2 人以上で開始した時に真っ暗になる問題を修正（本体の役職送信を通してから全員分の見え方を一括で上書き）
- テストモード: 開始ボタンが即「開始」、少人数のポップアップを自動 OK。`/diag on|off` の詳細トレース。言語ファイルの不足キーを起動時に追記。ログの静音化

**v0.4c**
- `/diag` 診断、F9 キャンセルがチャット入力中でも有効、廃村・「もう一度プレイ」後の状態リセット、ロビー時間切れバナーを設定画面中だけ非表示、PING 行に地域名、タイトル画面の「GitHub を開く」ボタン、Dleks アイコン

**v0.4b**
- ランチャーの友達用インストーラー（Steam 版のコピー → BepInEx → PocketRoles）、更新チェック、報告 zip
- タイトル画面の PocketRoles パネル
- チャット翻訳（Google / DeepL）、言語の自動判定、3 言語の案内行、`WelcomeAllLanguages`
- 設定タブの整理（ページボタン、「?」ヘルプ、バニラ設定ボタン）
- 権限: アドミン / モデレーター / VIP / BAN（`Admin.txt` など、`/admin` `/mod` `/vip` `/kick` `/ban`）
- バニラ設定の範囲拡張（キルクールダウン 0〜120 秒など、`/vset`）
- Issue テンプレートとサポート宛先

**v0.4**
- 名前を HostRoles から PocketRoles に変更（設定は自動移行）
- ロビー残り時間の表示、自動開始、時間切れ対策（延長 / 廃村）、廃村 `/haison`、キャンセルボタン、ホットキー F7 / F8 / F9、会議の強制終了 `/endmeeting` `/results`、地域の自動選択、歯車メニューの「PocketRoles 設定」パネル、ゲームマスター、コマンド無効化、挨拶のルール行 `/rules`、逆スケルド（Dleks）

## v0.3 — 2026-09-07

- ホストの画面だけの見た目カスタマイズ: 帽子・バイザー・ネームプレートの差し替え（`/cos ids`）、ロビー BGM、壁絵、ドロップシップ装飾、メニュー背景、カーソル

## v0.2 — 2026-09-07

- ロビーの設定画面に「PocketRoles」タブ
- 日本語 / 简体中文 / English（`lang\*.json`、参加者ごとの `/lang`）
- 挨拶文の編集 `/welcome`、自動再ホスト・自動公開、テストモード（`/test` `/assign` `/end`）、ゲームのバージョンチェック、登録オフ時のセーフモード、ランチャー、クレジット行

## v0.1 — 2026-09-07

- 最初の版（HostRoles）: 13 役職（シェリフ、メイヤー、スニッチ、ライター、スピードブースター、ベイト、マッドメイト、ヴァンパイア、マフィア、ジェスター、オポチュニスト、テロリスト、ジャッカル）、名前タグとチャットによる本人だけへの通知、チャットコマンド、ブラックアウト対策、簡易アンチチート、MOD 部屋登録（いわゆる +25）、更新スクリプト

---

Free and non-commercial. This mod is not affiliated with Among Us or Innersloth LLC. © Innersloth LLC for the game's materials.
